# Rust toolchain configuration for AffineDrift.
# Pins the same stable channel as all other flagship repos for consistency.
# Includes WASM target for web-based simulation delivery.

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
