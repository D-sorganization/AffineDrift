--- .github/workflows/quarto-syntax-check.yml
+++ .github/workflows/quarto-syntax-check.yml
@@ -32,5 +32,5 @@
       # Assessment E: Security scanning for dependency vulnerabilities
       - name: Audit Python Dependencies
         run: |
-          pip install pip-audit
-          pip-audit -r requirements.txt --progress-spinner=off || true
+          python3 -m pip install pip-audit
+          python3 -m pip_audit -r requirements.txt --progress-spinner=off || true
