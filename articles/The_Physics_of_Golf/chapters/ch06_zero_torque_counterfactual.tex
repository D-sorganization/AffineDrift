% Chapter~\ref{ch:zero_torque_counterfactual}: The Zero-Torque Counterfactual
% The Physics of Golf: Force, Drift, and Control in the Golf Swing

\chapter{The Zero-Torque Counterfactual}
\label{ch:zero_torque_counterfactual}

\epigraph{%
What does this exact model compute when one declared generalized-torque channel is set to zero? \\
The answer is conditional on the intervention record.
}{---A thought experiment in constraint physics}

\begin{laymansbox}{The Central Question}{context:ztcf_motivation}
The model assigns terms to a declared autonomous plant and to declared input
channels. That bookkeeping does not identify individual muscle forces or divide
a real swing into physiology versus physics.

The \textbf{forward Zero-Torque Counterfactual (ZTCF) trajectory} is a model intervention. It asks: \textbf{What trajectory follows when the declared applied generalized-control channel is set to zero while the declared effective plant is retained?}

The model result is counterintuitive. Its forward ZTCF trajectory---obtained by zeroing the declared applied generalized-control channel while retaining the effective plant---reproduces part of the downswing shape. This model comparison does not identify muscle activation or establish that biological control is primarily restraining.
\end{laymansbox}

%============================================================================
\section{Formal Definition of the ZTCF}
\label{sec:ztcf_formal_definition}

The concept of analyzing passive motion is foundational in biomechanics. By studying what a system does without active control, researchers can isolate and understand the contribution of passive forces \citep{Nesbit2005, MacKenzie2009}.

\begin{definition}{The Zero-Torque Counterfactual}{ztcf}
The \textbf{forward Zero-Torque Counterfactual trajectory} is $\bm{q}_{\text{ZTCF}}(t)$ obtained by solving the declared effective-plant equations from the current state $(\bm{q}(t_0), \dot{\bm{q}}(t_0),\bm{z}(t_0))$ with the declared applied generalized control set to zero:
\begin{equation}
\bm{M}(\bm{q})\ddot{\bm{q}} + \bm{h}(\bm{q},\dot{\bm{q}},\bm{z};\bm{p}) = \mathbf{0},
\qquad \bm{u}(t) \equiv \mathbf{0}
\label{eq:ztcf_eom}
\end{equation}

The retained term $\bm{h}$ must be declared and may include Coriolis/centrifugal effects, gravity, passive joint impedance, shaft dynamics, damping, and modeled contact or constraint reactions. Exogenous loads and contact modes follow the stated protocol. Zero declared control does not imply zero muscle activation, zero co-contraction, or a flaccid body.
\end{definition}

\section{Normative ZTCF Contract}
\label{sec:ztcf_contract}

Every executable or quantitative record uses
\texttt{affinedrift.ztcf-intervention/v1}. The published normative schema and
Python golden fixture freeze model/version/revision, inputs, state, units,
frame, retained controls/constraints/contact/loads/internal states,
solver/version, horizon, preconditions, postconditions, and failure states. The
ZTCF is a model-conditioned intervention. The record separates simulated trajectory difference
from any contribution measure. It states each causal estimand and physiological interpretation,
and records non-identifiability explicitly. Unavailable or engine-unsupported
interventions fail closed; no MATLAB, Simulink, or cross-engine parity result is
inherited from the Python fixture. The public records are
\url{https://affinedrift.com/data/ztcf/ztcf_intervention_v1.schema.json} and
\url{https://affinedrift.com/data/ztcf/planar_golf_forward_fixture_v1.json}.

\begin{laymansbox}{Understanding the ZTCF}{inplain:ztcf_meaning}
Think of this as a software intervention rather than a physiological event. At a selected state, the simulator sets the declared applied generalized-control vector to zero while retaining the effective plant and protocol-specified loads. It then evaluates either an instantaneous sample or a forward rollout.

The answer is the trajectory of the retained effective plant. Its forces are
exactly those listed in the intervention record; they are not assumed to be
purely gravitational, inertial, or biologically passive. The rollout is a
software branch, not a measured muscle state, and cannot uniquely allocate a
difference to activation, effort, strategy, or omitted forces.

Agreement with measured motion supports the adequacy of that model and intervention over the tested interval; it does not establish low EMG or relaxation. Divergence can reflect control input, parameter error, omitted forces, contact mismatch, or numerical error.
\end{laymansbox}

%============================================================================
\section{The Decomposition: What the ZTCF Reveals}
\label{sec:ztcf_decomposition}

At a fixed state, the acceleration block admits the pointwise decomposition
\begin{equation}
\bm{a}(\bm{x},\bm{u}) = \bm{a}_{\text{drift}}(\bm{x}) + \bm{B}_{a}(\bm{x})\bm{u}
\label{eq:torque_decomposition}
\end{equation}

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=1.2]
% Timeline from backswing through impact
\draw [thick, ->] (0, 0) -- (10, 0) node [right] {Time (swing phase)};

% Swing phase labels
\node at (0.5, -0.5) [font=\small] {Top};
\node at (2.5, -0.5) [font=\small] {Early Down};
\node at (5, -0.5) [font=\small] {Mid Down};
\node at (7.5, -0.5) [font=\small] {Late Down};
\node at (9.5, -0.5) [font=\small] {Impact};

% Draw two curves: actual torque and ZTCF torque
% ZTCF grows gradually then sharply (passive dynamics)
\draw [thick, red, smooth] (0, 1) to (1, 1.2) to (2.5, 2.0) to (5, 3.5) to (7.5, 6) to (9.5, 9);
\node at (1, 1.5) [font=\small, color=red] {$\tau_\text{ZTCF}$};

% Actual torque (more controlled, may not grow as fast)
\draw [thick, blue, smooth] (0, 1) to (1, 1.3) to (2.5, 2.2) to (5, 3.5) to (7.5, 4.5) to (9.5, 5.5);
\node at (7, 2.5) [font=\small, color=blue] {$\tau_\text{actual}$};

% Difference (control input)
\draw [thick, green, smooth] (0, 0.05) to (1, -0.1) to (2.5, -0.2) to (5, 0) to (7.5, 1.5) to (9.5, 3.5);
\node at (8.5, 0.8) [font=\small, color=green] {$\tau_\text{input}$};

% Add horizontal reference lines and annotations
\draw [dashed, gray] (0, 3) -- (10, 3);
\node at (-0.5, 3) [font=\tiny] {Ref};

% Shaded regions for DCR regimes
\draw [fill=lightblue, opacity=0.1] (0, 0) rectangle (2.5, 10);
\node at (1.25, 9.5) [font=\tiny] {Low DCR};

\draw [fill=lightyellow, opacity=0.1] (2.5, 0) rectangle (5, 10);
\node at (3.75, 9.5) [font=\tiny] {Medium DCR};

\draw [fill=lightcoral, opacity=0.1] (5, 0) rectangle (10, 10);
\node at (7.5, 9.5) [font=\tiny] {High DCR};

% Add y-axis label
\node at (-0.8, 5) [rotate=90, font=\small] {Torque};

\end{tikzpicture}
\caption{Schematic model comparison of a declared drift-equivalent generalized
quantity (red) and a separately specified reference branch (blue). The green
difference is illustrative only; without an intervention record it is not a
validated trajectory, causal effect, or physiological attribution.}
\label{fig:ch06_ztcf_timeline}
\end{figure}

where:
\begin{itemize}
\item $\bm{a}_{\text{drift}}$ is the acceleration induced by the declared autonomous effective plant.
\item $\bm{B}_{a}\bm{u}$ is the acceleration induced by the declared applied generalized-control channel.
\end{itemize}

Calling a drift term a ``ZTCF torque'' is avoided because generalized force and acceleration are different representations. Any force-space decomposition must declare its mass matrix, Jacobian, projection, and constraint treatment.

\begin{principle}{Generalized Input Is Not Physiological Attribution}{principle:control_vs_total}
Net generalized torque, the modeled input term, individual muscle forces,
activation, co-contraction, impedance, and effort are distinct quantities. A
pointwise affine decomposition is exact within its declared model, but it does
not identify the biological source of either term.
\end{principle}

\begin{laymansbox}{What This Analysis Can Support}{inplain:ztcf_application}
A registered ZTCF can compare model branches, test implementation closure, and
quantify a declared task difference. It cannot prescribe setup, timing,
relaxation, or coaching cues. Human-performance conclusions require measured
participants and an analysis that separates model discrepancy from the named
intervention.
\end{laymansbox}

%============================================================================
\section{Computing the ZTCF: Algorithm and Simulation}
\label{sec:ztcf_algorithm}

\begin{algorithm}{Computing the ZTCF Trajectory}{proc:ztcf_algorithm}
Given the current state $\bm{x}(t_0) = (\bm{q}(t_0), \dot{\bm{q}}(t_0))$ at time $t_0$:

\begin{enumerate}
\item \textbf{Capture the state:} Record the joint angles and angular velocities at the moment of interest (e.g., at mid-downswing).

\item \textbf{Set up the passive equations:}
\begin{equation}
\ddot{\bm{q}} = \bm{M}(\bm{q})^{-1} \left[ -\bm{C}(\bm{q}, \dot{\bm{q}})\dot{\bm{q}} - \bm{g}(\bm{q}) \right]
\label{eq:ztcf_acceleration}
\end{equation}
This equation gives the acceleration from the terms retained in this simplified declared model.

\item \textbf{Integrate:} Numerically integrate this ODE from $t_0$ forward in time using a standard solver (e.g., RK4). The declared applied generalized-control channel is zero; the retained effective plant determines the branch.

\item \textbf{Report the branch:} Compare only under a declared metric and uncertainty model. Divergence is not uniquely attributable to active control.
\end{enumerate}
\end{algorithm}

\begin{example}{Generic Branch-Switch Pattern}{ztcf_simulink}
A branch-switch architecture proceeds by:

\begin{enumerate}
\item Registering the forward-dynamics model and retained-force inventory.

\item Including a \textbf{``kill switch''} parameter, a binary flag $k_u$ that multiplies the input torques:
\begin{equation}
\bm{M}(\bm{q})\ddot{\bm{q}} + \bm{C}(\bm{q}, \dot{\bm{q}})\dot{\bm{q}} + \bm{g}(\bm{q}) = k_u \cdot \bm{B}(\bm{q}) \bm{u}
\label{eq:ztcf_kill_switch}
\end{equation}

\item Running the model twice:
\begin{itemize}
\item With $k_u = 1$: the actual (controlled) dynamics.
\item With $k_u = 0$: the ZTCF (uncontrolled) dynamics.
\end{itemize}

\item Both simulations start from the same registered state. Differences remain conditional on model identity, retained protocols, and numerical error.
\end{enumerate}

The Python fixture is the only executable engine registered here. A Simulink
switch is an illustrative architecture, not a supported or parity-validated
implementation under this contract.
\end{example}

%============================================================================
\section{The Zero-Velocity Counterfactual (ZVCF)}
\label{sec:zvcf}

\begin{definition}{The Zero-Velocity Counterfactual}{zvcf}
The \textbf{instantaneous Zero-Velocity Counterfactual (ZVCF)} is the zero-velocity, zero-control acceleration at the current configuration and declared internal state. It is an evaluation, not a trajectory:
\begin{equation}
\bm{a}_{\text{ZVCF}}(\bm{q},\bm{z};\bm{p})
=-\bm{M}(\bm{q})^{-1}\bm{h}(\bm{q},\mathbf{0},\bm{z}_0;\bm{p}),
\qquad \bm{u}=\mathbf{0}
\label{eq:zvcf_eom}
\end{equation}

In a gravity-only rigid model this reduces to $-\bm{M}^{-1}\bm{g}$. A richer effective plant can retain configuration-dependent elasticity and other protocol-specified loads.
\end{definition}

\begin{laymansbox}{ZVCF vs. ZTCF: What's the Difference?}{inplain:zvcf_vs_ztcf}
The ZTCF sets the declared control channel to zero. A pointwise ZTCF sample retains measured velocity; a forward or branched ZTCF trajectory integrates the resulting autonomous dynamics.

The ZVCF evaluates one instantaneous acceleration with both velocity and declared control set to zero. It is not a frozen-and-released trajectory, because the next state would generally have nonzero velocity and cease to satisfy the defining intervention.

This is useful because it isolates a clean decomposition:
\begin{equation}
\bm{a}_{\text{drift}}(\bm{q},\dot{\bm{q}},\bm{z})
= \bm{a}_{\text{ZVCF}}(\bm{q},\bm{z})
+ \bm{a}_{\text{velocity}}(\bm{q},\dot{\bm{q}},\bm{z})
\label{eq:drift_decomposition}
\end{equation}

This difference isolates velocity-dependent drift only when both evaluations use identical parameters, internal state, load protocol, and contact mode.
\end{laymansbox}

%============================================================================
\section{The Drift-Control Ratio (DCR) and Late-Downswing Authority}
\label{sec:dcr_blowup}

\begin{definition}{Drift-Control Ratio (DCR)}{dcr}
At any time during the swing, the \textbf{Drift-Control Ratio (DCR)} compares modeled drift against bounded modeled control in the same acceleration or task-projected space under an explicit norm or weighting. Consistent with Chapter~\ref{ch:05_affine_structure}, use
\begin{equation}
\mathrm{DCR}_{\bm{W},\mathcal{U}}(\bm{x})
=
\frac{\lVert \bm{W}\bm{a}_{\mathrm{drift}}(\bm{x}) \rVert}
{\sup_{\bm{u}\in\mathcal{U}(\bm{x})} \lVert \bm{W}\bm{B}_a(\bm{x})\bm{u} \rVert + \varepsilon},
\label{eq:dcr_definition}
\end{equation}
where $\bm{W}$ states which coordinates or task directions are compared, $\mathcal{U}(\bm{x})$ is the admissible control set, and $\varepsilon$ is a declared regularizer. A single-joint torque ratio is a useful special case only after those modeling choices are fixed.

When DCR $\approx 1$, modeled drift and bounded control have comparable magnitude under the chosen weighting. When DCR $\gg 1$, modeled drift dominates that comparison; it does not imply that the golfer is passive or that muscle activation is absent.
\end{definition}

\begin{constraintbox}{Minimum Reporting Standard for DCR}{constraint:dcr_reporting}
Any numerical DCR claim should report enough information for another reader to reproduce or challenge it:
\begin{enumerate}
\item the state vector, coordinate convention, and whether $\bm{f}$ and $\bm{G}$ are expressed in generalized coordinates, task coordinates, or acceleration coordinates;
\item the weighting matrix $\bm{W}$, norm, and small denominator regularizer $\varepsilon$;
\item the admissible torque or activation bound $u_{\max}$ and whether it is joint-specific, time-varying, or muscle-model-derived;
\item the anthropometric, club, shaft, contact, and damping parameters used by the model; and
\item a sensitivity check showing whether the qualitative conclusion survives plausible perturbations of $\bm{W}$, $u_{\max}$, and the inertial parameters.
\end{enumerate}
Without these declarations, a value such as DCR $=10$ should be read only as an illustrative scale, not as an experimentally established phase boundary.
\end{constraintbox}

\begin{constraintbox}{DCR as a Magnitude Diagnostic}{constraint:dcr_meaning}
The Drift-Control Ratio asks: \textbf{How large is the modeled drift field
compared with bounded control acceleration under the stated projection, norm,
and input set?} It does not determine whether a subsequent state is reachable,
whether a ZTCF branch approaches the reference branch, or how impact conditions
respond to an intervention. Those are finite-horizon questions.
\end{constraintbox}

\begin{example}{The DCR Growth in a Typical Swing}{dcr_blowup_numbers}
Consider a two-segment arm (shoulder and elbow) during the downswing. Assume:
\begin{itemize}
\item Shoulder angle: $q_1 = 90°$ (arm horizontal).
\item Shoulder angular velocity: $\dot{q}_1 = 5$ rad/s (rotating forward).
\item Elbow angle: $q_2 = 90°$ (arm flexed, club parallel to ground).
\item Elbow angular velocity: $\dot{q}_2 = 10$ rad/s (extending rapidly).
\end{itemize}

At this configuration, using the club center-of-mass distance $L_{c,2} = 0.15$~m rather than the full link length and omitting the extra factor of 2, the Coriolis torque is approximately:
\begin{equation}
\tau_{\text{Coriolis}} \approx m_{\text{club}} L_1 L_{c,2} \dot{q}_1 \dot{q}_2 \approx 0.2 \times 0.4 \times 0.15 \times 5 \times 10 \approx 0.6 \text{ Nm}
\label{eq:dcr_example_coriolis}
\end{equation}

For a declared illustrative torque bound of $7.5$ Nm:
\begin{equation}
\text{DCR}_{\text{elbow}} \approx \frac{0.6}{7.5} \approx 0.08
\label{eq:dcr_example_ratio}
\end{equation}

Later in the downswing, when $\dot{q}_1 \approx 15$ rad/s and $\dot{q}_2 \approx 20$ rad/s (the club is whipping), Coriolis grows to:
\begin{equation}
\tau_{\text{Coriolis}} \approx 0.2 \times 0.4 \times 0.15 \times 15 \times 20 \approx 3.6 \text{ Nm}
\label{eq:dcr_example_late_coriolis}
\end{equation}

Now:
\begin{equation}
\text{DCR}_{\text{elbow}} \approx \frac{3.6}{7.5} \approx 0.48
\label{eq:dcr_example_late_ratio}
\end{equation}

This toy calculation shows the velocity scaling of one term under its stated
parameters. It does not establish a human torque bound or a late-downswing DCR.

\textbf{Interpretation:} In this simplified interpretation, the main point is the quadratic scaling of passive torque with velocity. The late downswing still demands careful timing, but this toy example should not be read as a literal hundreds-of-Nm Coriolis estimate.
\end{example}

\begin{laymansbox}{Why the DCR Can Rise Quickly}{inplain:dcr_blowup_reason}
The mathematical origin of high DCR is the velocity-dependent scaling of the drift terms.

\textbf{Passive torque (from Coriolis/centrifugal):} These terms are quadratic in velocity:
\begin{equation}
\tau_{\text{passive}} \propto \dot{\bm{q}}^2
\end{equation}

\textbf{Declared generalized torque bound:} The input set must be registered:
\begin{equation}
|\tau_{\text{input}}| \leq \tau_{\text{bound}}
\end{equation}

As velocity increases during the downswing, the modeled velocity-dependent drift can grow as $v^2$, while available corrective torque is bounded. In the single-joint simplification:
\begin{equation}
\text{DCR} = \frac{\tau_{\text{drift}}}{\tau_{\text{bound}}} \propto \dot{\bm{q}}^2
\end{equation}

can grow rapidly with velocity in that simplification. Whether a task correction
becomes unavailable still requires a finite-horizon reachability calculation
with declared controls, metric, uncertainty, and event outcome.
\end{laymansbox}

%============================================================================
\section{Double Pendulum ZTCF: A Worked Example}
\label{sec:double_pendulum_ztcf}

\begin{example}{ZTCF for a Two-Link Arm in Downswing Configuration}{double_pend_ztcf}
Consider a double pendulum (shoulder and elbow) with parameters:
\begin{itemize}
\item Shoulder: length $L_1 = 0.4$ m, mass $m_1 = 2$ kg (upper arm), $I_1 = 0.05$ kg$\cdot$m$^2$ (about the shoulder).
\item Elbow: length $L_2 = 0.3$ m, mass $m_2 = 0.2$ kg (club), $I_2 = 0.01$ kg$\cdot$m$^2$ (about the elbow).
\end{itemize}

These values are an illustrative counterfactual setup for this worked example. They are intentionally different from the Chapter~03 canonical two-link baseline, because this example uses compact values to keep the mechanism and numerical algebra transparent.

At a snapshot in the mid-downswing, the state is:
\begin{align}
q_1 &= 45° \quad \text{(shoulder angle, measured from horizontal)}\\
\dot{q}_1 &= 8 \text{ rad/s} \quad \text{(shoulder rotating forward)}\\
q_2 &= 70° \quad \text{(elbow extension angle)}\\
\dot{q}_2 &= 12 \text{ rad/s} \quad \text{(elbow extending)}
\end{align}

\textbf{Step 1: Compute the mass matrix and passive forces.}

The mass matrix for a planar double pendulum is:
\begin{equation}
\bm{M}(\bm{q}) = \begin{bmatrix}
I_1 + m_1 (L_1/2)^2 + I_2 + m_2 L_1^2 + 2 m_2 L_1 (L_2/2) \cos(q_2) & I_2 + m_2 L_1 (L_2/2) \cos(q_2) \\
I_2 + m_2 L_1 (L_2/2) \cos(q_2) & I_2 + m_2 (L_2/2)^2
\end{bmatrix}
\label{eq:double_pend_mass_matrix}
\end{equation}

Substituting values:
\begin{equation}
\bm{M} \approx \begin{bmatrix}
0.05 + 0.08 + 0.01 + 0.2 \times 0.16 + 2 \times 0.2 \times 0.4 \times 0.15 \times \cos(70°) & 0.01 + 0.2 \times 0.4 \times 0.15 \times \cos(70°) \\
0.01 + 0.2 \times 0.4 \times 0.15 \times \cos(70°) & 0.01 + 0.2 \times 0.0225
\end{bmatrix}
\label{eq:double_pend_mass_matrix_numerical}
\end{equation}

Evaluating at $\cos(70°) \approx 0.342$:
\begin{equation}
\bm{M} \approx \begin{bmatrix}
0.180 & 0.014 \\
0.014 & 0.015
\end{bmatrix}
\label{eq:double_pend_mass_numerical}
\end{equation}

\textbf{Step 2: Compute Coriolis torques.}

The Coriolis term is:
\begin{equation}
C_1 = -m_2 L_1 (L_2/2) \sin(q_2) \dot{q}_1 \dot{q}_2
\label{eq:coriolis_1}
\end{equation}

Substituting:
\begin{equation}
C_1 \approx -0.2 \times 0.4 \times 0.15 \times \sin(70°) \times 8 \times 12 \approx -0.2 \times 0.4 \times 0.15 \times 0.940 \times 96 \approx -1.09 \text{ Nm}
\label{eq:coriolis_1_numerical}
\end{equation}

$C_2 = 0$ (no Coriolis on the second segment in this simple model).

\textbf{Step 3: Compute gravity torques.}

\begin{align}
g_1 &= m_1 g (L_1/2) \cos(q_1) + m_2 g L_1 \cos(q_1) + m_2 g (L_2/2) \cos(q_1 + q_2)\\
g_2 &= m_2 g (L_2/2) \cos(q_1 + q_2)
\end{align}

At $q_1 = 45°$ and $q_1 + q_2 = 115°$:
\begin{align}
g_1 &\approx 2 \times 9.81 \times 0.2 \times 0.707 + 0.2 \times 9.81 \times 0.4 \times 0.707 + 0.2 \times 9.81 \times 0.15 \times (-0.906)\\
&\approx 2.78 - 0.27 \approx 2.51 \text{ Nm}\\
g_2 &\approx 0.2 \times 9.81 \times 0.15 \times (-0.906) \approx -0.27 \text{ Nm}
\end{align}

\textbf{Step 4: Solve for ZTCF acceleration.}

With $\bm{u} = \mathbf{0}$:
\begin{equation}
\begin{bmatrix}
\ddot{q}_1 \\
\ddot{q}_2
\end{bmatrix} = \bm{M}^{-1} \begin{bmatrix}
-1.09 - 2.51 \\
-0 - (-0.27)
\end{bmatrix} = \bm{M}^{-1} \begin{bmatrix}
-3.60 \\
0.27
\end{bmatrix}
\label{eq:ztcf_accel_vector}
\end{equation}

Inverting the mass matrix:
\begin{equation}
\bm{M}^{-1} \approx \begin{bmatrix}
6.0 & -5.6 \\
-5.6 & 71.9
\end{bmatrix}
\label{eq:mass_inverse}
\end{equation}

Thus:
\begin{align}
\ddot{q}_1^{\text{ZTCF}} &\approx 6.0 \times (-3.60) + (-5.6) \times 0.27 \approx -21.6 - 1.5 \approx -23.1 \text{ rad/s}^2\\
\ddot{q}_2^{\text{ZTCF}} &\approx (-5.6) \times (-3.60) + 71.9 \times 0.27 \approx 20.2 + 19.4 \approx 39.6 \text{ rad/s}^2
\end{align}

\textbf{Interpretation:}

The shoulder would decelerate at $23.1$ rad/s$^2$ (gravity and Coriolis). The elbow would accelerate at $39.6$ rad/s$^2$ (the club is extending under its own weight and Coriolis effects).

Now compare to the actual swing. If the actual $\ddot{q}_1 = 2$ rad/s$^2$ and $\ddot{q}_2 = 8$ rad/s$^2$, then:
\begin{align}
\tau_{\text{input},1} &\approx 0.180 \times (2 - (-23.1)) = 0.180 \times 25.1 \approx 4.5 \text{ Nm}\\
\tau_{\text{input},2} &\approx 0.015 \times (8 - 39.6) = 0.015 \times (-31.6) \approx -0.47 \text{ Nm}
\end{align}

\textbf{Insight:} Under this coordinate convention and toy-model inventory, the
applied generalized input is approximately $4.5$ Nm at the shoulder and
$-0.47$ Nm at the elbow. These net generalized quantities do not identify a
muscle, activation level, or biological strategy.
\end{example}

%============================================================================
\section{Why ZTCF Cannot Be Measured Directly}
\label{sec:ztcf_model_dependent}

\begin{principle}{ZTCF is Model-Dependent}{principle:ztcf_model_dependent}
A crucial limitation: the ZTCF cannot be observed directly from experimental data. It requires a \emph{model} of the passive dynamics.

In a real swing, you can measure:
\begin{itemize}
\item Joint angles (via markers, IMUs, or motion capture).
\item Joint velocities and accelerations (by differentiating).
\item Muscle activation (via electromyography, EMG).
\item Reaction forces at the grip (via force plates or instrumented clubs).
\end{itemize}

But you cannot directly observe the trajectory generated by zeroing a modeled
generalized-control channel. That trajectory is a model-conditioned
counterfactual, not an observation of removed muscle activity.

To compute the ZTCF, you must:
\begin{enumerate}
\item Estimate the mass matrix $\bm{M}(\bm{q})$ from body segment parameters (inertias, lengths, masses).
\item Estimate the Coriolis matrix $\bm{C}(\bm{q}, \dot{\bm{q}})$ from the kinematics.
\item Estimate the gravity vector $\bm{g}(\bm{q})$ from segment centers of mass and gravitational acceleration.
\item Solve the passive EOM with $\bm{u} = \mathbf{0}$.
\end{enumerate}

The usefulness of the ZTCF depends on the declared model, retained-load
inventory, parameter estimates, solver, initial state, and horizon. A fixture
can verify software replay; it cannot establish a unique ``true''
counterfactual for a person.
\end{principle}

\begin{laymansbox}{Why Model Errors Matter}{inplain:ztcf_model_error}
Suppose your estimate of the club's moment of inertia is off by 20\%. This
changes the retained inertial and velocity-dependent terms, so the computed
ZTCF trajectory can change materially.

This is why researchers spend so much effort on accurate biomechanical parameter estimation. The ZTCF analysis is only as good as the model underneath.

However—and this is important—the ZTCF can still be \emph{qualitatively} useful under parameter uncertainty when the sensitivity is reported. The defensible insight is not a universal DCR blow-up threshold; it is that velocity-dependent drift terms can rapidly reduce late correction authority in the specified model, so the conclusion should be checked against plausible parameter ranges.
\end{laymansbox}

%============================================================================
\section{Hypothesis: Late-Downswing Correction Under a Declared Model}
\label{sec:swing_ballistic_commitment}

\begin{principle}{Model-Conditioned Late-Downswing Hypothesis}{principle:ballistic_commitment}
For a specified plant, admissible-input set, metric, and horizon, a ZTCF
comparison can motivate the hypothesis that the late-downswing state is
strongly conditioned by an earlier state. ZTCF alone does not establish human
control authority or coaching guidance.

Some velocity-dependent model terms are quadratic in generalized velocity,
but that fact alone does not determine reachable corrections. Any claim about
late corrective authority requires a declared admissible-control set,
finite-horizon reachability calculation, constraints, and sensitivity study.

The governed comparison may report branch separation in a named output metric.
It may not infer stiffness regulation, feedforward timing, muscular strategy,
elite technique, or a reachable-set boundary without the additional models and
measurements required for those claims.
\end{principle}

\begin{driftcontrol}{Drift Magnitude Versus Reachability}{driftcontrol:drift_dominance_late}
\textbf{Drift:} A declared drift term can exceed the bounded instantaneous
input effect in a stated norm at a specified state.

\textbf{Control:} That magnitude comparison is not a finite-horizon reachable
set and does not identify muscle activation.

\textbf{Implication:} Any statement about feasible correction, human skill, or
timing must be tested separately with declared controls, constraints, horizon,
and measurement evidence.
\end{driftcontrol}

\begin{example}{Required Analysis for an Impact-Time Claim}{impact_control_margin}
Freeze the model/version and state estimate; define the face-angle output,
admissible generalized inputs, contact mode, horizon, constraints, and solver
tolerance; then compute a finite-horizon reachable set with uncertainty. Until
that artifact is available, a numerical margin-to-correction or
muscular-capacity claim is unavailable rather than inferred from DCR or ZTCF.
\end{example}

%============================================================================
\section{Summary and Preview}
\label{sec:ztcf_summary}

\begin{principle}{Key Takeaways: The Zero-Torque Counterfactual}{principle:ztcf_key_takeaways}

\begin{enumerate}
\item \textbf{What the ZTCF is:} A model intervention that sets the declared applied generalized-control channel to zero and integrates the retained effective plant. It does not measure muscle activation or a biological "passive fraction."

\item \textbf{The decomposition:} The declared generalized equation separates
drift from applied input effect. Neither term is automatically a passive
fraction or muscular quantity.

\item \textbf{The ZVCF refinement:} Further isolates gravity from velocity-dependent forces, revealing the role of initial momentum.

\item \textbf{DCR boundary:} DCR is a declared instantaneous magnitude
comparison; it does not establish finite-horizon reachability or a human
control strategy.

\item \textbf{Double-pendulum example:} Demonstrates model arithmetic for
retained drift and a net generalized input, not muscle-force attribution.

\item \textbf{Model-dependence:} ZTCF requires a biomechanical model; it cannot be measured directly. Qualitative conclusions should be reported with the DCR weighting, torque bound, parameter set, and sensitivity checks rather than asserted as model-independent facts.

\item \textbf{Unavailable inference:} Late correction, skill, effort, and
coaching claims require separate reachability, muscle, and measurement evidence.
\end{enumerate}
\end{principle}

\textbf{What comes next:} ZTCF records one declared model intervention. The next
chapter studies constraint forces, which are distinct from both the drift/input
bookkeeping and physiological attribution.

\begin{exercises}{Chapter Exercises: The Zero-Torque Counterfactual}{ex:chapter_6_exercises}

\begin{exercise}{Conceptual}
Define a two-link model at the top of a simulated backswing. List which
generalized-control channels are zeroed and which passive, contact, and
constraint terms are retained. Predict the model trajectory, and list the
physiological conclusions that the intervention cannot support.

(Hint: think about gravity and the Coriolis effect as the shoulder starts rotating forward.)
\end{exercise}

\begin{exercise}{Quantitative}
For a single-segment arm (shoulder only, no elbow), estimate the ZVCF acceleration:
\begin{itemize}
\item Arm length: $0.7$ m (including club).
\item Arm mass: $2$ kg (upper arm + club).
\item Moment of inertia about shoulder: $0.08$ kg$\cdot$m$^2$.
\item Current angle: $45°$ from vertical (arm pointing down-forward).
\end{itemize}

What is the gravity-induced angular acceleration? How does this compare to a typical muscular torque of $10$ Nm?
\end{exercise}

\begin{exercise}{Computational}
Set up the double pendulum ZTCF from Section \ref{sec:double_pendulum_ztcf} in a numerical solver (Python with scipy, Matlab, etc.).
\begin{enumerate}
\item Start from the configuration given ($q_1 = 45°$, $\dot{q}_1 = 8$ rad/s, $q_2 = 70°$, $\dot{q}_2 = 12$ rad/s).
\item Integrate the ZTCF equations forward for 0.1 seconds.
\item Plot the ZTCF trajectory: $q_1(t)$ and $q_2(t)$.
\item Qualitatively describe the motion: does the shoulder continue rotating? Does the elbow extend further?
\end{enumerate}
\end{exercise}

\begin{exercise}{Analysis}
The Drift-Control Ratio (DCR) compares modeled drift with bounded modeled control under a stated weighting and torque bound. Choose plausible early- and late-downswing values for $\bm{W}$ and $u_{\max}$, declare the model parameters that matter most, then estimate how the DCR changes under at least two sensitivity perturbations.

Explain why these DCR values alone do not determine reachable corrections or
the type, timing, or magnitude of muscular effort.
\end{exercise}

\begin{exercise}{Application}
For three simulated downswing states, freeze a model/version, norm,
admissible-input set, and parameters. Compute DCR and a short-horizon reachable
set at each state. Compare what each diagnostic supports, and identify the EMG,
musculoskeletal, and uncertainty evidence needed before making a physiological
interpretation.
\end{exercise}

\end{exercises}

\index{Zero-Torque Counterfactual}
\index{ZTCF}
\index{Zero-Velocity Counterfactual}
\index{ZVCF}
\index{Drift-Control Ratio}
\index{DCR}
\index{late correction window}
\index{Passive dynamics}
\index{Coriolis torque}
\index{Muscle control signal}
