---
title: "Tangent Hyperplanes II: Dynamics"
description: "Infinitesimal dynamics and moving tangent spaces."
bibliography: ../../references/affine-drift.bib
csl: ../../references/chicago-author-date.csl
---

## Linearization Along a Nonlinear Trajectory

The tangent-space construction becomes useful when applied to a flow. Consider a smooth nonlinear dynamical system:

$$
\dot{x} = f(x, u)
$$

where $x \in \mathbb{R}^n$ is the state, $u \in \mathbb{R}^m$ is the control input, and $f$ is a smooth vector field. This system is genuinely nonlinear. The future state depends on $x$ in a complex, nonlinear way.

At a specified reference trajectory, the first variation of the dynamics is linear. The matrices $A(t)$ and $B(t)$ are the Jacobians of the vector field evaluated along that trajectory. They encode the instantaneous first-order response of the system, not the full finite-amplitude nonlinear flow.

This is the standard variational equation used in nonlinear systems analysis and trajectory optimization [@khalil2002nonlinear; @slotine1991applied; @tedrake2023underactuated].

## Linearization Along a Reference Trajectory

Suppose we have a reference trajectory $(x̄(t), ū(t))$ satisfying:

$$
\dot{x̄}(t) = f(x̄(t), ū(t))
$$

Now consider a perturbed trajectory $x(t) = x̄(t) + \delta x(t)$ driven by a perturbed control $u(t) = ū(t) + \delta u(t)$. The perturbed system satisfies:

$$
\dot{x̄}(t) + \delta\dot{x}(t) = f(x̄(t) + \delta x(t), ū(t) + \delta u(t))
$$

Subtracting the reference trajectory equation and expanding the right-hand side to first order gives:

$$
f(x̄ + \delta x, ū + \delta u) = f(x̄, ū) + \frac{\partial f}{\partial x}\bigg|_{(x̄,ū)} \delta x + \frac{\partial f}{\partial u}\bigg|_{(x̄,ū)} \delta u + O(\|\delta x\|^2, \|\delta u\|^2)
$$

This gives us the perturbation equation:

$$
\delta\dot{x}(t) = A(t)\delta x(t) + B(t)\delta u(t)
$$

where the Jacobian matrices are defined as:

$$
A(t) = \frac{\partial f}{\partial x}\bigg|_{(x̄(t),ū(t))}
$$

$$
B(t) = \frac{\partial f}{\partial u}\bigg|_{(x̄(t),ū(t))}
$$

These are $n \times n$ and $n \times m$ matrices, respectively, evaluated along the reference trajectory at each time $t$. The omitted terms are second order in the perturbations.

## Moving Tangent Spaces

![Moving tangent spaces](./figures/moving_tangent.svg)

The tangent space at the reference state $x̄(t)$ is the space in which this first-order perturbation model evolves. As the reference state moves, the tangent space moves with it. At each instant $t$, the tangent hyperplane is:

$$
H_{x̄(t)} = x̄(t) + T_{x̄(t)} M
$$

The perturbation $\delta x(t)$ lives in this moving linear subspace. The matrices $A(t)$ and $B(t)$ describe how first-order perturbations grow, shrink, or couple within it.

## Superposition in the Variational Equation

The variational equation is linear. Therefore, within that equation, superposition holds exactly:

If $\delta x_1(t)$ is a solution to $\delta\dot{x} = A(t)\delta x + B(t)\delta u_1$ and $\delta x_2(t)$ is a solution to $\delta\dot{x} = A(t)\delta x + B(t)\delta u_2$, then $\delta x_1(t) + \delta x_2(t)$ is exactly a solution to $\delta\dot{x} = A(t)\delta x + B(t)(\delta u_1 + \delta u_2)$.

This statement is about the linearized perturbation model. The original nonlinear system does not generally superpose over finite perturbations, because the second-order and higher-order terms introduce interactions.

## The State Transition Matrix

Solutions to the linearized system can be expressed using the state transition matrix (also called the fundamental matrix) $\Phi(t, t_0)$, which satisfies:

$$
\dot{\Phi}(t, t_0) = A(t)\Phi(t, t_0), \quad \Phi(t_0, t_0) = I
$$

A solution to the linearized system with control input is:

$$
\delta x(t) = \Phi(t, t_0)\delta x(t_0) + \int_{t_0}^{t} \Phi(t, \tau) B(\tau) \delta u(\tau) \, d\tau
$$

This is the exact solution of the linear perturbation equation. Its accuracy as a prediction of the nonlinear system depends on the size of the neglected remainder terms.

## Limitations: Finite Perturbations and Nonlinearity

The linearized system is exact infinitesimally. For finite perturbations, the story is more complicated:

:::{.callout-warning}
**Higher-Order Terms Neglected:** The error from dropping $O(\|\delta x\|^2, \|\delta u\|^2)$ terms is bounded under smoothness assumptions but non-zero. Locally, the leading bound scales quadratically with perturbation size.

**Superposition Breaks Down:** Superposition holds exactly for the linearized system. But when you add the higher-order correction terms back in, two small perturbations no longer combine linearly — there are interaction terms.

**Validity Range:** The linearized system is trustworthy only when $\|\delta x(t)\|$ and $\|\delta u(t)\|$ remain small. In nonlinear systems, small initial perturbations can grow exponentially. Eventually, the linearization fails.
:::

## Next

The variational equation is the entry point for local control design. [Part 3](./part-3-control.qmd) explains how LQR, DDP, and iLQR use this first-order structure.
