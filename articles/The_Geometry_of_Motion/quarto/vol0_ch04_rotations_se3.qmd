---
title: "Rotations, Quaternions, and SO(3)"
---

# Rotations, Quaternions, and SO(3) {#sec-so3}

::: {.callout-note}

Rotations are everywhere in mechanics---every joint in a robot arm rotates, every
satellite tumbles, every golf club sweeps through an arc. But rotations are
mathematically strange in a way that catches everyone off guard the first time.

Here is the surprise: **the order of rotations matters**. If you rotate a book 90
degrees around the vertical axis, then 90 degrees around the horizontal axis, you get
a different result than if you do those same two rotations in the opposite order. Try
it right now with a book on your desk. This is not true for translations (walking 3
steps north then 2 steps east ends up the same as 2 east then 3 north).

This non-commutativity means rotations do not live in ordinary Euclidean space. They
live on a curved surface called $SO(3)$---the group of all rotations. This chapter
introduces that surface and its properties. The payoff is enormous: by respecting the
geometry of rotations, we avoid the singularities and artifacts that plague simpler
representations like Euler angles.
:::

## The Coordinate Frame


When we model movement or control robotic systems, we are not looking at the physical object itself; we are tracking a **Coordinate Frame** rigidly attached to the object, often denoted as the Body Frame ($B$). 

To know where the Body Frame $B$ is relative to the absolute stationary universe—the Spatial (or World) Frame ($S$)—we must define two mathematical properties:
1. The **translation** vector $\bm{p} \in \Reals^3$ pointing from the origin of $S$ to the origin of $B$. 
2. The **rotation** of the axes of $B$ relative to the axes of $S$.

Unlike the straightforward $(X, Y, Z)$ translation vector, rotation is profoundly mathematically problematic.


## Euler Angles and Gimbal Lock


The most intuitive way to describe the orientation of an aircraft, drone, or robotic arm is to split rotation into three successive angles around axes, usually denoted as Roll, Pitch, and Yaw ($X, Y, Z$).

This is known as an **Euler Angle** representation: $\bm{\theta} = [\theta_1, \theta_2, \theta_3]^T$. While highly intuitive for a human pilot, Euler Angles harbor a fatal mathematical flaw known as **Gimbal Lock**.

::: {.callout-important}
## Euler-Angle Convention Used in This Book

Where Euler angles appear in later chapters (e.g., for aerospace-style visualization), we use **intrinsic Tait–Bryan $Z\!-\!Y\!-\!X$** (yaw–pitch–roll). Rotations are applied to the body frame in the order yaw (about body $z$), pitch (about new body $y$), roll (about new body $x$). Other stacks use different conventions (e.g., ROS tf2 uses extrinsic $X\!-\!Y\!-\!Z$), so always translate explicitly when interoperating.
:::


::: {.callout-note}
## What Gimbal Lock Actually Is

  If an airplane pitches exactly $90^\circ$ up, its Roll axis aligns with the original Yaw axis. Two previously independent Euler-angle axes become physically identical. The rotation matrix $R\in SO(3)$ itself is **always invertible** — it is an element of $SO(3)$ and $R^{-1}=R^T$ exists everywhere. The rotation has not become singular; the *coordinate chart* (Euler angles) has.

  More precisely: the $3\times 3$ **Euler-angle Jacobian** $J(\phi,\theta,\psi)$ that maps Euler-rate vectors $(\dot\phi,\dot\theta,\dot\psi)$ to body angular velocity $\bm\omega$ is:
  $$J_{ZYX} = \begin{bmatrix} -\sin\theta & 0 & 1 \\ \cos\theta\sin\psi & \cos\psi & 0 \\ \cos\theta\cos\psi & -\sin\psi & 0 \end{bmatrix},\qquad \det J_{ZYX} = \cos\theta$$
  At $\theta=\pm\tfrac{\pi}{2}$, $\det J_{ZYX}=0$ and the inverse map (from $\bm\omega$ back to $\dot\phi,\dot\theta,\dot\psi$) blows up. This is a coordinate pathology — the chart fails — not a failure of the rotation itself or of the computer. Using rotation matrices or quaternions instead of Euler angles bypasses the singularity entirely.

:::



Since our control theory and geometric motion algorithms fundamentally rely on continuous trajectories twisting through all possible spatial angles (like chaotic golf club arcs or throwing motions), we must completely abandon Euler angles. We cannot afford mathematical singularities.


## The Special Orthogonal Group: SO(3)


To map 3D orientation without singularities in all continuous space, we define a rotation by looking at the purely physical unit vectors defining the Body Frame $B$: the $\hat{x}_b, \hat{y}_b, \hat{z}_b$ axes.

If we write these three body axes explicitly in the coordinates of the Space Frame $S$, we can stack them vertically into a $3 \times 3$ matrix:

$$
R = \begin{bmatrix} | & | & |  \hat{x}_b & \hat{y}_b & \hat{z}_b  | & | & | \end{bmatrix} \in \Reals^{3 \times 3}
$$


This is a **Rotation Matrix**. 

Because $\hat{x}_b, \hat{y}_b,$ and $\hat{z}_b$ are all length $1$ (unit magnitude) and perfectly orthogonal (all $90^\circ$ apart), the columns are an orthonormal basis. The strict mathematical definitions that guarantee this orthonormality are:
1. $R^T R = I$ (The transpose equals the inverse)
2. $\det(R) = 1$ (The determinant is 1 to preserve right-handed orientation—no reflecting through a mirror)


::: {.callout-note}

  The continuous, infinite geometric space of all possible valid $3 \times 3$ rotation matrices satisfying $R^T R = I$ and $\det(R) = 1$ is a mathematical **manifold** called the **Special Orthogonal Group in 3 Dimensions**, denoted as $\SO$. 

  If a Configuration Space $\configspace$ is simply a rigid body rotating around a point, we say $\configspace = \SO$.

:::



## Unit Quaternions and $S^3$


While $\SO$ completely prevents Gimbal Lock and provides pure, continuous rotation matrices, tracking 9 independent variables inside a $3 \times 3$ grid is wildly computationally inefficient for an engine updating a 15-DOF human model at 1,000 Hertz.

To solve this, William Rowan Hamilton invented **Quaternions** ($\mathcal{H}$) in 1843. Rather than tracking a full Cartesian body frame $R \in \SO$, any 3D rotation can be flawlessly described as a combination of precisely two things:
1. A single unit vector $\hat{\bm{\omega}}$ defining the solitary physical axis of rotation.
2. An angle $\theta$ denoting how far to spin around that axis.

A Unit Quaternion $q$ mathematically encodes these into 4 variables representing an exponential scalar and vector map:

$$
q = \begin{bmatrix} q_0  q_1  q_2  q_3 \end{bmatrix} = \begin{bmatrix} \cos(\theta/2)  \hat{\bm{\omega}}_x \sin(\theta/2)  \hat{\bm{\omega}}_y \sin(\theta/2)  \hat{\bm{\omega}}_z \sin(\theta/2) \end{bmatrix} \in \Reals^4
$$


By simply enforcing that the sum of the squares of these 4 variables equals 1 ($q_0^2 + q_1^2 + q_2^2 + q_3^2 = 1$), a Unit Quaternion provides a singularity-free, ultra-fast algebraic mapping of $\SO$.

::: {.callout-important}
## The Double Cover $S^3 \to SO(3)$

Unit quaternions live on the 3-sphere $S^3 \cong SU(2)$ — they are **not** elements of $\mathfrak{so}(3)$. There is a surjective group homomorphism (the **double cover**):
$$\pi : S^3 \to SO(3), \qquad q \mapsto R(q) = R(-q)$$
Both $q$ and $-q$ represent the *same* rotation in $SO(3)$. This means $\pi_1(SO(3)) = \mathbb{Z}/2$ — the fundamental group of $SO(3)$ is two-element (the "belt trick"). Unwinding consequence: a naive attitude-feedback law computed from quaternion error $\tilde q = q_d \otimes q^{-1}$ can steer through the long path ($-\tilde q$ instead of $\tilde q$), producing a $360°$ detour instead of the shortest-path correction. The fix is to negate $\tilde q$ whenever $\tilde q_0 < 0$. [@bhat2000topological; @chaturvedi2011rigid]
:::

::: {.callout-tip}
## The Lie Algebra $\mathfrak{so}(3)$

The tangent space to $SO(3)$ at the identity $I$ is the **Lie algebra** $\mathfrak{so}(3)$:
$$\mathfrak{so}(3) = T_I SO(3) = \{X \in \mathbb{R}^{3\times 3} : X^T + X = 0\}$$
Elements are $3\times 3$ skew-symmetric matrices. The **hat isomorphism** $(\cdot)^\wedge : \mathbb{R}^3 \to \mathfrak{so}(3)$ is:
$$\hat\omega = \begin{bmatrix}0 & -\omega_3 & \omega_2 \\ \omega_3 & 0 & -\omega_1 \\ -\omega_2 & \omega_1 & 0\end{bmatrix}, \qquad \hat\omega\,\bm{v} = \omega\times\bm{v}$$
The Lie bracket $[A,B] = AB - BA$ on $\mathfrak{so}(3)$ corresponds to the cross product: $[\hat\alpha, \hat\beta] = \widehat{\alpha\times\beta}$. So $(\mathfrak{so}(3), [\cdot,\cdot])$ and $(\mathbb{R}^3, \times)$ are isomorphic as Lie algebras. [@hall2015lie, Ch. 3]
:::

::: {.callout-important}
## Angular Velocity and the Rotation Derivative

Differentiating $R^T R = I$ gives $\dot R^T R + R^T \dot R = 0$, so $R^T \dot R$ is skew-symmetric. We define two angular velocity representations:
$$[\omega_b]^\wedge := R^T \dot R \in \mathfrak{so}(3) \quad\text{(body-frame angular velocity)}$$
$$[\omega_s]^\wedge := \dot R R^T \in \mathfrak{so}(3) \quad\text{(spatial-frame angular velocity)}$$
with the relation $[\omega_s]^\wedge = R\,[\omega_b]^\wedge\, R^T$, or equivalently $\omega_s = R\,\omega_b$. Every subsequent chapter that uses angular velocity assumes these identities. [@lynch2017modern, §3.2]
:::

Throughout *Tangent-Space Methods*, specifically when interacting with the codebase bridges connecting algorithms like `Pinocchio` or `UpstreamDrift` (which natively uses MuJoCo physics engines), all 3D rotational mechanics explicitly bypass Euler Angles and map directly into Quaternions and $\SO$ Rotation Matrices.

::: {.callout-important}
## Quaternion Conventions in This Book

Quaternion conventions differ between software stacks, and using the wrong one silently produces inverted rotations, broken kinematic chains, and wrong dynamics. The textbook locks a single convention and provides interop tables for downstream tools.

**Layout (scalar-first Hamilton):** we write $q = [q_0,\, q_1,\, q_2,\, q_3]^T = [w,\, \bm{v}]^T$ with the scalar part $w = \cos(\theta/2)$ first, matching Lynch & Park [@lynch2017modern] and MuJoCo.

**Multiplication (Hamilton product):**
$$ q_1 \otimes q_2 = \begin{bmatrix} w_1 w_2 - \bm{v}_1 \cdot \bm{v}_2  w_1 \bm{v}_2 + w_2 \bm{v}_1 + \bm{v}_1 \times \bm{v}_2 \end{bmatrix}. $$

**Active rotation, composition, and vector action.** A quaternion rotates vectors (active convention). For two successive rotations $R_1$ then $R_2$, the composed rotation is $R = R_2 R_1$, which in quaternion form is $q = q_2 \otimes q_1$ (left-to-right temporal order). A vector transforms as $\bm{v}' = q \otimes [0,\, \bm{v}] \otimes q^*$ where $q^* = [w,\, -\bm{v}]$ is the conjugate.

**Double cover:** $q$ and $-q$ represent the same rotation in $\SO$. When composing long chains or running SLERP, if $q_1 \cdot q_2 < 0$ negate $q_2$ first so interpolation stays on the short geodesic arc.

**Interop with common stacks (as of 2026):**

| Stack | Layout | Reorder from our $[w, x, y, z]$ |
| MuJoCo 3.x | scalar-first $[w, x, y, z]$ | none (`q_mj = q`) |
| Pinocchio 3.x / Eigen | scalar-last $[x, y, z, w]$ | `q_pin = [q[1], q[2], q[3], q[0]]` |
| ROS `tf2` / `geometry_msgs` | scalar-last $[x, y, z, w]$ | same as Pinocchio |
| SciPy `Rotation.from_quat` | scalar-last $[x, y, z, w]$ | same as Pinocchio |
| Unity / Unreal (games) | scalar-last, left-handed $[x, y, z, w]$ | reorder + handedness swap |

When in doubt: unit-test a known $90°$ rotation about $\hat{z}$ and check that a test vector lands where you expect.
:::


## Numerical Stability: A Practical Consideration


The choice between different rotation parameterizations has profound implications not just for singularities (which Gimbal Lock handles for Euler Angles), but for **numerical stability** during computation. This is critically important for practitioners implementing control algorithms and physics simulators. Rather than treating numerical stability as a post-hoc "patch," it deserves integration into the design of the rotation representation itself.

### Rotation Matrices: Stability Through Constraints

**Rotation Matrices** ($R \in \SO$) are numerically robust once computed correctly—multiplication is simple matrix multiplication, and the 9 entries intrinsically encode orthonormality constraints. However, maintaining this orthonormality over thousands of sequential matrix multiplications (as in iterative dynamics algorithms) requires periodic re-orthogonalization, adding computational cost. Two standard techniques exist: QR factorization (simple but biased toward the first columns) and polar decomposition / SVD (the orthogonal factor $U$ from $R = U P$ is the nearest orthogonal matrix in Frobenius norm, and is symmetric in its treatment of all columns). Polar decomposition is typically preferred for re-orthogonalizing drift-corrupted matrices; QR is cheaper and acceptable when each step introduces only tiny numerical error. The advantage of matrices is that once you enforce $R^T R = I$ (which is cheap to check), you directly access the body-frame axes $\hat{x}_b, \hat{y}_b, \hat{z}_b$ needed for many geometric calculations.

### Unit Quaternions: Efficiency Meets Stability

**Unit Quaternions** are typically the most numerically stable choice for continuous rotations and interpolation. Quantitatively, over a chain of $N$ compositions rotation-matrix orthogonality drift grows as $O(N \varepsilon)$ and requires periodic re-orthogonalization, while quaternion norm drift grows as $O(\varepsilon)$ per composition and is corrected by a single division (Featherstone [@featherstone2008rigid], §2.11; Shoemake 1985). They require only 4 parameters, compute quaternion multiplication very efficiently, and maintain the unit-norm constraint easily (via simple normalization). Critically, quaternion interpolation (SLERP) is geometrically smooth and well-behaved, making quaternions ideal for smoothly varying trajectories. However, the double-cover property (where $q$ and $-q$ represent the same rotation) must be handled carefully in algorithms to avoid jumping discontinuities—apply the short-arc rule from the conventions box before any chain composition or SLERP step. For real-time simulation at high frequency (e.g., 1000 Hz golf swing dynamics), quaternions are the preferred internal representation, with rotation matrices computed on-demand when the body axes are needed.

### Euler Angles: Where Singularities Propagate

**Euler Angles** suffer from Gimbal Lock (a singularity), but *near* the singularity—even when not exactly at $90°$—the Jacobian becomes poorly conditioned, meaning small changes in joint angles produce wildly inconsistent changes in orientation. This numerical ill-conditioning makes Euler Angles dangerous for iterative algorithms, not just at the singularity itself, but in a neighborhood around it. For these reasons, Euler Angles should be avoided entirely in computational algorithms—reserve them only for human-readable initialization and visualization.

### Practical Guidance

For real-time control and simulation, the recommendation depends on the application context:

- **Continuous dynamics and long kinematic chains:** Use unit quaternions internally for efficiency and numerical robustness. Convert to rotation matrices only when geometric insight (e.g., body-frame projections) is needed.
- **Frequent composition with geometric constraints:** Use rotation matrices with periodic re-orthogonalization. The cost of QR decomposition is negligible compared to the computational savings of avoiding quaternion-to-matrix conversion overhead.
- **High-speed ballistic motion (e.g., golf swing):** Prefer quaternions to handle rapid changes in angular velocity without singularity concerns.


To merge Translation and Rotation into a single unified control framework for physical bodies mapping the full world, we must upgrade from $3 \times 3$ Rotation Matrices to the magnificent $4 \times 4$ space of $\SE$.

## Exercises {#sec-so3-exercises}

**4.1 (Conceptual)** Verify non-commutativity of $\SO$: compute $R_x(\pi/2)R_z(\pi/2)$ and $R_z(\pi/2)R_x(\pi/2)$ and show they differ.

**4.2 (Computational)** Implement SLERP with short-arc disambiguation. Test with a $90^\circ$ rotation about $\hat{z}$; verify the midpoint ($t=0.5$) gives $45^\circ$.

**4.3 (Conceptual)** Bhat & Bernstein (2000) [@bhat2000topological] prove no smooth, time-invariant feedback globally asymptotically stabilizes attitude on $\SO$. Explain why the topology forces this.

**4.4 (Computational)** Compare QR vs polar decomposition re-orthogonalization over $10^4$ simulation steps. Which gives smaller Frobenius error?

