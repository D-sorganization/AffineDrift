---
title: "Chapter 2 - Variational Dynamics and Linearization"
bibliography: ../references.bib
---

::: {.callout-warning}
## Chapter Outline — In Progress

This chapter is currently a **structural outline**. The key concepts are identified and the geometric intuition is sketched, but full mathematical development, proofs, and worked examples are in progress. Treat the content below as a detailed table of contents rather than a complete chapter.
:::


## What Happens to a Small Perturbation as It Flows Along a Nominal Path?

A skier carves a perfect line down a mogul field. Now imagine a friend starts one meter to the left. Does the sideways distance grow, shrink, or stay constant? The answer depends on the slope's curvature. The curvature at each point is captured by a single matrix—the Jacobian of the vector field with respect to the state. This is the bridge from "what the full system does" to "how small deviations behave." This chapter formalizes that intuition.

## Introduction and Qualitative Purpose

This chapter formalizes infinitesimal dynamics and shows how continuous and discrete local models are constructed around nominal trajectories.

## In Laymans Terms

Even if the full system is nonlinear, tiny errors evolve according to a linear rule that we can compute and exploit.

## Geometric Intuition

Linearization gives the local transport rule between tangent spaces along a curve.

## Key Equations

$$
\dot{\delta x} = A(x,t)\delta x + B(x,t)\delta u,
$$

$$
\delta x_{k+1} = A_k\delta x_k + B_k\delta u_k.
$$

## Design Relevance

The local maps $A$ and $B$ quantify sensitivity and control authority, and they are the immediate inputs to LQR, DDP, and contraction checks.
