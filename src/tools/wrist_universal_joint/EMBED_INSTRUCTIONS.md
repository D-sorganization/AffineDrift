# Embedding Quick Reference

Use the implemented browser page, or an optional Streamlit instance configured
with the package layout described in the [embedding guide](EMBEDDING_GUIDE.md).
Keep the model qualification beside the iframe: this is a supported Cardan
shaft and synthetic projection demonstration, not a validated golf wrist model.
The browser page depends on Plotly; offline operation is not guaranteed.
