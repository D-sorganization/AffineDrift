# Minimal dependency set for the opt-in benchmark workflow.
# Keep this focused so benchmark setup stays fast on shared runners.
numpy==2.5.2
scipy==1.18.1
pytest==9.1.1
pytest-benchmark==5.3.0
pydantic==2.13.4
PyYAML==6.0.3
pytest-asyncio==1.4.0
