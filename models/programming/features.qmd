---
title: "Feature Parity Matrix"
description: "Parity state of each UpstreamDrift feature across its desktop (PyQt), API, and web surfaces"
---

::: {.callout-note title="Provider Pin"}
Generated from the provider-published UpstreamDrift artifact `upstreamdrift-companion-27b6eeadbbd970bebb8926a3c83aeaea1f6ee4da` (manifest SHA-256 `a1c9a799ecceb4e81f28c50755fb50ad28f390ed8db335b64ecb89028f910b06`; attestation: verified: gh attestation verify (actions/attest-build-provenance); installed 2026-09-03). The provider marks this catalog **draft**: Engine capability claims require qualification evidence. Documentation and screenshot inventories require their governed provider slices. See the [freshness dashboard](freshness.html).
:::

## Authority Boundary

> [!NOTE]
> **This establishes** which execution surfaces (desktop PyQt, API, web) each feature is
> registered on and the provider's parity state for it.
> **This does not establish** scientific validation or equivalence between solver implementations.

## Feature Surface Parity (41 Records)

Parity state is the provider's own classification: `parity` (all registered surfaces agree),
`gap` (a surface is missing or partial; the tracking issue is linked), or `exempt` (parity is
not expected for this feature). Scientific qualification is reported exactly as the provider
declares it; every record here is `unqualified`, meaning catalog inclusion is a software fact,
not validation.

| Feature ID | Title | Surfaces | Parity State | Parity Issue | Scientific Qualification |
| :--- | :--- | :--- | :---: | :---: | :--- |
| `analysis.analysis_tools_api` | Analysis Tools REST endpoints (swing metrics, biomechanics) | `api`, `web` | `gap` | [#7448](https://github.com/D-sorganization/UpstreamDrift/issues/7448) | `unqualified` — Feature parity record only |
| `analysis.counterfactuals` | ZTCF/ZVCF + induced-acceleration counterfactuals | `pyqt` | `gap` | [#7450](https://github.com/D-sorganization/UpstreamDrift/issues/7450) | `unqualified` — Feature parity record only |
| `analysis.cross_engine_robustness` | Cross-engine robustness dashboard (perturbation/CV) | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `analysis.static_plots` | Static analysis plots (20+ plot types) | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `biomech.exercise_injury_dashboards` | Exercise + injury-risk biomechanics dashboards | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `canonical_core.workspaces` | Canonical-core estimation/comparison workspaces | `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `chat.live_context` | Live app/engine context in chat | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `chat.transport` | AI chat transport (message send/stream) | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `diagnostics.integrations_health` | Diagnostics + integrations-health panel | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `docs.document_library` | Document library / project map viewer | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `engines.dashboards` | Per-engine interactive dashboards (Drake/MuJoCo/Pinocchio) | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `engines.load_and_simulate` | Engine load/probe + basic simulation loop | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `export.recordings_downloads` | Export/recording parity (HDF5/MAT/C3D/CSV/video, persisted recordings) | `api`, `pyqt` | `gap` | [#7451](https://github.com/D-sorganization/UpstreamDrift/issues/7451) | `unqualified` — Feature parity record only |
| `launcher.docker_management` | Docker engine management dialog | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `launcher.embedded_tabs_docks` | Embedded tool host (tabs + docks) | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `launcher.mcp_config` | MCP server configuration writer/preferences | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `launcher.tile_grid` | Launcher tile grid from shared manifest | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `launcher.tile_web_reachability` | Manifest tile web-reachability contract (route / native-window / unavailable) | `pyqt`, `web` | `gap` | [#7461](https://github.com/D-sorganization/UpstreamDrift/issues/7461) | `unqualified` — Feature parity record only |
| `mocap.breadth` | Motion-capture breadth (C3D upload/playback, OpenPose source) | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `onboarding.about_version` | About/version info + onboarding | `pyqt` | `gap` | [#7459](https://github.com/D-sorganization/UpstreamDrift/issues/7459) | `unqualified` — Feature parity record only |
| `optimization.swing_optimizer` | Swing Optimizer (trajectory optimization GUI) | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `platform.aip_protocol` | AI Protocol (AIP) structured method dispatch | `api` | `parity` | — | `unqualified` — Feature parity record only |
| `settings.desktop_only_tabs` | Desktop-only settings tabs (MCP Servers, Processes, Startup/Docker, Layout, Performance) | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `settings.preferences` | Settings/preferences surface + persistence | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `sidekick.terminal_repl_jupyter_skills` | Sidekick OS terminal / REPL / Jupyter / skills | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `simulation.controls_wiring` | Web SimulationControls wiring (camera presets, recording toggle, trajectory export, force overlays, actuator controls) | `pyqt`, `web` | `gap` | [#7452](https://github.com/D-sorganization/UpstreamDrift/issues/7452) | `unqualified` — Feature parity record only |
| `simulation.golf_suite_batch` | Golf Simulation Suite (parameter sweeps, batch runs) | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `simulation.realtime_ws_stream` | Live simulation data over WebSocket pub-sub | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `simulation.shot_tracer` | Shot Tracer / ball-flight visualization | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `simulation.swing_objective_lab` | Swing Objective Lab — mechanism-vs-outcome downswing comparison | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `tools.bunkershot3d_workbench` | BunkerShot3D designer workbench (W2 sole parameters, W3 sand condition, F0 dynamic-RFT shot, W7 metrics, playability window, bounce utilisation, animated sole load field, 3-D shot animation through the ADR-0027 viewport, linked scalar traces with a validity band, F1 sand-field cross-sections, A/B comparison, validity verdict) | `pyqt` | `gap` | [#8607](https://github.com/D-sorganization/UpstreamDrift/issues/8607) | `unqualified` — Feature parity record only |
| `tools.character_builder` | Character Builder (humanoid URDF generation) | `api`, `pyqt`, `web` | `gap` | [#7448](https://github.com/D-sorganization/UpstreamDrift/issues/7448) | `unqualified` — Feature parity record only |
| `tools.data_explorer` | Data Explorer (import/filter/visualize datasets) | `api`, `web` | `gap` | [#7448](https://github.com/D-sorganization/UpstreamDrift/issues/7448) | `unqualified` — Feature parity record only |
| `tools.dataset_generator` | Swing dataset generation and import | `api`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `tools.launch_monitor_analytics` | Launch-monitor import, interdependency analysis, monitor comparison, dispersion, and longitudinal trends | `api`, `pyqt` | `gap` | [#8364](https://github.com/D-sorganization/UpstreamDrift/issues/8364) | `unqualified` — Feature parity record only |
| `tools.matlab_suite` | MATLAB/Simscape model suite | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `tools.model_explorer` | Model Explorer (browse/select/build URDF-MJCF) | `api`, `pyqt`, `web` | `gap` | [#7448](https://github.com/D-sorganization/UpstreamDrift/issues/7448) | `unqualified` — Feature parity record only |
| `tools.pose_editing` | Pose Studio interactive pose editing | `pyqt` | `exempt` | — | `unqualified` — Feature parity record only |
| `tools.putting_green` | Putting green simulation | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `tools.rate_of_closure` | Rate of Closure Impact Explorer (swing-impact-flight-putting simulation suite) | `api`, `pyqt`, `web` | `parity` | — | `unqualified` — Feature parity record only |
| `tools.terrain_engine` | Terrain and topography configuration | `api`, `web` | `parity` | — | `unqualified` — Feature parity record only |
