---
id: GH1661
github_issue: 1661
repo: D-sorganization/AffineDrift
title: "12 type: ignore comments across src/ - several suppressible"
created_at: 2026-03-22T21:11:28Z
---

# GH1661: 12 type: ignore comments across src/ - several suppressible

**Source:** D-sorganization/AffineDrift#1661

## Description

## Assessment: J. API Design / I. Code Style

**Finding:** 12 `# type: ignore` comments across `src/`:
- 2 in `core/contracts/` for numpy optional import (acceptable)
- 4 in `wrist_universal_joint/` for Streamlit cache decorators `@st.cache_resource` (investigate proper typing)
- 4 in `wrist_universal_joint/streamlit_app.py` for ndarray type args and return types
- 1 in `affine_control/ddp.py` for np.clip return type
- 1 in `core/contracts/definitions.py` for noqa

**Impact:** Type ignore comments mask real type errors and reduce the value of strict mypy.

**Fix:**
1. For `@st.cache_resource`, consider adding a typed wrapper function
2. For ndarray type args, use `npt.NDArray[np.floating[Any]]` from `numpy.typing`
3. For `np.clip` return, use an explicit cast

**Files:** See list above

## Acceptance Criteria

1. The issue described in #1661 is resolved
2. All new code has tests (TDD)
3. `ruff check` passes on modified files
4. No new `print()` calls in `src/`
5. PR created targeting `staging` branch with clear description
