---
id: GH1665
type: qa-report
verdict: PASS
date: 2026-03-22
---

# QA Report — GH1665

## Verdict: PASS

## Acceptance Criteria Review

| AC | Description | Result |
|----|-------------|--------|
| AC1 | Issue resolved: vacuous checks replaced with meaningful isinstance checks | PASS |
| AC2 | All new code has tests (2 new tests added) | PASS |
| AC3 | `ruff check` passes on modified files | PASS |
| AC4 | No new `print()` calls in `src/` | PASS |
| AC5 | PR created targeting staging branch | PASS (pending) |

## Test Results

- Module tests: 46/46 passed
- Full suite: 1518 passed, 1 skipped
- Coverage: 66.1% (threshold: 50%)
- Black format: clean
- Ruff check: clean

## Change Summary

**File:** `src/affine_control/swing_optimizer.py` lines 180-187

**Before:**
```python
require(len(self.optimal_controls) >= 0, "optimal_controls must be a list")
require(len(self.optimal_trajectory) >= 0, "optimal_trajectory must be a list")
```

**After:**
```python
require(isinstance(self.optimal_controls, list), "optimal_controls must be a list")
require(isinstance(self.optimal_trajectory, list), "optimal_trajectory must be a list")
```

The error messages stated "must be a list" but the prior checks only verified `len()` was callable (always >= 0). The fix enforces actual `list` type.

## No Friction
