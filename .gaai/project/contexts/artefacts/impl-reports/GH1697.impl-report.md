---
id: GH1697
type: impl-report
status: COMPLETE
created_at: 2026-04-20
---

# GH1697 Implementation Report

## Summary

- Extracted duplicated limit-resolution logic into `resolve_line_limit()` in `budget_check_utils.py`
- Updated both budget check scripts to use the shared function
- Added 5 tests covering all branches of the new function

## Changes Delivered

| File | Change |
|------|--------|
| `src/tools/utils/budget_check_utils.py` | Added `resolve_line_limit()` — extracts duplicated inline limit-resolution logic |
| `scripts/check_module_size_budget.py` | Replaced 4-line inline if/else with `resolve_line_limit()` call |
| `scripts/check_changed_file_size_budget.py` | Replaced 5-line inline if/else with `resolve_line_limit()` + added import |
| `tests/test_budget_check_utils.py` | Added `TestResolveLineLimit` class with 5 tests |

## Acceptance Criteria Verification

| AC | Result | Evidence |
|----|--------|---------|
| AC1: Issue resolved | PASS | DRY violation in budget check scripts eliminated; limit logic consolidated to single source |
| AC2: New code has tests | PASS | 5 tests in `TestResolveLineLimit` class |
| AC3: `ruff check` passes | PASS | Zero violations on all 4 modified files |
| AC4: No new `print()` in `src/` | PASS | `resolve_line_limit()` uses no print statements |
| AC5: PR targeting staging | PENDING | PR to be created in step 8 |

## Friction Log

None. Clean implementation.
