---
id: GH1664
type: impl-report
status: complete
created_at: 2026-03-22
---

# GH1664 Implementation Report

## Summary

- Replaced all `sys.stdout.write()` calls in `print_results()` with `logger.info()`.
- Removed the now-unused `import sys` from `src/tools/rl_funnel_benchmark.py`.
- Updated 4 test methods across 2 test files to use `caplog` instead of `capsys`.

## Files Changed

| File | Change |
|------|--------|
| `src/tools/rl_funnel_benchmark.py` | Removed `sys` import; converted `print_results()` to `logger.info()` |
| `tests/tools/test_rl_funnel_benchmark.py` | Updated 3 `TestPrintResults` tests to use `caplog` |
| `tests/tools/test_rl_funnel_benchmark_ext.py` | Updated 1 `TestPrintResultsEdgeCases` test to use `caplog` |

## AC Verification

1. Issue resolved — `sys.stdout.write` replaced with `logger.info` in `print_results()`.
2. Tests updated (TDD maintained — all assertions preserved via `caplog`).
3. `ruff check` passes — zero violations.
4. No new `print()` calls in `src/`.
5. PR to be created targeting `staging`.
