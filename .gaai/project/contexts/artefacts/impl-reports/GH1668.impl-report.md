---
id: GH1668
type: impl-report
status: complete
created_at: 2026-03-22
---

# GH1668 Implementation Report

## Summary
- Fixed dead state bug: `MPC_WARN` is now reachable via a proper three-state mode machine
- Updated 4 test files to reflect correct three-state escalation and recovery paths
- All 1519 tests pass; coverage 66.15%; ruff and black clean

## Changes

| File | Purpose |
|------|---------|
| `src/affine_control/residuals.py` | Rewrote `update()` transition logic; added MPC_WARN as intermediate state with counter resets on transition |
| `tests/test_affine_control/test_residuals.py` | Updated `test_monitor_switching` to step through all three states; added 3 new targeted tests |
| `tests/test_dbc_application_contracts.py` | Fixed parametrized test `test_monitor_mode_transition_with_high_residuals` to expect MPC_WARN at n_hyst steps, MPC_FULL at 2*n_hyst |
| `tests/test_tangent_integration.py` | Fixed `test_monitor_transitions_on_large_residuals` and `test_monitor_returns_to_lqr_on_small_residuals` to use correct step counts |

## Root Cause
The original transition block only handled `LQR -> MPC_FULL` and `MPC_FULL -> LQR`, bypassing `MPC_WARN` entirely. The `eps_warning` threshold only reset `low_count` but never triggered a mode change.

## Acceptance Criteria
- AC1: Issue resolved — MPC_WARN is now reachable ✓
- AC2: TDD tests added (3 new tests in test_residuals.py) ✓
- AC3: `ruff check` passes with zero violations ✓
- AC4: No new `print()` calls in `src/` ✓
- AC5: PR to be created targeting `staging` branch ✓
