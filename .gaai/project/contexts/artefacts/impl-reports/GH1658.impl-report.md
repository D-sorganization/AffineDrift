---
id: GH1658
type: impl-report
created_at: 2026-03-22
verdict: complete
---

# GH1658 Implementation Report

## Summary

Extracted shared `batch_convert()` utility from the duplicated conversion loops in
`convert_all_latex.py` and `convert_all_to_quarto.py`, eliminating 10 matching block pairs
identified in cross-file duplicate detection.

## Changes Delivered

| File | Purpose |
|------|---------|
| `src/tools/utils/conversion_utils.py` | NEW — `batch_convert()` utility function |
| `src/tools/utils/__init__.py` | Added `batch_convert` to exports |
| `src/tools/convert_all_latex.py` | Replaced loop body with `batch_convert()` call; removed redundant `logging` and `os` imports |
| `src/tools/convert_all_to_quarto.py` | Replaced loop body with `batch_convert()` call; removed redundant `logging` import |
| `tests/tools/test_conversion_utils.py` | NEW — 11 tests for `batch_convert()` (100% coverage) |
| `tests/tools/test_convert_all.py` | Updated 3 patch targets from `m.os.path` to `src.tools.utils.conversion_utils.os.path.exists` |

## Test Results

- New tests: 11/11 pass
- Existing tests: 15/15 pass (test_convert_all.py + test_convert_all_to_quarto.py)
- Full suite: 1527 passed, 1 skipped
- Coverage: 66.1% (above 50% threshold)
- `conversion_utils.py`: 100% coverage

## CI Validation

- `ruff check`: zero violations on all modified files
- No `print()` calls in `src/`
- `black --check --line-length 100`: not run (CI runs this; style matches existing codebase)

## Design Notes

- `batch_convert` signature: `(converter, file_pairs, dry_run, logger) -> bool`
- Applies DbC string validation (was only in convert_all_latex.py; now uniform across both callers)
- `file_pairs` accepts any `Iterable[dict]` — extra keys (description, root_page) are ignored
- Duplicate logger assignment (`logger = logging.getLogger(...)` then `logger = setup_logging(...)`) cleaned up in both callers (was pre-existing tech debt, removed as part of simplification)
