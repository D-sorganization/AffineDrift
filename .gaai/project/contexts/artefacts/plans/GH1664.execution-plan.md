---
id: GH1664
type: execution-plan
tier: 1
created_at: 2026-03-22
---

# GH1664 Execution Plan

## Approach

Tier 1 MicroDelivery. Single-function refactor in one file.

## Steps

1. Remove `import sys` from `src/tools/rl_funnel_benchmark.py` (only used in `print_results`).
2. Convert all `sys.stdout.write(...)` calls in `print_results()` to `logger.info(...)`.
3. Update `TestPrintResults` in `test_rl_funnel_benchmark.py` to use `caplog` instead of `capsys`.
4. Update `TestPrintResultsEdgeCases` in `test_rl_funnel_benchmark_ext.py` to use `caplog`.
5. Run `ruff check`, `black --check`, `pytest --cov --cov-fail-under=50`.
