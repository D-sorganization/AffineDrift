# Technical Review: Mechanics, Control, and Impact

Date: 2026-09-05. Parent epic: [#4009](https://github.com/D-sorganization/AffineDrift/issues/4009).

## Argument Being Investigated

The golfer prepares and continually changes a coupled mechanical state. Geometry determines force transmission; inertia couples accelerations; activation and tissue mechanics determine forces and impedance; earlier work becomes kinetic and elastic energy; feedback and prediction manage deviations; impact selects which aspects of the delivered state matter. The useful research question is how those contributions interact under explicit constraints and measurable objectives.

A same-state affine partition is an algebraic tool. It does not independently identify muscle effort, intention, metabolic efficiency, or how the nervous system controls the swing. A forward counterfactual is a model intervention whose state evolves; it must declare its input, boundary conditions, starting state, and comparison event. These distinctions make the argument testable rather than weakening it.

## Corrections Implemented

| Finding | Correction | Evidence |
| --- | --- | --- |
| #4148: Inverse Dynamics | Standard inverse dynamics returns applied generalized load. Feeding it matched zero-input acceleration returns zero; subtracting a supposed drift output from it double-counts bias terms. Retain rigid-flexible off-diagonal inertia. | Coupled-mass and pendulum counterexamples; shared inverse-dynamics include; Newton–Euler identities. |
| #4148: Constraint Forces | Ideal reactions lie in the range of the constraint Jacobian transpose, not generally in the actuation nullspace. Reactions depend on applied input. | Fully actuated constrained example; workless reaction identity. |
| #4148: Wrenches and Power | Translate moments using the same force and reference point; power uses the matching point velocity. Net hand wrench does not identify bilateral hand forces or muscle forces. | Wrench translation and power-invariance tests. |
| #4148: Impedance and Flexibility | Stiffness does not add to physical inertia. Configuration, modal coupling, preload, activation history, and contact assumptions affect different parts of the model. | Scalar spring response; constrained inverse mass; corrected state ordering and spatial/modal Jacobians. |
| #4149: Optimal Control | Repair Hamiltonian/Lagrangian signs, matrix dimensions, costate gradients, iLQR/DDP distinction, Riccati cross terms, convergence assumptions, and finite-horizon controllability claims. | Analytic CARE residual, closed-loop eigenvalues, cross-cost identity, uncontrollable finite-horizon example. |
| #4149: Passive Stabilization | Separate restoring stability from attraction, finite stiffness from kinematic rank, intrinsic response from metabolically passive tissue, and UCM covariance from causal identification. | Compliance example, stiffness derivative with moment arms, variable-spring energy balance, normalized UCM projection. |
| #4150: Impact | Derive spin loft by a vector dot product; compute spin direction from contact geometry; distinguish speed from its projection, relative COR from energy fraction, and screw-axis distance from total-speed/angular-speed ratio. | Nonzero attack/loft counterexample; normal-impulse vector calculation; momentum and energy identities. |
| #4150: Measurement Interpretation | Observational regression and mediation support associations, not established causal efficiency mechanisms. Null associations do not prove equivalence or identify timing adaptation. | Primary study design and explicit dimensional distinction between impulse and work. |
| #4151: Underactuation and Release | Define absolute and relative coordinates through virtual work; distinguish full column rank from full row rank; include input bounds in feasibility; distinguish forward reachability from an orbit; replace unsupported phase-stability assertions with a variational calculation. | Power-invariant coordinate transformation, positive-definite mass matrices with opposite coupling signs, bounded-input counterexample, and a drifting system that cannot move backward. |
| #4154: Tangent-Space Reference | Use the evolved baseline in a four-trajectory interaction; derive mixed second variations with state-transition transport; separate intrinsic curvature, nonlinear interaction, and integration error; correct repeated DDP/iLQR, energy-bookkeeping, and constrained-control claims. | Linear-system false-interaction counterexample, nonlinear input on a flat line, exponentially amplified Hessian source, and a finite-difference endpoint-map bound. |
| #4156: Constrained Geometry | Rebuild the parallel-mechanisms chapter around vector/covector duality, correct Coriolis loads, affine acceleration constraints, effective input rank, common-frame grip mechanics, floating-base contact counts, force-plate conventions, power, observability limits, and momentum-dependent geometric phase. | Unequal-mass orthogonality, centripetal acceleration, inertial power identity, weighted allocation, contact-power and COP counterexamples; paired source regression checks. |
| #4157: Superposition Reference | Reconcile the abstract, accessible summaries, and conclusions with the incremental theorem; correct rotating-frame transport, spatial inertia, wrench duality, 2R inertia, activation/excitation scope, and causal/attractor overclaims. | Body-velocity transport, spatial kinetic-energy identity, two uniform-rod inertia calculation, affine-baseline counterexample, and an article wording guard. |
| #4158: Contraction Reference | Correct the metric inequality and coordinate rules; distinguish Riccati cost decrease from contraction; replace invalid penalty certificates and unsupported benchmarks; repair DDP, muscle-force, covariance, task-space, and hybrid-event arguments. | Matrix-order counterexample, unstable product of stable updates, finite-horizon metric collapse, exact generalized eigenvalue rate, singular task pullback, critical-damping and noise-floor checks. |

The longest foundational monograph and its multipart/manifesto editions share five reviewed explanatory includes. The inverse-dynamics and intentional-constraint-collapse articles were revised substantially. Canonical LaTeX and Quarto editions of the optimal-control, passive-stabilization, and impact chapters were revised together; the Bosch integration copy follows its canonical chapter. The impact reference, tangent LQR chapter, proximal-distal article, and force-measurement article received targeted corrections.

Unsupported numerical result tables and physiological diagrams were removed where no reproducible result package or identified study supported them. They were replaced with checkable derivations, worked examples, and specified experiments. This is not evidence that the underlying research hypothesis is false.

## Primary Sources Checked

- [Modern Robotics: Constrained Dynamics](https://modernrobotics.northwestern.edu/nu-gm-book-resource/8-7-constrained-dynamics/) — constraint reactions and projection.
- [Featherstone's Spatial-Vector Reference](https://royfeatherstone.org/spatial/v2/) — force/motion cross-product operators, spatial inertia, and articulated dynamics.
- [MIT Underactuated Robotics: LQR](https://underactuated.mit.edu/lqr.html) and [Trajectory Optimization](https://underactuated.mit.edu/trajopt.html) — value propagation and model/algorithm scope.
- [OpenSim Activation Dynamics](https://opensimconfluence.atlassian.net/wiki/spaces/OpenSim/pages/53090590) — activation as a dynamic state distinct from excitation.
- [Rack and Westbury, 1974](https://pubmed.ncbi.nlm.nih.gov/4424163/) — short-range response of activated muscle; not evidence of a universal golf stiffness schedule.
- [Scholz and Schöner, 1999](https://pubmed.ncbi.nlm.nih.gov/10382616/) — task-equivalent variability and the uncontrolled manifold.
- [USGA Collision Explanation](https://www.usga.org/content/usga/home-page/articles/2025/05/what-happens-collision-between-club-ball.html) — ordinary relative-speed COR definition.
- [Rachnavy et al., 2026](https://www.frontiersin.org/journals/sports-and-active-living/articles/10.3389/fspor.2026.1790645/full) — observational design underlying the mediation discussion.

## Coverage and Remaining Work

`corpus-review-index.csv` inventories tracked article/book/content QMD and LaTeX sources by length and records the scope of this pass. An indexed or pattern-scanned source has not completed a line-by-line technical audit. Likewise, repairing a claim does not clear every other claim on its route.

The existing full-corpus tracker #4021 and route batches #4054–#4061 remain open. Continue with the remaining chapters of the long motion-control manuscript and Geometry of Motion volumes, tangent/control monographs, the complete impact/aerodynamic evidence tables, remaining Physics of Golf chapters, and then all shorter articles and critiques. Check each source's references, equations, worked examples, figures, accessible summaries, and paired editions. Do not mark deferred routes reviewed merely because their sources were enumerated or searched.

The immutable `articles/proximal_distal_energy_transfer/` publication remains unchanged. Its upstream source and revision-gated refresh process govern corrections to that monograph.

The parallel-mechanisms chapter received a further complete source pass under #4156. Its earlier correct reaction elimination was contradicted by incorrect projections and stronger observability/control claims elsewhere. The revised chapter retains the progression through geometry, dynamics, control, inverse problems, contact, geometric phase, and numerical methods, with complete worked examples and a golf investigation section. It removes unsupported equivalences between geometric feasibility, neural strategy, and coaching benefit. Other routes in #4055 still need their own review.

## Validation

The final first-wave Python run passed 3,715 tests with 92.62% coverage, with 26 skips. The Python 3.12 content lane passed 128 tests with four skips. Ruff and Black passed repository-wide; the CI mypy command passed 74 source files. Four additional underactuation tests passed after the new wording guard failed on the old editions. New tests independently check equations as well as preventing the identified wording regressions.

The revised Physics of Golf and Geometry of Motion Volume I PDFs compiled. All revised optimal-control, passive-stabilization, and impact chapter pages were visually inspected. The changed QMD editions rendered. The foundation article rendered 651 math expressions and the impact reference 270, with no MathJax error nodes. Local site preview blocked a legacy Quarto polyfill under the existing CSP; full site deployment and live-route checks remain separate.

The revised Geometry Volume II and complete motion-control PDFs also compiled, with no undefined references in their final logs; their underactuation chapter pages were visually inspected. The complete motion manuscript was missing 25 chapter macro definitions, which were restored from the standalone chapter preambles. Its legacy `compile_book.py` still contains machine-specific paths and is not the build command used here; reproducible PDF builds used `latexmk` on the tracked complete LaTeX source.

After the tangent corrections and CI-format fixes, the Python 3.12 full suite passed 3,712 tests, with 29 skips and 92.32% coverage. The separate 3.13 run above had different optional dependencies; its coverage is not directly interchangeable. CI found three malformed LaTeX commands and a disallowed textbook callout format; the corrected sources pass `scan_quarto_syntax.py` and the textbook-quality tests. The inspected tangent HTML rendered without MathJax error nodes. Its paired PDF is regenerated from the QMD with Quarto/XeLaTeX, with equation labels, contents, lists, and revised derivation pages checked visually.

The subsequent constrained-geometry tests passed six checks after the paired-source regression failed on the old editions. Tree parity, LaTeX structure, and Quarto citation integrity passed. A later static CI step also found two LaTeX-style quotation pairs in Markdown; these were replaced with ordinary Markdown quotes, and the dedicated quotation gate passes.

Geometry Volume I was rebuilt to 188 PDF pages, and all 11 revised constrained-mechanics pages were visually inspected. Its HTML rendered 306 math expressions without errors. The complete website build rendered all 238 pages successfully. In the website preview, its normal stylesheet prevents page overflow at a 390-pixel mobile width; a standalone chapter render without that stylesheet does not represent the website layout. The content lane still passes 128 tests with four skips after this chapter revision.

The superposition reference received a further full source pass under #4157, including its accessible explanations and critique responses. Five new mechanics/regression tests pass. Its previous prose alternated between a correct incremental theorem and stronger claims about muscle independence and inevitable attraction; the revision applies the same scope throughout.

The final superposition website render displays 355 math expressions without MathJax errors or mobile page overflow. All 34 read-only Python contract checks from the static CI lane passed, including a separate root-hygiene rerun after Quarto completed its temporary output relocation. CI on the preceding commit passed 3,723 Python tests and all eight textbook builds; its remaining terminology failure is corrected by explicitly naming the forward ZTCF. Final-commit CI and deployment remain separate checks.

## First-Wave Merge and Continued Review

PR #4153 protected-squash-merged as `d5a55f229d190e0dda71d7c55f0d5e8f40518281` on September 5, 2026, after all final-head checks passed. CI passed 3,728 Python tests with 29 skips and 92.29% coverage, 128 content-lint tests with four skips, all eight textbook builds, JavaScript tests, browser layout/accessibility checks, and repository governance checks. The 59-file correction wave closes #4148, #4149, #4150, #4151, #4154, #4156, and #4157. Deployment run 33957446845 is tracked separately from the merge.

The next longer-first pass addresses #4158 in the contraction/tangent reference and its generated LaTeX edition. It replaces unsupported numerical tables and a nonfunctional proposed optimizer with a reproducible linear certificate, full differential derivations, and a golf outcome investigation. Eight independent/regression checks pass after the source guard failed on the previous editions. Its revised HTML renders 190 math expressions without MathJax errors, and historical critique anchors are preserved. The critique receives a dated revision note identifying the old source and limitations of its own proposed repairs; that note does not certify the entire critique.

Additional primary sources checked for this pass are the [original contraction paper](https://web.mit.edu/nsl/www/preprints/contraction.pdf), [control contraction metrics](https://arxiv.org/abs/1503.03144), the [integrability amendment](https://arxiv.org/abs/1711.08128), and the [saltation-matrix review](https://arxiv.org/abs/2306.06862). The paired PDF is locally generated and visually checked; it was not previously a tracked publication PDF.

The continued pass also passes all 34 static CI contracts, the publication gate, and 128 content-lint tests with four skips. Its website preview has no page overflow at 1440- and 390-pixel widths. The 12-page PDF's title/contents, equations, code example, and investigation section were visually inspected; the contents and abstract are explicitly separated for print.


## Physics Parallel-Mechanisms Correction

Issue #4160 addresses the next long textbook treatment and its paired web edition. Both editions invented a spine loop, conflated total/internal/platform mobility, inferred unique reactions from kinematics, confused task and constraint null spaces, double-counted drift, treated radial load as torque, and claimed that ground contact supplied mechanical power. The web edition also retained unsupported injury thresholds and a contradictory Stewart-platform example. The chapter is rebuilt around the actual mechanical graph, independent closure rank, separated hand frames, regular constrained dynamics, grasp-map identification, momentum/power balances, and testable golf outcomes. Direct repetitions of the false spine-loop explanation in the anatomy chapter and an inverse-dynamics exercise are corrected in both editions; those targeted corrections do not constitute full audits of either chapter.

Nine independent/regression checks pass after the paired source guards failed on the original editions. They cover complete UPS/SPS mobility accounting, a full-rank platform length Jacobian, compatible motion that changes the hand task, actuation-dependent reactions at identical acceleration, zero moment and power of a radial load, COP/free-moment reconstruction, and the two-point grasp-map null space. The chapter includes a new, visually inspected graph showing both real loops and the connecting spine path. Content lint passes 128 checks with four skips. The 593-page Physics PDF compiles; all 12 revised chapter pages and both targeted passages were visually inspected, with no new chapter overflow or undefined references. The final website preview renders 180 mathematical expressions with zero MathJax error nodes and no page overflow at 1440 or 390 pixels. The existing local-preview polyfill/CSP warning remains distinct from mathematical rendering. All 34 static CI contracts pass. The clean CI-equivalent Python 3.12 suite passes 3,745 tests with 29 skips and 92.32% source coverage; the earlier concurrent-build run had temporary-output failures that disappeared after rendering completed.

Primary sources checked for this pass include the Modern Robotics mobility and constrained-dynamics derivations linked above, [Gosselin and Angeles's original singularity classification](https://doi.org/10.1109/70.56660), and [Brown, Selbie, and Wallace's X-factor measurement study](https://doi.org/10.1080/02640414.2013.775474). Unsupported historical load-sharing attributions, numerical force ratios, and universal performance/injury claims were removed rather than carried forward as established evidence.

PR #4159 merged as `fba9ad794d486ab60895d777c43bcb03dcaafaa6`, with 3,736 Python tests passing, 29 skips, 92.29% coverage, all eight textbook builds, and all other final-head checks passing. Deployment 33959676165 succeeded, including every live route at that revision. Direct public inspection confirms 190 MathJax expressions, zero error nodes, and no page overflow at 390 pixels. Comparable PR axe reports improve from 248 to 246 route-level violation categories; deployment scan counts are a separate baseline. This is not site-wide accessibility clearance.

The full long inverse-dynamics chapter is separately tracked as #4161. Its spatial recursion, bilateral measurement rank, EMG interpretation, instrumentation equations, conditioning examples, and forward-counterfactual conclusions require a comprehensive revision. Its one corrected spine-loop exercise under #4160 does not clear those findings.

## Inverse-Dynamics Chapter Correction

Issue #4161 rebuilds the complete long inverse-dynamics chapter and synchronizes its LaTeX and Quarto editions. The argument now proceeds from the desired scientific quantity through complete spatial mechanics, closure and measurement rank, hand-wrench transport, sensor calibration, recruitment assumptions, segment balance, error propagation, and forward counterfactuals. Nine worked numerical checks and two paired-source guards pass after the old editions failed the guards. A shared workflow diagram replaces the incorrect sequence that implied external loads automatically follow from kinematic differentiation.

The chapter derives weighted load inference, illustrates a sensor that adds no identification rank, distinguishes joint inertia from task singularity, calculates the bridge and charge-amplifier examples with explicit units, and demonstrates why diverged trajectories' acceleration difference cannot recover control. The aerodynamic chapter's incoming explanation is corrected in both editions, and the interdisciplinary LaTeX chapter's matching statement is corrected; these targeted changes do not constitute their full audits. The paired interdisciplinary QMD does not contain that passage and still needs its own full review.

Primary references checked include [Featherstone's spatial recursion](https://royfeatherstone.org/spatial/v2/), [NI's strain-measurement documentation](https://www.ni.com/getting-started/set-up-hardware/data-acquisition/i/strain-gages), [Kistler's force-sensor documentation](https://www.kistler.com/GB/en/cp/piezoelectric-force-sensors-9224a/P0001619), [Farina, Merletti, and Enoka on EMG interpretation](https://pubmed.ncbi.nlm.nih.gov/25277737/), and [OpenSim activation dynamics](https://opensimconfluence.atlassian.net/wiki/spaces/OpenSim/pages/53090590/First-Order+Activation+Dynamics).

Local validation passes 3,756 Python tests with 29 skips and 92.32% source coverage, 128 content checks with four skips, and all 34 static CI contracts. The 580-page Physics PDF compiles with no undefined references or revised-chapter overflow. All 13 revised chapter pages were visually inspected; the final equations and exercise layout were checked again after avoiding an orphaned final exercise. The final website chapter renders 190 mathematical expressions with zero error nodes and no page overflow at 1440 and 390 pixels. The shared figure loads in both editions. The existing local-preview polyfill/CSP warning remains a separate site concern.

PR #4162 merged the preceding loop-mechanics correction as `32b774c438eb325c7cdb059d512e8ea3b54cdda0`. Final-head CI passed 3,745 tests with 29 skips and 92.29% source coverage, 128 content checks, all eight textbook builds, and every other check. Deployment 33964697060 remains separate until revision-matched live verification completes. The corpus inventory's word counts are the original prioritization baseline; its scope column records the work performed and does not certify the remaining claims.


## Aerodynamic Chapter Correction

Issue #4164 replaces the complete aerodynamic chapter and its paired web edition. The corrected treatment connects air-relative velocity, coefficient-area conventions, distributed shaft loads, virtual power, constrained dynamics, inverse load inference, forward interventions, impact conditions, and ball-flight outcomes. It removes incompatible pivot/speed assumptions, universal component percentages, reversed torque-bias claims, energy double counting, and unsupported fatigue, forgiveness, and control-gain conclusions.

The original LaTeX counterfactual reported 85 rad/s from a zero-input equilibrium; the web copy had partly corrected the number but still started from an equilibrium and retained conflicting force signs. The replacement pendulum starts from a stated displaced configuration. Adaptive ODE integration agrees with both an energy identity and an independent angle-domain solution: first-bottom speeds are 2.23607 and 1.75731 rad/s without and with the specified drag. Peak speed with drag occurs before the bottom. The distinction between matched time, event, configuration, and peak outcome is explicit. A shared computed plot and eight exercises connect the derivations to research design.

Eleven mathematical/regression tests pass, covering wind-frame power, coefficient-area pairing, distributed moments with a nonzero pivot offset, virtual power, inverse signs, unchanged input coefficients, exact equilibrium, ODE/energy agreement, and both editions' wording. Primary references checked are [NASA's drag equation](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/drag-equation/), [NASA's equation of state](https://www1.grc.nasa.gov/beginners-guide-to-aeronautics/equation-of-state/), and [Bearman and Harvey's 1976 wind-tunnel paper](https://doi.org/10.1017/S0001925900007617). The unverified `aerodynamic_drag_golf` bibliography entry is removed. The coefficient values are labeled illustrative; their calculation is not experimental validation of a golfer model.

The preceding loop correction's deployment 33964697060 succeeded, including every revision-matched live route. Direct inspection confirms 180 mathematical expressions without errors and no page overflow at 390 pixels. PR #4163 then merged the inverse-dynamics correction as `bbb163d48d21318658dfd6650a6e1bc454ebd461`, with 3,756 Python tests passing, 29 skips, 92.29% source coverage, all eight textbook builds, and all remaining final-head checks green. Its deployment 33966729029 is tracked separately.

Local validation passes 3,767 Python tests with 29 skips and 92.32% source coverage, 128 content checks with four skips, all 34 static contracts, Ruff, Black, and the new test file's code-quality inspection. The 573-page Physics PDF compiles; all nine revised chapter pages were visually inspected, with the final headings and exercise layout rechecked. There are no revised-chapter overfull boxes or undefined references in the final build. The final website renders 135 math expressions without errors or page overflow at 1440 and 390 pixels, with the table and shared figure present. The existing local-preview polyfill/CSP warning remains separate. Other chapters' legacy PDF glyph and duplicate-label warnings are not cleared by this pass.

## Motion-Control Foundations Correction

Issue #4166 replaces the complete introduction in Geometry Volume II, the standalone motion-control chapter, the combined manuscript, and the paired web edition. The revised argument connects the scientific outcome at impact to admissible inputs, feasible trajectories, timing, state completeness, metric choice, feedback, and uncertainty. It retains motion as the organizing perspective while correcting the false opposition between motion and established tracking, regulation, and Lyapunov analysis.

Worked examples show that identical configuration paths with different timing require different forces and produce different impact velocities; a feasible reference may lack a unique input, while a geometrically drawn curve may have no admissible input; finite-time boundedness is weaker than attraction; and a shrinking geometric tube needs a verified boundary condition. An explicit one-input spring system has four-dimensional controllability and four independent unforced trajectory derivatives. This also corrects the repeated false twice-actuator-count bound in chapter two of all four editions. Only that passage of chapter two is cleared by this correction.

The chapter derives a robust scalar tube, distinguishes orbital attraction from phase synchronization, qualifies nearest-point phase coordinates and coordinate scaling, and states what a golf investigation must measure before drawing conclusions about muscle coordination, wrist moments, or performance. Thirteen tests independently check the numerical and algebraic examples and prevent the identified claims from returning in paired sources. The initial source guards failed on the old editions; all thirteen pass on the revision. The shared timing plot and ten exercises make the dependencies inspectable.

Primary sources checked include [MIT's trajectory LQR treatment](https://underactuated.mit.edu/lqr.html), [Lyapunov analysis](https://underactuated.mit.edu/lyapunov.html), [orbital stability](https://underactuated.mit.edu/limit_cycles.html), and [Manchester's transverse-dynamics treatment](https://arxiv.org/abs/1010.2241). The examples are mathematical model checks, not empirical validation of a golfer model.

Local validation passes 3,780 Python tests with 29 skips and 92.32% source coverage, 128 content checks with four skips, all 34 static CI contracts, Ruff, Black, and the new test file's code-quality inspection. Four PDFs were rebuilt: Geometry Volume II (107 pages), the complete motion-control manuscript (83), and standalone chapters one and two (10 and 16). All 27 revised introduction pages across the three print editions, the changed covers and preface, and the targeted chapter-two rank passages were visually inspected. The complete web volume renders 1,072 math expressions with zero error nodes and no page overflow at 1440 and 390 pixels. Existing warnings elsewhere in these books are not cleared by this pass.

The first test run identified stale dependency digests after the Volume II preface changed. The existing book-publication and claim-audit records were regenerated while preserving their historical review scopes, rationales, and limitations. This metadata refresh does not certify the entire volume's scientific content. The corpus inventory continues to distinguish complete chapter corrections from targeted passages and pending reviews.

PR #4163's deployment 33966729029 succeeded through every revision-matched live route; direct inspection confirms 190 mathematical expressions without errors or mobile overflow. PR #4165 merged as `627918628c52ef0320136f201668a5f816717f8a`, after final CI passed 3,767 tests, 92.29% source coverage, all eight textbook builds, and every other check. Its deployment 33968761331 remains tracked separately until live verification completes.

## Curve Geometry and Its Mechanical Interpretation

Issue #4168 replaces the complete long curve chapter in Geometry Volume II, the standalone and combined motion-control manuscripts, and the paired web volume. It distinguishes physical clubhead paths, configuration paths, and normalized state trajectories before connecting their geometry to force, timing, and impact outcomes. The revised chapter repairs the ellipse-axis example, regularity at stops and inflections, metric dependence, oriented torsion, partial Frenet frames, shape reconstruction, and the local limits of osculating and nearest-point constructions.

The mechanical treatment derives physical acceleration and the Jacobian-rate contribution, distinguishes total force from actuator effort and zero-work constraint reactions, and substitutes path timing into constrained equations of motion. The exact phase/transverse coordinate equations replace the unsupported universal speed-squared stability correction. Two systems with the same timed circular orbit have opposite transverse stability; a shared calculated figure makes this counterexample visible. A rotating-normal-frame test independently checks the speed-times-skew transport term. A commuting-input example also disproves the proposed identification of high curvature with higher-order Lie brackets.

The golf investigation now specifies space, frame, metric, derivative estimation, low-speed uncertainty, event alignment, feasible loads, and held-out impact outcomes. Unsupported measured acceleration, release signatures, universal curvature peaks, and coaching conclusions are replaced with explicit hypotheses and ten exercises. The framework connects the mathematical description to evidence without treating the description itself as a biological explanation.

Primary references checked include [Petersen's differential-geometry notes](https://www.math.ucla.edu/~petersen/DGnotes.pdf), [Lynch and Park's path-constrained time scaling](https://modernrobotics.northwestern.edu/nu-gm-book-resource/9-4-time-optimal-time-scaling-part-1-of-3/), and [Manchester's transverse-dynamics treatment](https://arxiv.org/abs/1010.2241). Sixteen independent mathematical/regression checks pass after all four old-edition guards failed. Local validation passed 3,796 Python tests with 29 skips and 92.32% source coverage; 128 content checks with four skips; all 34 static contracts; Ruff, Black, and the new test file's code-quality inspection. Three publication PDFs were regenerated and all revised chapter pages visually inspected. Their final exercise spacing avoids an isolated final exercise. The web volume has over 1,000 mathematical expressions without error nodes or page overflow at 1440/390 pixels; all 21 revised display equations were explicitly brought into view and typeset, and the shared figure loads.

The paired-edition inspection caught a quotation conversion that had replaced two second-derivative marks in the introduction's web equations. PR #4167 corrects those marks before merge and adds a dedicated guard; the conversion helper now matches actual paired prose quotes instead of replacing derivative primes. The curve guards also require the correct second and third derivatives. The repaired introductory equation was checked in the browser's rendered MathML. A pattern scan of earlier changed web sources found no additional derivative-quote occurrences; this scan is not full route clearance.

PR #4165's deployment 33968761331 succeeded through every revision-matched live route. Direct inspection confirms 135 math expressions, zero errors, and no mobile overflow. PR #4167 merged as `e79dbdee039a00b3a83f9732c3f2d6515c31c0b8` after every final-head check passed, including 3,781 tests with 29 skips and 92.29% source coverage and all eight book builds. Deployment 33972606460 remains a separate pending gate. Curve correction PR #4170 passes all 30 focused foundation/curve checks after rebase. The complete corpus and the remaining book chapters remain open; configuration-manifold findings are tracked in #4169.

## Configuration Geometry, Mechanical Forces, and Attitude Tracking

Issue #4169 covers the full configuration-manifold chapter in Geometry Volume II and the standalone, combined, and web motion-control editions. The correction distinguishes ideal joint topology from limits, closure, and task outputs; configuration from augmented mechanical state; and force covectors from tangent vectors. It derives coordinate transformations that preserve power and kinetic energy, separates metric length from work and timing-dependent kinetic energy, and writes both vector and covector versions of the mechanical equations consistently.

The rotation treatment states body/world and ZYX conventions, separates coordinate, mechanical, and task singularities, and distinguishes the principal logarithm's half-turn branch ambiguity from the exponential differential's full-turn singularity. It removes unsupported spacecraft and universal golf-shoulder claims. A polar-coordinate free particle demonstrates nonzero connection coefficients on flat space; a passive gravity pendulum demonstrates that zero input does not imply a kinetic-metric geodesic. The path-force equation includes potential, passive/external loads, actuation image, and bounds. The attitude example derives reference-frame transport and feedforward, verifies its power-like dissipation identity, and identifies the undesired half-turn equilibrium instead of promising global convergence.

Eleven independent numerical examples and four paired-edition guards pass; the guards failed on all four old editions before correction. The full local suite passes 3,812 tests with 29 skips. All 34 static contracts and 128 content checks (four skips) pass. A preliminary content run overlapped the temporary selected-page Quarto configuration; both configuration-dependent failures pass after the normal configuration is restored. No evidence digest was rewritten to conceal that transient mismatch. Three PDFs were regenerated: Geometry Volume II (94 pages), complete motion-control (71), and standalone chapter three (10). All 29 revised pages were visually inspected. The web volume initially contains 922 math containers; all 17 revised displays were explicitly typeset, with zero math errors or page overflow at desktop/mobile widths.

Primary conventions were checked against [Lynch and Park](https://hades.mech.northwestern.edu/images/2/25/MR-v2.pdf) and [Lee, Leok, and McClamroch](https://arxiv.org/abs/1003.2005). The investigation protocol connects coordinate consistency, measured loads, energy balance, uncertainty, forward interventions, and impact outcomes. Neither a configuration tube nor a geometric model is presented as evidence of a biological control policy. Remaining chapters and the full corpus still require review.

PR #4170 merged as `9efa16e9a9f6931c3a820f8a85878063e426607d` after all final-head checks passed, including 3,797 Python tests and all eight textbook builds. Its deployment 33974016554 supersedes the cancelled 33972606460 run and includes the foundations correction; live verification is still pending. The manifold correction is PR #4172.


## Orbital Stability, Transient Growth, and Impact Sensitivity

Issue #4171 covers the full orbital-stability chapter in all four motion-control/Geometry Volume II source editions. It separates timed tracking, stability of invariant periodic sets, and finite swing segments. A persistent small phase offset demonstrates failure of asymptotic timed convergence rather than instability. Exact nominal-time phase/transverse coordinates include the phase denominator and rotating-normal-frame term, with dimensions and locality assumptions stated. A controlled oscillator exposes the original feedback sign/projection error; a second example rejects an infeasible nominal circle before linearization.

The periodic treatment states the strict Floquet test and its limits, distinguishes the autonomous phase multiplier from additional neutral directions, and relates Poincare and transverse maps through consistent coordinates. Opposite cubic radial dynamics with identical unit multipliers demonstrate why nonlinear stability is inconclusive at the boundary. Hybrid events include the saltation correction for changed event timing, differentiable guards/resets, nongrazing passage, and a fixed transition sequence. Unsupported numerical gait and biological-feedback percentages are replaced with labeled mathematical examples.

Finite-horizon analysis uses transition singular values and physical metrics. A stable triangular system amplifies errors above 3.7 before terminal decay. Event-output sensitivity includes the perturbed impact time; a constant-speed example separates position cancellation at the event from remaining velocity error. The LQR derivation preserves input dimensions, terminal conditions, phase scheduling, and the Riccati dissipation identity. A checked scalar Riccati example has expanding invariant level-set width, disproving automatic geometric shrinkage. Nonlinear remainder, disturbance, saturation, boundary, and reset conditions are required before claiming a verified tube. A shared two-panel figure makes both counterexamples visible.

The golf protocol links feasible nominal motion, perturbation transport, uncertain measurements, event outcomes, and identifiable interventions. A fitted path or local gain does not identify human feedback; lower impact dispersion can reflect several mechanisms that the proposed experiments must distinguish. Primary references checked are [Tedrake's limit-cycle notes](https://underactuated.mit.edu/limit_cycles.html), [his LQR treatment](https://underactuated.mit.edu/lqr.html), [Manchester's transverse-dynamics analysis](https://arxiv.org/abs/1010.2241), and [Kong and colleagues' hybrid sensitivity derivation](https://arxiv.org/html/2306.06862v3).

Twelve independent mathematical checks and four edition guards pass after all four guards failed on the original sources. Local validation passes 3,828 tests with 29 skips and 92.32% source coverage, 128 content checks with four skips, all 34 static contracts, Ruff, Black, and code-quality inspection of the new test. Three publication PDFs were regenerated (88/66/9 pages); every revised page was visually inspected (27 pages total). The web volume has 844 math containers; all 20 revised displays actually typeset with zero errors. The shared figure loads and desktop/mobile layouts have no page overflow. The remaining corpus is still pending.

PR #4172 has passed every final-head check, including 3,812 CI tests with 29 skips and 92.29% coverage and all eight textbook builds. Its anti-phantom guard initially parsed an external NASA URL as a repository path; the issue now names the actual changed source/test files, and the unmodified guard passes on rerun. Publication remains separately tracked.


Deployment 33974016554 succeeded through every revision-matched live route and publishes both foundations and curve corrections. Manifold PR #4172 merged as `0ed10bdb28705e829e1aa99d2662ecda7731a977`; deployment 33975676795 is pending. The orbital correction is PR #4174. The next full chapter audit is trajectory optimization, issue #4173.


## Trajectory Objectives, Numerical Fidelity, and Repeatability

Issue #4173 covers the complete trajectory-optimization chapter in Geometry Volume II and the standalone, combined, and web motion-control editions. The correction fixes reversed normalized-time dynamics and the inconsistent Hamiltonian optimization sign. It develops an event-aware OCP with duration, contact geometry, first passage, actuator bounds, normal PMP/transversality conditions, and the limits of necessary conditions. A free-time effort example has an unattained infimum; rest-to-rest effort, minimum-time travel, and maximum first-arrival speed select different control patterns. These independently checked examples replace the uncomputed universal shoulder-torque reversal.

The transcription treatment derives Hermite--Simpson defects, checks an exact cubic example, and separates numerical solver success from continuous-path feasibility, global optimality, and model validity. Underactuated equations use configuration accelerations and the actual actuation image, with independent closure/contact constraints and loads. Clubhead speed uses the full kinematic Jacobian; equal distal angular speed can coexist with zero or nonzero tip speed. The shared figure compares duration costs and objective-dependent control schedules without claiming measured golf data.

The uncertainty treatment separates local covariance propagation from covariance steering and nonlinear robustness certification. It includes multiplicative-noise contributions, impact-time sensitivity, bias plus variance in output loss, and marginal versus joint chance constraints. A constraint Jacobian condition number is not an absolute gain, and an exactly satisfied hitting guard cannot represent all impact errors. The original Harris–Wolpert hypothesis is presented within its eye/arm movement scope rather than as a universal golf torque-noise law. The golf protocol connects objective choice, constrained power flow, perturbation transport, impact outcomes, and forward interventions that can distinguish competing explanations.

All 12 mathematical checks and four edition guards pass after the old-edition guards failed. Local validation: 3,844 Python tests, 29 skips, 92.32% source coverage; 128 content checks with four skips; all 34 static contracts; Ruff, Black, and new-test code-quality inspection. Three publication PDFs (88/70/9 pages) are rebuilt and all 27 revised pages visually inspected. The final web volume contains 916 math containers; all 14 revised display equations actually typeset, with zero math errors and no page overflow at 1440/390 pixels. The shared figure loads. Final copy review clarified random output moments and improved a heading's line break.

Primary sources checked: [Tedrake's trajectory-optimization notes](https://underactuated.mit.edu/trajopt.html), [Kelly's original transcription tutorial](https://arxiv.org/abs/1707.00284), [Liberzon's maximum-principle and endpoint conditions](https://liberzon.csl.illinois.edu/teaching/cvoc/node1.html), [Tedrake's robust/stochastic notes](https://underactuated.mit.edu/robust.html), and [Harris and Wolpert's original paper](https://www.nature.com/articles/29528). The adjacent funnel chapter is separately tracked in #4175; the remaining corpus is not cleared.

Orbital PR #4174 passes 3,828 CI tests with 29 skips and 92.29% coverage, and all eight textbook builds. Its browser checks and publication remain separate gates.


## Long-Chapter Visibility Repair

Issue #4176 records a reader-facing failure discovered during trajectory chapter visual QA: a long section remained fully transparent despite valid equation markup and no horizontal overflow. The fade observer's 10% threshold was unreachable for the chapter's height in the tested browser. The canonical module now reveals a section on any intersection. A controlled 18,000-pixel chapter regression fails with the old asset and passes with the corrected asset, including an anchor into a nested example and resizing to a mobile viewport. Tests run against the current local preview with service workers blocked to avoid stale assets.

Validation: two browser regressions pass, all 325 JavaScript tests pass (19 skipped), and all 34 static contracts pass. Browser QA explicitly checks ancestor opacity; element presence and MathJax success alone are insufficient evidence of visible prose. The existing preview service-worker cache was cleared before checking the corrected full publication page.

Orbital PR #4174 merged as `c6496781dc9f96894dd0f78eb8c8b563e474c2cd` after all final-head checks passed, including 3,828 CI tests, 92.29% coverage, and eight book builds. Deployment 33977761586 remains in progress. Manifold deployment 33975676795 succeeded through every revision-matched live route.


## Nonlinear Funnels, Impact Goals, and Certificate Scope

Issue #4175 replaces the full funnel-synthesis chapter in all four source editions. It derives the moving-boundary condition with nominal residual, time derivative, disturbance quantifiers, regularity, and finite-horizon scope. Exact robust scalar examples show valid widening, a disturbance floor, saturation limits, and the difference between exponential attraction and finite-time equality. Timed goal inclusion and event arrival/output conditions are separate obligations. Phase coordinates retain their physical speed and progress requirements.

The corrected chapter distinguishes outer forward uncertainty envelopes, inner backward capture certificates, and cost/value-function comparisons. It derives SOS boundary implications with free equality multipliers and explicit strictness margins, checks a complete polynomial identity, and demonstrates escape missed by endpoint sampling. Taylor remainders, lifted mechanical variables, floating-point Gram validity, knot conditions, and reset composition are explicit. Ellipsoid axes use inverse eigenvalues, joint normalization is identified, and general volume synthesis is not described as one convex SDP. A calculated equal-volume example gives tenfold different task-output tolerances.

The golf treatment connects constrained mechanics, available correction effort, estimation/delay, perturbation transport, progress, and impact outputs. Uncomputed athletic funnel shapes and exact physiological limits are replaced with conditional mechanisms and a reproducible verification protocol. The shared scalar figure is labeled as calculated model evidence, not a fitted golf result.

Validation: 18 new mathematical/source checks pass after all four old-edition guards failed. The full local suite passed 3,862 tests, 29 skips, with 92.32% source coverage; the focused checks pass after final exposition/layout edits. All 128 content checks, four skips, and 34 static contracts pass. Ruff, Black, and new-test code-quality inspection pass. Three PDFs (90/77/10 pages) are rebuilt; all 29 revised pages are visually inspected, with unchanged early pages confirmed byte-identical after final rendering. The nine revised website display equations actually typeset; the final volume contains 1,055 math containers, zero math-error nodes, and no overflow at 1440/390 pixels. Parent opacity is explicitly verified and the shared figure is visible. Existing unrelated preview CSP warnings are not cleared by this work.

Primary sources: [Tobenkin, Manchester, and Tedrake (2010)](https://arxiv.org/abs/1010.3013), [Majumdar and Tedrake (2017)](https://groups.csail.mit.edu/robotics-center/public_papers/Majumdar16.pdf), [Tedrake's Lyapunov notes](https://underactuated.mit.edu/lyapunov.html), and [Boyd and Vandenberghe's convex-optimization text](https://web.stanford.edu/~boyd/cvxbook/bv_cvxbook.pdf). Their mathematical methods do not establish the proposed human-golf interpretation.

Orbital deployment 33977761586 succeeded through every revision-matched live route. Trajectory PR #4178 now passes 3,848 CI tests, 92.29% coverage, and all eight book builds; its browser checks remain in progress. Scanner false positive #4181 is repaired in that PR. The funnel branch incorporates the repair and separately reruns the scanner contract suite and complete 348-file syntax scan. The next recorded full-chapter findings are phase control #4177, stochastic motor variability #4179, and learning control #4180. The full corpus remains open.


## Phase Control, Coordination, Internal Dynamics, and Contact

Issue #4177 fully replaces the phase-variable chapter in four source editions. The treatment distinguishes configuration paths from their velocity-state lifts, retains phase speed and acceleration, and states monotonicity, progress, estimation, and periodic-coordinate limits. Virtual constraints require both position and tangent-velocity conditions; feedback depends on decoupling rank, actuator bounds, and curvature terms. Analytic counterexamples demonstrate decaying output error with unstable internal motion and active mechanical power on an exactly maintained virtual constraint.

The chapter derives an explicitly scoped plastic-contact reset and shows why continuous zero dynamics need not remain invariant at impact. It separates hybrid invariance, orbital stability, finite arrival, and golf contact outputs. The golf argument connects input authority, hand/ground closure, phase estimation, internal energy, clubhead speed, event geometry, and held-out outcome validation. It makes no claim that human coordination identifies a particular neural controller.

Validation: 14 mathematical/source checks, including four old-edition failures before correction; full local suite 3,880 passed, 29 skipped, 92.32% source coverage. Final focused checks and 128 content checks pass, four content skips. All 34 static contracts and the 348-source syntax scan pass after final edits, alongside Ruff, Black, and code-quality inspection. Three PDFs (92/80/6 pages) are rebuilt; all 18 revised pages are visually inspected. Display equations use readable lines, the figure stays with its explanation, and exercises stay together. The final web volume has 1,108 math containers, all 12 revised displays actually typeset, zero math errors, parent opacity one, and no page overflow at 1440/390 pixels. Existing update notices and unrelated preview console warnings are outside this correction.

Primary reference: [Grizzle and Chevallereau, Virtual Constraints and Hybrid Zero Dynamics](https://grizzle.robotics.umich.edu/files/HZD_HandbookApril2015.pdf). Worked counterexamples and golf interpretation are separately derived and do not claim an experimental golf result.

Trajectory PR #4178 merged after every check passed, including 3,848 CI tests and all eight book builds; deployment 33981540954 is pending. Funnel PR #4182 is open at 505d85eed316071bced8a5a03afe3a22ca01e12d. Its issue's provisional test filename was corrected to the implemented name so the unchanged merge guard could verify the scope; that rerun passed. Browser CI remains pending. Stochastic #4179 and learning #4180 are claimed and under primary-source review. The final case-study outline and glossary have further findings awaiting correction. Full-corpus #4021 and the route batches remain open.


## Motor Variability, Stochastic Dynamics, and Outcome Risk

Issue #4179 fully replaces the stochastic chapter in four source editions. It specifies discrete conditional noise, Brownian scaling and physical units, Itô/Stratonovich conventions, multiplicative moment dynamics, task covariance, and random contact time. Original force, eye/arm, reaching, and target-acquisition studies are distinguished from untested golf extrapolation. Worked examples separate kinematic jerk from torque, mean stability from mean-square stability, and lower peak effort from lower impact variance. A bounded mean-variance optimization explicitly has both maximum-effort and interior optima. The text connects these distinctions to inertial coupling, sensing, feedback, phase, and impact goals.

Validation: 20 mathematical/source checks; the four old-edition guards failed before the rewrite. PDF review caught a missing plus between stochastic phase drift and diffusion; a new expression guard failed in all four editions before synchronization and now passes. Full local suite: 3,900 passed, 29 skipped, 92.32% coverage. Final focused checks and 128 content checks pass, four content skips. All 34 static contracts and the 348-source syntax scan pass. Ruff, Black, and code-quality inspection pass. Three PDFs (93/85/7 pages) were rebuilt and all 21 revised print pages inspected. Web verification: 1,173 math containers, all 12 revised displays typeset, zero math errors, parent opacity one, and no page overflow at 1440/390 pixels; light and dark appearance inspected. Existing update notices and unrelated preview console warnings remain outside this correction.

Primary sources: Jones, Hamilton, and Wolpert (2002); Harris and Wolpert (1998); Flash and Hogan (1985); Fitts (1954); Särkkä and Solin (2019). Original article links and explicitly scoped interpretations are included in the chapter. Figures and numerical counterexamples are calculated illustrations, not experimental golf results.

Trajectory PR #4178 deployment 33981540954 succeeded. Funnel PR #4182 merged as 3defe864edb195f48a106dca8a646a2f82120ff0 after every check passed, including 3,866 Python tests and all eight book builds; deployment 33983136557 remains in progress. Phase PR #4183 is open at e5c8c54f4393e23a6023167b54a47e38c00ed55f with only browser CI still pending. Learning #4180 has a complete draft but no synchronized correction or numerical verification yet. Full-corpus #4021 and remaining route batches remain open.


## Learning, Identifiability, and Controller Reuse

Issue #4180 fully replaces the learning chapter in four source editions. The text derives the lifted ILC update with explicit error sign, dimensions, feedback map, and repeatability assumptions. Independent examples show asymptotic convergence with transient growth, gain-driven divergence, filtering bias, and a measurement-noise floor. It separates task improvement from parameter identification and Lie-algebra accessibility. Policy gradients retain their assumptions; reward penalties do not become hard constraints; two stable systems can have an unstable average. Library reuse requires valid initial sets, transfer conditions, and renewed whole-interval verification after adaptation. The golf interpretation connects command learning, feedback, coordination, energy, contact timing, and held-out outcome comparisons without asserting a biological learning law.

Validation: 20 mathematical/source checks, including four old-edition failures before synchronization. Full suite: 3,920 passed, 29 skipped, 92.32% coverage. Final focused checks and 128 content checks pass, four content skips; 34 static contracts and 348-source syntax scan pass. Ruff, Black, and code-quality inspection pass. Three PDFs (94/90/7 pages) are rebuilt; all 20 revised pages are visually inspected. Final layout keeps references and exercises together and avoids stretched inter-section spacing. Web: 1,224 math containers, all 10 revised displays typeset, zero math errors, visible parent content, no 1440/390 page overflow, and inspected light/dark appearance. Figure-adjacent inline mathematics is also checked after lazy typesetting.

Primary sources: Bristow, Tharayil, and Alleyne (2006), Sutton et al. (1999), and Tedrake et al. (2010), linked in the chapter. Counterexamples and the golf-testing protocol are independently derived, not experimental golf results reported by those papers.

Funnel deployment 33983136557 succeeded through every live route. Phase PR #4183 merged as 693e4a5f7965e91e55cd0a440698b0239fdd4f0d after every check passed; deployment 33984854219 remains pending. Stochastic PR #4185 is open at 75406b7ff2576c8894ae1e316c7a3535d45f5f67 with browser CI pending. Case study and shared glossary/reading correction #4184 is claimed and drafted only. Full-corpus #4021 and the remaining route batches stay open.


Final learning publication check: the final layout exposed a validator false positive for a valid closing LaTeX group. Added one failing-before/fixed-after regression and accepted the closing command without changing malformed-prose detection. All 42 structure tests, the article-wide structure scan, Ruff, Black, and code-quality checks pass. The previously recorded full-suite count precedes this additional regression. Phase deployment 33984854219 succeeded; stochastic PR #4185 merged as 11f3e623 after every required check passed.

## Golf Case Study, Shared Glossary, and Reading Lists

Issue #4184 replaces unsupported completed-optimization, perfect-swing, and passive-noise-removal claims in all four concluding case-study editions. The chapter is now a reproducible investigation protocol with independently derived examples. It connects declared input authority, mechanical power, dynamically feasible references, phase speed, first-contact timing, output sensitivity, nonlinear containment, uncertainty, and learning. It distinguishes analytic examples, numerical candidates, verified model guarantees, and measured golf results. No completed musculoskeletal optimization or golf experiment is invented.

Every entry in the three shared glossary copies is reviewed and corrected where necessary: tangent/configuration/state distinctions, local flow and exponential maps, force/velocity duality, kinematic versus dynamic feasibility, stability versus attraction, bounded directional authority, and the exact zero-torque/zero-velocity intervention. The three reading lists now explain the scope of primary texts, provide authoritative companion links, and remove unsupported superlatives. Back-matter headers and unreferenced glossary equations no longer inherit the preceding chapter number; reading entries and the case-study references remain together across page breaks.

Validation: nine independent mathematical checks plus seven old-source-failing/fixed-source-passing guards; all 16 pass. Full suite: 3,936 passed, 29 skipped, 129 deselected, 92.32% coverage. Content: 128 passed, four skips. All 34 static contracts and the 348-source syntax scan pass with the pending learning PR's valid-LaTeX-group validator repair applied; 58 combined case/validator checks pass. Ruff, Black, and code-quality inspection pass. Five PDFs (299/189/100/94/6 pages) are rebuilt. All 33 revised print pages were inspected; affected pages were inspected again after the final header and page-break changes. Web: 1,260 math containers, all eight revised displays typeset, zero mathematical errors, opacity one, no 1440/390 page overflow, and light/dark appearance inspected. Final source changes after browser review only alter stripped print-layout commands and trailing whitespace.

Primary sources: Bullo and Lewis, Featherstone, Grizzle and Chevallereau, Majumdar and Tedrake, and the six primary texts linked in Further Reading. These support engineering methods and terminology; they do not experimentally validate the proposed golf protocol.

Phase deployment 33984854219 succeeded. Stochastic PR #4185 merged as 11f3e6235912fb2911a55555524e11ca7e5d0778 after every required check passed; deployment 33986589045 is in progress. Learning PR #4186 is open at e202df8ca653339f9563db14b6e97a51a7913d17. The case-study branch is still based on the earlier learning content commit and must be rebased onto the merged learning PR before publication. Full-corpus #4021 and the remaining route batches remain open. Initial close reading of the long manifesto has reached line 354 and found additional geometric and flexible-shaft issues to investigate; that article is not cleared.


## Rigid–Flexible Foundations and Golf Interpretation

Issue #4187 reviews the complete canonical Theory Part 1 and synchronizes its foundational derivation with the first part of the long manifesto and the foundational sections of the affine monograph. Corrected local configuration/state geometry, the force-covector/metric distinction, zero-input drift versus geodesic spray, rotating shaft Jacobians, head center-of-mass offset and rotary inertia, complete system inertia, passive-load and energy accounting, modal gravity, input rank and bounded acceleration sets, Schur-complement semidefiniteness, coordinate-dependent coupling, and same-state versus trajectory interpretation. The introduction and accessible explanations now match those limits, including fixed-base reactions and the difference between mathematical and empirical validation. Verified primary reading guidance replaces unverified video timestamps.

An independently constructed rotating flexible-link example changes one coupling coefficient from 0.050389 to 0.636186 without changing equipment parameters. Ten mathematical checks verify placement derivatives, center-of-mass velocity, direct versus assembled energy, positive inertia, Schur/input identities, passive-load retention, coordinate scaling, and covariant acceleration. Three source guards fail before the correction and pass afterward. Thirteen focused checks and 25 combined foundational/content-contract checks pass. Full suite: 3,949 passed, 29 skipped, 129 deselected, 92.32% coverage. Content: 128 passed with four skips. All 34 static contracts and the 348-source syntax scan pass with the already merged #4186 valid-LaTeX-group validator repair temporarily applied; that unrelated repair is not duplicated in this commit. Ruff, Black and code-quality checks pass. Final changes after the full suite only split three equations for readability, move an equivalent gravity definition into prose, and link the executable example; focused checks pass again.

All three routes render. Browser inspection confirms 24/24/25 scoped display equations and 198/509/597 total math containers respectively, zero mathematical errors, visible example headings (effective opacity 0.9), and no 1440/390 page overflow. Nine desktop/mobile/light/dark screenshots were inspected after splitting the wide equations. These are web sources; no print manuscript is modified by this correction.

Primary sources read: Bullo and Lewis's author-hosted geometric-control workshop (configuration acceleration, force covectors, and kinetic metric, PDF pp. 18–22), Lynch and Park's official Lagrangian-dynamics and mass-matrix transcripts, and the previously verified Featherstone teaching resources. The numerical example is independently constructed and does not claim empirical golf validation.

Stochastic PR #4185 is published (deployment 33986589045 succeeded). Learning #4186 merged as e611e3ad with every check passing; deployment 33988300905 is in progress. Case study/glossary PR #4188 is open at 4e9185b4 after rebase and 58 combined checks. This foundations branch is still based on old case content dd513dd8 and must be rebased onto merged #4188 before publication. The complete canonical Theory Part 4 has now been read; its modal/pendulum appendix defects are tracked and claimed in #4189, with no appendix edits yet. Other portions of both long articles remain pending; full-corpus #4021 and route batches remain open.


## Modal Appendices and Reproducible Pendulum Models

Issue #4189 fully reviews Theory Part 4 and synchronizes the matched manifesto Part 4 and affine-monograph appendices. The revised derivation retains complete passive loads and gravity, distinguishes input-induced from total acceleration, derives the Euler–Bernoulli weak form and dynamic tip-mass/rotary-inertia boundary conditions, and uses the loaded mass inner product. It removes unsupported universal mode-count, frequency and accuracy claims. A completely specified rotating flexible pendulum supplies its placement, kinetic/potential energies, all coefficients and units, force equations and energy balance. Gravity reverses the total modal acceleration relative to the torque contribution in the worked example. A coordinate-sign change leaves physical displacement unchanged; support freedom changes the modal frequency. Spatial examples distinguish a point pendulum's two orientational degrees of freedom from positive-definite rigid-body SO(3) dynamics and derive the body-angular-velocity variation rather than applying coordinate Christoffel formulas to quasi-velocities.

Fifteen new checks (12 independent mathematical checks and three old-source failures) verify energy assembly, potential gradients, velocity-power cancellation, integrated work/dissipation, acceleration signs, modal-coordinate transformations, loaded frequencies, beam inertia/stiffness, non-diagonal damping, manufactured weak-form boundary signs, spatial gravity torque and point-mass rank. Full suite: 3,964 passed, 29 skipped, 129 deselected, 92.32% coverage. Content checks: 128 passed and four skips. All 34 static contracts and the 348-source syntax scan pass with the already merged #4186 valid-LaTeX-group validator temporarily applied and then restored; that repair is not duplicated. Ruff, Black and code-quality checks pass; the final change only renames a test constant to its required dimensional name, and all 15 checks pass again.

All three routes render and all 28 scoped displays per route typeset. They contain 136/524/542 math containers, zero math errors, visible example headings (effective opacity 0.9) and no 1440/390 page overflow. Nine final desktop/mobile/light/dark screenshots and the complete figure on each of the three routes are inspected. The figure is independently calculated scientific output with different units on its two panels. No print manuscript changes.

Primary reading: Chatterjee, Batra and Natarajan (2024), arXiv:2407.16195v1, Sections I–II (moving support, loaded beam boundary conditions and state inner product); Holm, Marsden and Ratiu (1998), arXiv:chao-dyn/9801015, rigid-body variations and equations 1.13–1.26; MIT Parks beam-vibration lecture. These support model formulation, not empirical golf validation.

Case/glossary PR #4188 merged as 6ad676bb after every check passed. Learning #4186 is published. Foundations #4191 is open from rebased 6e6d8002 with 55 combined post-rebase checks passing. This modal correction is based on old foundation content b042fe10 and must be rebased onto merged #4191 before publication. Counterfactual/invariance #4190 is claimed after reading canonical Parts 2, 3 and 5; corrections are next. The bibliography's Koike 2019 record misnames a rugby-kicking study as a golf study and will be repaired in that batch. Other corpus routes remain open under #4021 and epic #4009.


## Counterfactual Dynamics, Invariance and Evidence

Issue #4190 completes the full review of canonical Theory Parts 2, 3 and 5, with matched sections and consistency edits throughout both long editions and the Part 1 input definition. Combined with #4187/#4189, both long articles have now received a complete substantive read and mathematical review. This is a scoped content review, not empirical validation of their proposed golf research program.

The revision separates a same-state acceleration decomposition, a recorded-trajectory integral and a freely evolving zero-input branch. It specifies state, loads, schedules, admissibility and event matching; derives reference sensitivity, logarithmic-norm error bounds and contact-time corrections; and replaces unsupported universal prediction horizons with output tolerances. Checked examples show positive input work with deceleration, nonlinear contraction, linear divergence, nonnormal transient amplification and a model-error floor. The invariance treatment distinguishes force inputs, stiffness commands, activation dynamics, feedback policies, coordinate-dependent input zeros and constrained reaction forces. It retains the energy supplied by time-varying stiffness and distinguishes inverse net loads from individual muscle forces, braking from torque sign, and stiffness from stability. The numerical section is a reproducible proposed procedure rather than a claim of completed physical validation.

Primary-source correction: the Koike et al. 2019 reference is a rugby-kicking induced-speed analysis, not a golf paper. Its title, authors, journal, pages and DOI are corrected in the bibliography and reading guide; the explanation distinguishes coefficients evaluated on recorded motion from independent forward interventions. The accepted manuscript's abstract, methods and discussion were read. Nesbit's 2005 golf paper was checked for its actual inverse/kinematically driven modeling scope. Neither study is represented as validation of the proposed zero-input branch experiment.

Validation: 22 new checks (15 independent numerical/analytical checks and seven source regressions); 3,986 full-suite passes, 29 skips, 129 deselected, 92.32% coverage. The first run exposed a stale bibliography dependency fingerprint; regeneration corrected the evidence inventory/report and the final full suite passed. All 34 static contracts, 128 content checks/four skips and the 348-source syntax scan pass with the already merged valid-LaTeX-group validator applied temporarily and restored. A final dimensional clarification of a squared stiffness command was followed by all 22 checks, content checks, syntax/evidence checks and three fresh renders. Ruff, Black and code-quality checks pass.

All six affected routes render. Theory Parts 1/2/3/5 and the manifesto/affine article contain respectively 201/114/90/13/551/580 math containers and 26/19/10/0/83/89 display equations. Every display was actually typeset; no math errors or page overflow at 1440/390 pixels, and inspected headings are visible. All 27 desktop/mobile/light/dark screenshots were inspected, with affected targets refreshed after the last edit. No print manuscript changes.

Case/glossary #4188 is published. Foundations #4191 merged e4977cd7 with every check passed and deployment pending. Modal #4193 is open at caf92db9, with 57 combined post-rebase checks passing. This batch is based on old modal content 1dba0b2f and must be rebased onto merged #4193 before publication. The next independently claimed finding is #4192: the long tangent reference confuses tangent spaces, derivatives, finite predictions and functional input sensitivities. Parts I/II and the concluding/appendix claims have been read; Part III still needs full review. The rest of the corpus remains open under #4021 and epic #4009.


## Tangent Reference Foundations, Transport and Sensitivity

Issue #4192 corrects Parts I/II, the conclusion and appendices of the long unified reference after a full substantive read. The remaining Part III has also been fully read; its control/numerical/mechanical findings are separately tracked in #4194 and are not certified by this batch.

The revision separates tangent spaces, derivatives of maps and coordinate Jacobians of vector fields; states differentiability and remainder hypotheses; and gives checked counterexamples to directional-linearity, quadratic-remainder and imaginary-eigenvalue claims. It derives the moving-chart transformation, distinguishes flow transport from parallel transport, and separates solver refinement from finite-amplitude error. Control-affine increments remain exact at one common state; nonlinear trajectories do not acquire global superposition. LQR/Lyapunov conclusions now retain their hypotheses.

Part II derives variation of constants, two-time state transport, noncommuting transitions, input kernels as densities, endpoint derivatives as operators and adjoint gradients with a specified policy and objective. A numerical gradient check agrees with independently perturbed nonlinear rollouts. Impulse and work retain complete dynamics and trajectory dependence. The conclusion connects posture, force transmission, shaft deformation, feedback and impact timing through three distinct levels: immediate acceleration, infinitesimal future sensitivity and finite intervention. Empty prerequisite and code-index promises are replaced by concrete, scoped references.

Primary reading: Hunter's ODE notes, Sections 1.3–1.4, and Liberzon's calculus-of-variations notes, Section 1.3.2. Mathematical examples and numerical checks are independently derived; no empirical golf validation is claimed.

Validation: 20 new checks (14 independent mathematical checks and six source regressions); 4,006 full-suite passes, 29 skips, 129 deselected, 92.32% coverage. All 34 static contracts, 128 content checks/four skips and the 348-source syntax scan pass with the already merged valid-LaTeX-group validator temporarily applied and restored. Ruff, Black and code-quality checks pass. Final layout edits were followed by focused checks and fresh renders. All 159 display equations actually typeset; 743 math containers, zero math errors, no page overflow at 1440/390 pixels and visible headings. Fifteen desktop/mobile/light/dark target views were inspected and affected views refreshed after layout changes. Long foundation equations were split into readable lines; the concluding comparison uses paragraphs instead of a clipped table. No tracked print manuscript is affected.

Publication context: foundations #4191 is published; modal #4193 merged 56cd949c with deployment 33993093520 pending. Counterfactual #4195 at 60d35e42 awaits its remaining browser checks. This batch must be rebased from old counterfactual anchor bd088fd0 onto merged #4195. Part III #4194 has 17 independently passing mathematical prototype checks; source corrections are next. Full corpus #4021 and epic #4009 remain open.


## Tangent Reference Control, Discretization and Worked Cases

Issue #4194 completes the full Part III review. Together with #4192, the complete unified reference has been substantively read, corrected and mathematically checked; this does not certify its examples as empirical golf, orbital or flight controllers.

The optimal-control treatment now uses a consistent minimum Hamiltonian, retains the full dynamic residual in the multiplier variation, distinguishes normal/abnormal and endpoint/free-time conditions, checks a globally minimal double-integrator transfer, and derives correct box KKT signs and coupled quadratic projection. Squared input is distinguished from physical energy. Pure state-constraint and contact conditions retain their separate requirements.

The numerical treatment differentiates the actual held-input discrete transition, includes nominal motion/defects and timestep-scaled costs, separates local/global integration order from finite-amplitude variation error, and distinguishes physical instability from numerical instability. Checked examples reproduce exact double-integrator sampling, Euler/RK4 refinement and backward-Euler/trapezoidal fast-mode behavior. DDP and iLQR now share one quadratic cost convention; full DDP retains the value-gradient-weighted transition Hessians. A finite-difference Hessian check verifies the distinction. Prior corrected MPC stability, regularization and feasibility conditions are retained and integrated into the argument.

The spacecraft sketch is replaced by a complete exact-sampling finite-horizon LQR calculation. An independent condensed quadratic program agrees with its controls to about 1e-8 N, and continuous integration verifies the sampled transition. The published code was separately extracted and run: cost 1.22796775345, accumulated force magnitude 39.3975087381 N s and peak force 0.0936039374 N reproduce. Micrometre terminal position error coexists with nonzero terminal velocity; no successful docking is claimed. The unrelated 12.4 N s / 300 s propellant example is corrected to 0.00421483 kg. Arm placement, point-mass inertia, gravity, forward/inverse kinematics and power now agree. Quadrotor gravity/thrust signs, quaternion convention, 12-dimensional local errors, rotor-force units, hover input, rank-four actuation/rank-twelve controllability and horizon arithmetic are corrected. Invented iteration, settling and robustness results are removed.

Primary reading includes Liberzon's optimal-control notes (Section 3.4), SciPy's solve_ivp documentation, MIT's circular-reference relative-motion derivation, NASA's specific-impulse definition and Tassa/Mansard/Todorov's DDP paper (algorithm/derivative and control-limit sections). Examples and counterexamples are independently calculated.

Validation: 23 new checks (17 independent mathematical/numerical checks and six source regressions), with all six source guards failing before correction. Full suite: 4,029 passes, 29 skips, 129 deselected, 92.32% coverage; 47 combined tangent checks pass. All 34 static contracts, 128 content checks/four skips and the 348-source syntax scan pass; Ruff, Black and code-quality checks pass. The already merged valid-group validator was temporarily applied for static checks and restored. Final layout edits were followed by all 23 checks, syntax scan and a fresh render. All 165 displays actually typeset; 732 math containers, no math errors, no 1440/390 page overflow, visible headings. All 18 target screenshots were inspected, with nine affected views refreshed after layout changes. Wide mobile displays retain verified horizontal scrolling; oversized inline rotor/attitude expressions were converted to readable displays. No tracked print manuscript changes.

Publication context: modal #4193 is published. Counterfactual #4195 merged db5dc202 after every check passed; deployment33994648733 is pending. Tangent foundations PR #4196 is open at 64b296c6; this batch must be rebased from old foundations anchor a8e51c9c onto its merge. The next long impact/flight reference has received a full substantive read; non-aerodynamic findings are #4197 and aerodynamic/evidence findings #4198, both claimed, with primary verification under way and no source corrections yet. Full corpus #4021 and epic #4009 remain open.


## Coupled Impact, Rotation and Closure Evidence

Issue #4197 corrects all non-aerodynamic parts of the long impact/flight reference and completely rewrites the rotation-induced-spin and reference-point companions. The full long source was substantively read; Part V is preserved for the separately claimed aerodynamic issue #4198. It is not cleared by this batch.

The collision now conserves both bodies' linear/angular momenta through one positive-definite contact inverse-mass matrix. Normal and tangential impulses couple off centre. The scalar effective-mass and restitution formulas state their conditions; finite-head response, integrated friction, tangential compliance and passivity are checked separately. A counterexample shows that directional restitution bounds and an impulse friction cone alone do not guarantee passivity. The prescribed-surface 5/7 result is distinguished from fixed-centre and fixed-contact velocity experiments. The corrected finite-head example gives 343 rpm rather than a universal 205 rpm prediction. A complete published coupled calculation independently reproduces impulse, launch, spin, and energy: 68.6746 m/s forward launch, 152.9626 rad/s spin, and -15.2936 J kinetic-energy change. Its tangential sign reverses relative to isolated rotation because the normal impulse couples into the tangential response. This is an illustrative model, not a measured golfer.

Angle conventions now distinguish incidence from the surface and spin loft from the normal. The sliding-spin formula normalized by total speed uses sine, with a separately derived slip/grip threshold. Launch ratios are empirical projections, not causal force percentages. Wood's published intervals are correctly identified as 95% confidence intervals, including retained-shot counts and the coupled robot manipulation. The D-plane, three-component spin, finite contact and reference-point interpretations have explicit mechanical conditions. Spin and launch arise from the same contact law.

Closure now means a defined face-azimuth derivative, not total angular speed or handle twist. Exact rigid-body reference transport separates first-order direction and speed changes; the 120 mph example gives about -1.305 degrees and +0.0311 mph. Instantaneous screw-axis radius excludes axial velocity. Bias and dispersion are both possible, and contact-event timing enters covariance. Cheetham's observation of one captured drive versus seasonal accuracy is distinguished from causal intervention, population equivalence and a universal handle/head transfer ratio. Bibliographic errors in Henrikson, Leach and Williams/Sih are corrected.

Primary reading: Cross 2002 complete impact paper, Wood 2018 complete six-page study, Henrikson 2020 complete eight-page study, Cheetham 2014 selected methods and complete Study 1 results/discussion, USGA historical restitution notices, and publisher metadata for the cited measurement/impact papers. The main argument is supported by independent mechanics, including source discrepancies checked against conservation laws.

Validation: 24 focused checks (19 mathematical/numerical and five source checks); all five source checks failed before the rewrite. The full suite collected the first 19 new checks and passed 4,048 tests, 29 skipped, 129 deselected, 92.32% coverage. Five additional mathematical checks were then added and all 24 focused checks pass. All 34 static contracts, 128 content checks/four skips, the 348-source syntax scan, Ruff, Black and code-quality checks pass. Static checks use the already merged valid-group validator temporarily and restore it afterward. The published coupled example was separately extracted and executed with numerical assertions.

Web verification covers all three routes and all 73 display equations (43/15/15): final math counts 296/80/52, no MathJax errors or page overflow at 1440/390 pixels, visible headings. All 30 initial target views were inspected; 12 affected equation/example/table views were refreshed, followed by the final three compact-table views. The short launch tables now wrap within the article on mobile, instead of inheriting the wide-table minimum. No tracked print manuscript changes.

Publication: corrections through PR #4195 are published (18 batches). Tangent foundations #4196 merged 04e38261; deployment33996201945 remains pending at record time. Tangent control/cases #4199 is open at cefcdcab, pending final browser checks. This batch is based on old control anchor6c3f5d6f and must be rebased onto its protected merge. Aerodynamic #4198 remains open and primary verification is in progress, including the June 2026 ODS notice and Naruo's original equations. Epic #4009, full-corpus #4021 and route batches #4054–#4061 remain open.


Contact publication update: PR #4201 is open from content a3337f71, rebased onto control/cases merge fe332df6. All 89 combined contact/control/validator checks pass after rebase. Foundations deployment33996201945 succeeded (19 published batches); control deployment33997700775 is pending. Final PR checks and publication remain pending.


## Coupled Aerodynamic Flight and Evidence

Issue #4198 completes Part V of the long impact/flight reference after the full source read and the separate non-aerodynamic correction #4197. The rewrite connects air-relative force and torque to launch conditions, changing Reynolds/spin-ratio operating points, atmosphere, event-sensitive carry and correlated uncertainty. It distinguishes air-relative dissipation from ground-frame wind work and derives the spin-decay/path-length and still-air energy identities. Force-fit domains, zero-spin limits, torque reference length and unit conventions are explicit.

Primary reading includes Naruo/Mizota/Shimozono's complete seven-page 2004 original paper, with its equations visually checked; Choi/Jeon/Choi's complete four-page 2006 flow study; Lyu's complete 2018 and 2020 studies; McNally/Lambeth/Brekke's 2023 methods, results and comparison; and the complete 17 June 2026 USGA/R&A notice. The current USGA protocol's relevant section was verified through indexed primary text; direct full-PDF retrieval was unavailable and is not claimed. Beratlis's 2019 abstract and a primary patent filing provide explicitly limited support. The bibliography corrects Naruo's title, Lyu's article number, Leach's initials and McNally's year/title.

The original Naruo fits are reproduced with radius-based spin ratio and diameter-based torque. A complete published solve_ivp example launches at 40 m/s, 12 degrees and 2000 rpm and reaches the descending launch-height plane after 2.720636 s, carrying 85.4319 m with final spin 184.2220 rad/s. Its sampled Re range is 78,136–113,867 and S range 0.11179–0.14350. An independent force implementation and refined RK4 integrator reproduce the result to a few micrometres. The four-panel original figure shows trajectory, Re, spin and spin ratio. Adding 0.01 to drag coefficient throughout this example costs 1.1883 yards; finite perturbation demonstrates why a local yards-per-coefficient slope is not universal.

Evidence distinctions correct several material claims: local reattachment can occur below the delayed-separation regime; reverse Magnus occupies a bounded region and is not universal in long drives; production-ball differences do not isolate dimple geometry; sensor-propagated uncertainty differs from repeat-shot standard deviation; and inverse trajectory fitting does not uniquely identify density, coefficients or every mechanism. McNally's matched comparison is 6.63 versus 4.75 yards, approximately 28%, on a model-selection validation set. Wind and temperature jointly affect model inputs. The June 2026 notice supersedes the older implementation schedule and does not settle every future parameter.

Validation: all 22 new focused checks pass (18 mathematical/numerical, four source checks initially failing). Full suite: 4,075 passed, 29 skipped, 129 deselected, 92.32% coverage. All 34 static contracts, 128 content checks/four skips, 348-source syntax scan, Ruff, Black, code-quality and diff checks pass. The already merged valid-group validator was temporarily used for static checks and then restored. The published Python example was independently extracted and executed. The final source was read for consistency.

Web verification: all 63 display equations typeset; 254 math containers and zero math errors; no page overflow at 1440/390 pixels. All 18 equation/example/evidence views and three additional figure views were visually inspected, including dark mode. The standalone high-resolution figure was also inspected. No tracked print manuscript changes.

Publication state: twenty prior correction batches through #4199 are published; deployment33997700775 succeeded. Contact/closure PR #4201 merged as fb3df92f54940f0f19b20d5f34775f67d92a232b at 23:40 UTC, with deployment33999279815 pending. This batch must rebase from old contact anchor1c3ce27e697a3342110b4c9999cffc7e7810f19d onto that protected merge before its own PR. Energy-transfer article and bibliography were fully read and identified corrections are claimed in #4202; no correction is yet certified there. Epic #4009, full-corpus #4021 and route batches #4054–#4061 remain open.


Aerodynamic publication update: PR #4203 is open from content8473e5b6 rebased onto contact mergefb3df92f. All 88 combined aerodynamic/contact/validator checks pass. Final CI and contact deployment33999279815 remain pending.

## Energy Transfer, Mechanical Ledgers and Sequencing Evidence

Issue #4202 follows a complete substantive read of the long energy-transfer article and its companion bibliography. The revision distinguishes absolute club rate from relative wrist rate, supplies the COM/parallel-axis mass matrix and complete quadratic/gravity terms, and derives separate arm, club and joint-actuator work balances. Passive damping remains in the declared drift. Geometry-dependent inertial interaction is not an independently intervened acceleration, and alignment maximizes tip speed only with the appropriate angular-rate signs.

An independently checked unit-rod counterexample has arm acceleration −12/7 and absolute club acceleration +18/7 rad/s² while club kinetic energy decreases at 6/7 W: its rotational gain is outweighed by COM translational loss. A second state in the same undriven model transfers 1 W from arm to club while conserving total energy. Moving-pivot power, force normal to the achieved hand path, reference-point transport and a Galilean observer change are treated separately. The mechanical identities connect the investigation to preparation work, actuator limits, shaft response, delivery-event geometry and measurement rather than asserting a universal technique.

Primary verification includes the relevant methods/results of Nesbit/Serrano 2005, MacKenzie/McCourt/Champoux 2020, Miura 2001 and Kenny 2008; primary abstracts for Tinmark, Marsan, Anderson, Milburn and Joyce; the original Myers paper's bibliographic/abstract information; and White's undriven-model statement. This is not a claim that every cited paper was read in full. Nesbit's work percentages describe four selected model reconstructions and include elbows in upper-limb work. MacKenzie's association is not an intervention effect. Sequence depends on measured quantity; peak segment energies do not identify interface flux. The editable bibliography corrects authors, omitted authors, titles, venues and DOIs; the immutable upstream monograph is unchanged.

Pinned UpstreamDrift commit a1a613999eb0c744da96caa040941955eb210a21 was inspected through its archived model, experiment runner, mass parameters, work summaries and ground-atlas implementation. The latter's 1.72–2.00 quantity is a symmetric relative error, not a dissipated-work ratio; 30 J versus 2 J illustrates an error of 1.75 and ratio of 15. The registered failed match remains the headline. The article now distinguishes 55.1 J of shoulder work from stored arm energy, 131.7 J of force work from power, and relative wrist-actuator work from club-side moment work. Different selected 60/100 N m commands and program-dependent geometric delivery events are explicit.

Validation: 20 new checks (15 independent mathematical checks and five source checks initially failing); 37 combined energy/article checks pass. Full final suite: 4,095 passed, 29 skipped, 129 deselected, 92.32% coverage. Three initial publication-phrase regressions were restored with precise definitions before the final green run. All 34 static contracts, 128 content checks/four skips, 348-source syntax scan, Ruff, Black, CSS Prettier and diff checks pass. The already merged valid-group validator was used temporarily for static checks and restored. Generated date/format-only changes were restored after semantic comparison.

Web verification: 31 display equations and 196 math containers, zero math errors and no page overflow at 1440/390 pixels. All 27 main equation/evidence/figure/table views were inspected in desktop, mobile and dark mode, followed by the three refined parameter-table views. Article-local CSS removes compounded equation shrinking and keeps numeric parameter values together. Wide-equation horizontal scrolling is checked. No tracked print manuscript changed.

Publication state: twenty-one prior correction batches through #4201 are published. Aerodynamic #4203 merged 13ebbfcbe9039038e3caea1c4a2dba7f70cd5b4d at 00:23 UTC on 6 September, with deployment34001155414 pending. Rebase this energy-only batch from old aerodynamic content anchor f6587fb19814de5a0922ba2a3dc094fca9d18414 onto the protected merge before its PR. Both linear-algebra editions have been fully read and their identified corrections are claimed in #4204. Epic #4009, full-corpus #4021 and batches #4054–#4061 remain open.


Energy publication update: PR #4205 is open from rebased content b57e25242c6f39367e4b37225f05ac45ca89d0ae on aerodynamic merge13ebbfcbe9039038e3caea1c4a2dba7f70cd5b4d. All 79 combined energy/article/validator checks pass after rebase. Final CI and aerodynamic deployment34001155414 are pending. The final 390-pixel parameter-table refinement was also inspected; all 12 wide mechanics displays scroll horizontally at approximately 17.8-pixel font size, without page or table overflow.

## Linear Algebra as a Mechanical and Control-Theoretic Foundation

Issue #4215 completes the full print/web review begun in #4204. Both original sources were read in full, then reconciled against the independently merged #4214 at 3ccd69daa7c0d53ab31e7432c88fd846269a8ba2. Useful changes from that PR, including the expanded spatial inertia and figure contrast, are retained and corrected where necessary. The scope is the entire Volume 0 chapter 1, its matching Quarto edition, the rebuilt Volume 0 PDF, and focused independent mathematical checks. It does not certify the other chapters in that PDF.

The editorial sequence now follows what the model actually does: choose coordinates and feasible states; differentiate the configuration-to-task map; map velocities and dual forces; assemble kinetic energy; evolve perturbations; and interpret measurements. This connects the mathematics to golf and humanoid modeling without equating a convenient matrix diagnostic with a validated performance or neural-control claim.

### Coordinates, Rank and Evolution

The revision distinguishes an abstract vector from its coordinates, affine points from displacements, and bounded activation/trajectory sets from vector spaces. Forward kinematics is nonlinear; its tangent Jacobian is not a change of basis. Projection types, determinant orientation versus absolute volume scaling, nonzero cross-product directions and the identity tangent space of SO(3) are made explicit.

A full-row-rank 3-by-7 hand Jacobian has a four-dimensional kernel without being kinematically singular or proving underactuation. Singularity is a rank drop relative to the attainable maximum for the specified task. For two unit planar links at the straight posture, (1, -2) is initially task-null, but the straight joint path t(1, -2) sends the hand to (2 cos t, 0). An instantaneous null direction alone does not establish finite self-motion.

The double integrator supplies both temporal counterexamples. Its input matrix has rank one, but [B, AB] has rank two and its finite-time controllability Gramian has determinant T^4/12. A position-only measurement hides velocity instantaneously, but [C; CA] is full rank. These examples prevent the four-subspace table from confusing instantaneous mappings with controllability or observability over time.

### Spectra, Metrics and Inverse Sensitivity

The eigenvector diagram now uses one actual map, diag(2, 0.5), consistently for every arrow. A shear has eigenvalues 1, 1 but golden-ratio singular values: ellipse axes are singular directions, with absolute eigenvalues sufficient only for symmetric maps. Orthogonal SVD factors can reflect; a ball maps to a possibly degenerate ellipsoid. Real matrices may have complex eigenvectors, and planar quarter-turns have no real eigenline.

For A = [[-1, 10], [0, -1]], an initial (0, 1) grows to norm sqrt(101)/e, approximately 3.697, at one second despite asymptotic decay. Zero Jordan blocks distinguish bounded constant motion from unbounded double-integrator drift; the nonlinear systems xdot = plus/minus x^3 share the same zero linearization and opposite stability. Thus eigenvalues alone do not bound finite-time swing error, and instantaneous spectra of time-varying matrices are not stability certificates. A nonpositive Lyapunov derivative is not by itself a convergence proof, nor must a Lyapunov metric equal physical energy.

Joint and task metrics H and W define the normalized Jacobian W^(1/2) J H^(-1/2). Virtual work supplies tau = J^T F and the corresponding dual torque budget. Reciprocal static force axes require rank and budget assumptions; they do not imply unlimited force or structural strength. The Moore-Penrose inverse gives minimum-Euclidean-norm least squares, exact only for requested velocities in the image. For diag(1, 0.001), requesting (1, 1) produces (1, 1000); epsilon I has condition number one but inverse norm 1/epsilon. Damped least squares bounds each gain by 1/(2 lambda), accepting residual rather than establishing speed limits or closed-loop stability.

### Mechanical Matrices and Reproducible Examples

A nonsymmetric cross dyad is not a conservative spring: F = (0, x) does one unit of work around a counterclockwise unit square. Truss assembly must retain nodal connectivity through K = sum k B^T B, with B = [-n^T, n^T]. Inertia references are explicit. Spatial inertia maps the twist at the chosen origin to momentum; wrench additionally requires the momentum derivative and body-frame transport. Mass assembly sums matching Ji^T Isp_i Ji. Congruence transforms inertia and dual forces, whereas similarity transforms an endomorphism; energy and power survive coordinate rescaling even when raw inertia eigenvalues change.

Positive mass definiteness is tied to independent velocities with positive kinetic energy. Redundant coordinates, massless idealizations and constrained augmented systems need separate treatment. The MuJoCo factorization statement was checked against its official computation documentation: sparse L^T D L and back-substitution, without an unqualified linear complexity claim. The Kalman 1960 filtering paper is not presented as the invention of state representation; its full scanned paper was not independently read in this batch.

Published Python blocks were extracted and executed. The sample Jacobian has singular values 1.74703644, 1.12332941, 0.71832771 and condition number 2.43208832. The print inertia example reconstructs to approximately 4.92e-15 and satisfies the principal-moment triangle inequalities. The web torque example gives -5 N m and -10 W for the declared axis and rate, without an unsupported anatomical label. Upright-pendulum growth is explicitly local. The double-pendulum exercise declares absolute downward angles, point masses and massless links, with independently checked frequencies sqrt((2 plus/minus sqrt(2)) g/L).

### Verification and Publication Boundary

Twenty-five focused checks cover 21 independent mathematical properties and four source guards; the four guards failed before correction. The extracted examples, 34 static contracts, 128 content checks with four skips, 348-source syntax scan, Ruff, Black, CSS formatting and mypy on the configured 74-file scope pass. Full-suite and final print results are recorded in the following publication update.

The rebuilt book contains 301 pages; the entire revised chapter spans physical pages 17–46. All 30 chapter pages were visually inspected. The web edition has 29 display equations, 155 math containers and no math-rendering errors. Twenty-four targeted desktop, mobile and dark-mode views were inspected. Eight wide displays scroll at 17.78-pixel font size on a 390-pixel viewport; the full comparison table fits, with no page overflow. Local CSS corrects compounded mobile math shrinking and table minimum width. An inherited blocked external polyfill request is not counted as a new scientific or layout failure. Other chapters retain existing PDF warnings and remain subject to their own review.

Publication reconciliation: aerodynamic #4203 and energy #4205 are published, with deployments 34001155414 and 34042314215 successful. The partial linear correction #4214 is also published, deployment 34052864741 successful. This follow-up remains pending protected CI, merge and deployment. The next long source, Contraction_Tangent_CRITIC.qmd, was fully read; its mathematical and evidence defects are tracked and claimed in #4216. Epic #4009, full-corpus #4021 and route batches #4054–#4061 remain open.

Final local validation: 4,166 passed, 29 skipped, 129 deselected, 92.32% coverage in the default tests/benchmarks lane. An earlier tests-only run had two workspace-hygiene failures from our root browser-evidence directory; moving that temporary evidence into the QA directory resolved them. After the full run began, the final edits only named the unchanged gravity constant in a test and shortened a print proof heading. All 25 focused checks, Ruff, Black and the custom code-quality check pass again. The final PDF rebuild has zero overfull warnings in the revised chapter; the changed proof page was visually rechecked. Generated date/format-only outputs were restored after semantic comparison, along with our nested-render stylesheet side effect.

Linear-algebra publication update: PR #4217 is open from content afa108960e3c4f5dd8ca4869d13944b995c79fdf, based on published 3ccd69da. The push hook initially flagged HTTP/XML usage in four untracked historical QA helpers; those helpers now use requests with timeouts and defusedxml, and all hooks pass. No QA helper or security-policy change is included in the PR. Required CI, protected merge and deployment remain pending. The next critique draft and independent checks are prepared under QA for #4216 and will be moved onto a separate topic branch after this metadata push.


## Contraction Critique: Correcting the Review's Own Arguments

Issue #4216 follows a complete read of the original 11,633-word Contraction_Tangent_CRITIC.qmd. The rewrite preserves the useful investigation while replacing erroneous remedies and repetitive assertions with derivations, counterexamples and a testable golf-swing argument. Historical reference and critique revisions are pinned: withdrawn claims are not attributed to the already corrected current reference. The paired reference QMD/TEX receives only two stochastic differential-product spacing corrections in this batch; its earlier full review remains separately recorded.

### Riccati, Inverse Optimality and Metric Geometry

For continuous LQR, substitution uses the actual closed-loop A_c = A - BK and gives Sdot + A_c^T S + S A_c = -Q - K^T R K. A contraction rate additionally requires uniform positive metric bounds and a generalized decay inequality. The sharp pointwise rate is half the smallest eigenvalue of S^(-1/2)(Q + K^T R K)S^(-1/2); a lower bound uses lambda_min(Q + K^T R K)/(2 lambda_max(S)). Stabilizability/detectability are described as standard sufficient hypotheses rather than universal necessary conditions for every algebraic Riccati solution. The finite-horizon scalar xdot = 0, B = 0, Q = 1, terminal cost zero has S = T - t while x remains constant, exposing terminal metric degeneracy.

The old inverse-optimal counterexample actually admits positive running costs. For A = [[-2, 1], [0, -3]] and M_2 = diag(2, 1), Q_2 = [[8, -2], [-2, 6]] has eigenvalues 7 plus/minus sqrt(5). More generally an open-loop contracting M admits the constructed cost Q = -A^T M - M A + M B R^(-1) B^T M for any chosen B and positive R, but the resulting feedback changes the dynamics and does not identify a human objective. A valid prescribed-policy counterexample uses xdot = -x + u, K = -1/2 and M = 1: the loop is stable, but RK = B^T M requires an inadmissible negative R in the declared zero-cross-term cost class.

The material derivative follows the full implemented vector field, including spatial metric derivatives. Uniform scale bounds produce an overshoot factor, not a different exponential rate. The earlier xdot = -x, M = exp(2t) example has zero differential decay; xdot = x, M = exp(-4t) has declining metric length despite Euclidean growth and condition number one. Finite-distance conclusions need connecting curves within the certified domain and appropriate invariance. Under z = phi(x), A_z = Jdot J^(-1) + J A_x J^(-1) and M_z = J^(-T) M_x J^(-1); the complete residual transforms by congruence. The scalar phi(x) = x + x^3 example independently checks cancellation of the moving-coordinate terms. Raw eigenvalues need not survive congruence, whereas the metric inequality does. A metric is covariant (0,2); changing coordinates, changing a certificate and changing a controller have different physical meanings.

### Noise, Evidence and Optimization

For fully observed additive linear-quadratic diffusion, the HJB diffusion trace changes the scalar value term. In partial observation, estimator gains and the complete observation-to-action law depend on noise even when the LQR gain does not. Control-dependent diffusion D_i u contributes u^T sum(D_i^T S D_i) u and changes the optimization. Todorov/Jordan's actual model includes multiplicative noise, filtered commands and delayed observations; it is not reduced to the additive LQG claim or an identity between Gramian eigendirections and biological synergies. An Ornstein-Uhlenbeck variance floor separates common-noise synchronization from independent-trial repeatability. EMG factorization alone does not uniquely identify a neural objective or contraction metric, and mechanical confounds do not disprove neural synergies.

Replacement robot trials, standard deviations, p-values, warm-start counts, condition ranges and LQR-tree comparisons lacked traceable observations and are withdrawn. Missing records also do not prove that historical experiments were single-trial or unfairly tuned. The article specifies common physical outcomes, comparable resources, paired comparisons, participant-level independence and uncertainty. Wilson intervals for hypothetical 94/100 and 100/100 counts are approximately [0.875, 0.972] and [0.963, 1], explicitly mathematical illustrations rather than collected data. Force and impulse units, finite-horizon success versus a basin, and recovery time log(C/epsilon)/lambda are distinguished.

The quadratic penalty example min (u-1)^2 + mu max(u,0)^2 has minimizer 1/(1+mu), violating u <= 0 for every finite positive mu. It rejects the earlier exact-feasibility claim without inventing a replacement threshold. A legitimate neighborhood argument starts with a negative nominal residual margin eta and a verified Lipschitz bound L; radius below eta/L preserves the residual sign inside the verified domain. Metric bounds, feasible feedback, connecting paths and invariance remain additional requirements. Grid samples and local optimizer stationarity do not provide recursive MPC feasibility or a global basin certificate.

### Discrete Dynamics, Mechanical Energy and Impact

Discrete Riccati substitution yields A_c^T S_next A_c = S - Q - K^T R K. A squared-length factor rho gives a length rate -log(rho)/(2 Delta t); variable factors require a product and rho = 0 is a one-step case. Omitting a positive semidefinite term can give a weaker valid bound, correcting the old critique's contrary objection. Mechanical energy derivative -d v^2 is not strictly negative at every nonzero displacement, and a configuration-space inertia is not automatically a full-state metric. A critically damped Jordan example excludes an exact rate-one bound with finite prefactor for all time, while smaller rates can hold. Variable-inertia Coriolis cancellations require inspection rather than blanket rejection.

Hybrid contraction needs the event/reset sensitivity as well as flow inequalities. A scheduled xdot = -x system with reset multiplier two every 0.1 seconds grows because 2 exp(-0.1) exceeds one. State-triggered events require saltation rather than just the reset derivative; common flow metrics are sufficient in some formulations, not necessary for all stable hybrid systems. For a fixed terminal time, output sensitivity Dh Phi delta x connects geometric delivery, inertial response, feasible muscle/contact policies and perturbation evolution to the measured golf outcome. State-triggered impact changes that derivative. The article and lay explanation distinguish a proved model property from the empirical question of how a golfer coordinates movement.

### Sources, Validation and Remaining Work

Primary verification read relevant sections, not every paper in full: Manchester/Slotine arXiv1503.03144v3 setup, path assumptions, IV-B theorem/remark and appendix; MIT's finite-horizon LQR derivation; Todorov/Jordan 2002 Methods and multiplicative-noise/model passages; Kutch/Valero-Cuevas 2012 abstract, author summary and relevant confound discussion; and the short NIST Wilson-interval page. Numerical and symbolic examples were independently derived and tested. No fabricated experimental results replace withdrawn values.

Twenty-one new checks comprise 18 independent mathematical checks and three source guards; the guards failed before the rewrite. All 29 combined critique/reference checks pass. Full default tests/benchmarks: 4,187 passed, 29 skipped, 129 deselected, 59 warnings, 92.32% coverage. All 34 static contracts, 128 content checks/four skips, 348-source syntax scan, Ruff, Black, custom code-quality and CSS formatting pass. After the full run began, the final article-local mobile font correction and two paired stochastic spacing edits were followed by the 29 focused checks and final static/syntax checks. There are no production Python changes.

Root-site rendering verifies 31 display equations, 158 math containers and zero math errors. All 30 targeted desktop/mobile/dark critique views were visually inspected. Sixteen wide inline/display math containers scroll at 17.78 pixels on a 390-pixel viewport, without page overflow. The local CSS removes compounded mobile shrinking. The paired stochastic excerpt is separately checked in three modes. An inherited blocked external polyfill request is not reported as zero console errors. The paired reference TEX spacing is corrected; no tracked PDF is rebuilt. Its existing mobile display font remains smaller than the newly corrected critique font and is not certified as a full reference layout re-review.

Publication is pending. Linear-algebra PR #4217 has ordinary protected auto-merge enabled while its browser lane and aggregate quality gate finish; all completed checks, including eight textbook builds, are green. The next long source is Volume I applications: print lines 1–770 have been read and reveal candidate model/state-dimension, mass/coupling, drift, reachability, task-metric and numerical-example defects. The remaining print source, paired web source and executable model must be read before claiming a complete review. Epic #4009, full-corpus #4021 and route batches #4054–#4061 remain open.


Contraction-critique publication update: PR #4219 is open from content b5518477, replayed onto protected linear-algebra merge ec715e258127fd30ee0897a611890ac5ee524911. All 29 focused checks pass after replay, and all push hooks pass. Linear deployment34086327872 is running; critique CI, merge and deployment remain pending. The earlier unused remote critique branch retains its original content03ab0d96; no force push or branch-policy bypass was used.

Applications turnover: both full print/web sources, the entire golf teaching model and its tests, and the relevant numerical generator were read. All confirmed issues are tracked and claimed in #4218. A separate local checkpoint f377fc5c on fix/applications-model-rigor adds the missing shaft q-q kinetic-energy block, normalizes signed modal tip displacement, keeps modal springs symmetric and makes the six-state counterfactual explicitly rigid. Six new checks failed before correction; all 49 combined model checks pass afterward. This checkpoint is not a completed article correction: generator/text reconciliation, independent cross-domain examples, full validation and print/web QA remain. Its base is old critique03ab0d96; replay only f377fc5c onto the current critique branch before continuing. Preserve the pedagogical model boundary; no new empirical or upstream model authority is claimed.


## Applications: Make the Model, Intervention, and Evidence Agree

Issue #4218 follows the complete reading of the 11,584-word print chapter and 10,171-word web edition, the entire golf model and its 42 existing tests, and the relevant worked-example generator. The chapter's earlier numerical corrections had not reconciled its hand derivations, physical interpretations, or paired web text. The new treatment follows geometry, inertia, admissible forces, perturbation evolution, impact outputs, and measurement as separate but connected questions. This is a substantive rewrite of both complete editions, not a clearance based on search hits.

### Mechanical Derivation and Implementation Decisions

The rigid three-link mass matrix is derived both from Cartesian center-of-mass/angular velocities and six explicit independent entries. It includes all specified COM inertias, offsets, and cross terms. Relative joint rates are distinguished from absolute segment rates. Positive joint-space inertia is not lost merely because an endpoint Jacobian is singular. The original web matrix's indefiniteness was not a physical task singularity.

The flexible appendix example was missing the beam's contribution to the q-q block while retaining q-eta and eta-eta inertia. The correction integrates every block from one two-dimensional material-point velocity at zero deflection. It adds the shaft mass to the rigid carrier explicitly, without asserting that the manufactured carrier is calibrated anatomy or that a flexible-tip head mass has been modeled. The block units are kg m², kg m, and kg, so raw full-matrix eigenvalues are scale dependent. A light-carrier counterexample failed before the correction and now remains positive definite. Changing the shaft mass changes the distal inertia by the independently derived mL²/3 amount.

Both modal shapes now divide by their signed tip value. The old second mode had tip -1 while the documentation promised +1. Spring assembly is explicitly symmetric/diagonal; prescribed 40/120 rad/s frequencies do not identify one uniform EI, whose first-two frequency ratio is approximately6.267. Full rotating flexible dynamics would require modal states, potentials and consistent velocity forces; the inertia illustration is not advertised as that simulation.

Schur elimination retains the modal bias. With Gamma=-D^-1 C^T and H=(A-CD^-1C^T)^-1, qdd=H(Bu-hq+CD^-1 heta) and etadd=-D^-1 heta+Gamma qdd. This corrects both the duplicated mobility factor and the missing modal restoring term. The positive-Schur proof follows directly from the kinetic-energy quadratic form. The first-order state now has the correct ten components and derivative units.

The six-state zero-torque integrator intentionally uses the rigid mass and rigid bias together. It no longer mixes flexible effective inertia with rigid-only gravity/Coriolis forces. Its manufactured relative initial rates12,-18,-25rad/s correspond to absolute rates12,-6,-31, not anatomical maxima. Generated matrices, accelerations and trajectory outputs are reconciled in both editions. The table's final column is acceleration norm in rad/s², not a dimensionless drift-to-control ratio. The tip speed falls35.37→21.66m/s in0.1s while the acceleration norm increases; this does not imply an energy gain, a human performance result, or a recommendation to relax.

### Causal and Golf-Swing Interpretation

The acceleration ratio requires a declared projection/normalization and applied input. Zero input makes its denominator zero; an epsilon is a new definition. The full state drift norm mixes rates and accelerations, so it cannot equal an acceleration norm. Nonlinear coordinates add the acceleration connection term, quadratic velocity contributions can cancel, and a speed-dependent input/posture prevents a universal drift-dominance phase rule. Pointwise affine additivity is distinguished from a finite trajectory intervention. Sensory delay does not turn off ongoing activation or force.

Energy transfer requires segment force/moment powers and interval balances. Shared-point ideal-joint force powers cancel for the combined bodies while transferring energy between them; relative actuator power is distinct from each body's absolute rotational power. Proximal deceleration alone cannot establish distal energy transfer. The frozen modal energy derivative includes modal velocity, coupling, base acceleration and damping; base-acceleration sign alone does not establish shaft energy release. Coefficient variation and rotating effects require the full model.

The text explicitly separates shaft experiments with prescribed grip motion, prescribed applied forces, and an adapting golfer/controller. MacKenzie/Boucher2016 was read in full (main text, methods, results, discussion and references, not a visual audit of every PDF figure): two shafts,33men,14trials each, larger deflection-related speed contribution and lower grip angular velocity for the more flexible shaft, without a significant group total-speed difference. The chapter's concise account does not claim equivalence, universal individual response, or identification of a neural mechanism. Individual comparisons need multiplicity and repeatability consideration.

Fixed-time impact sensitivity is separated from event-time sensitivity, with the transverse-event denominator and explicitly time-dependent output term. Common post-impact time needs saltation/reset treatment. Weighted singular values require declared scales and represent local directional worst cases. State, parameter and cross covariance all enter output uncertainty. A currently invisible task direction can become important through trajectory evolution.

### Cross-Domain Examples and Withdrawn Claims

The original robot circle exceeded the0.9m workspace. A feasible0.05m circle centered at(0.45,0.15), fixed orientationpi/4, and an explicit continuous IK branch now replace it. A2x3 position Jacobian has a rank-two pullback at a regular pose; adding orientation changes the task. Epsilon regularization is a coordinate cost, not singularity avoidance or a contraction proof. A normalized computed-torque double integrator supplies an exactly verifiable Riccati example with six-state extension and actuator/model/chart limitations. The draft rate was itself corrected by independent calculation: generalized minimum sqrt3-1/sqrt2, metric length rate half that, rather than an incorrectly recalled value. Finite-horizon, sampled-metric and spectral-radius distinctions replace invented Riccati traces and accuracy tables.

CW orbital equations use declared radial/along-track/normal coordinates and the small-separation circular-chief approximation. At6.7e6m the DE440 Earth GM gives n0.001151215638rad/s and5457.870011s period, not geosynchronous motion. A900s quintic along-track approach1000→10m has explicit feedforward acceleration and ends at standoff, not docking. Quadratic acceleration effort is distinguished from thrust-based propellant use and hard constraints. Exact ZOH discretization is separated from continuous-time stability. Unsupported fuel savings and15percent validation claims are withdrawn.

The bicycle is exactly affine in acceleration and curvature; steering-angle linearization is local, or steering can be a state with rate input. Nonholonomic restrictions constrain velocities without reducing configuration dimension. A fixed-speed straight-line error law derives its gains from the desired polynomial; arbitrary speed changes add a vdot/v term, zero speed is singular, and tire/steering feasibility is not established by the kinematic equation. Unscaled drift norms do not support high-speed dominance claims.

A complete executable SciPy ARE/residual/rate example replaces broken library sketches. MATLAB dlqr's fifth argument is a cross-weight matrix, not the finite horizon. Calibration, verification and held-out validation remain distinct. Confidence-interval containment of a bound is not validation, and directional errors need not saturate a worst-case rate. Twelve exercises now require derivation, counterexamples and checks rather than confirmation of unsupported claims.

### Source Scope and Validation Turnover

Primary material verified: full MacKenzie/Boucher2016 article text, DOI10.1080/02640414.2016.1157262; MIT OCW16.346Lecture26 relative-motion PDF text; JPLDE440 Earth GM table/source; MIT LQR relevant double-integrator/finite-horizon derivations; MathWorks dlqr syntax/algorithm; Modern Robotics13.3.2part3 complete short transcript. MacKenzie/Sprigings2009 stiffness paper was abstract-only and is not cited for full-paper detail. A university beam page was read but its image formulas were not successfully inspected; the chapter instead presents an independently checked boundary-condition derivation. Timed-out sources are not represented as reviewed. UpstreamDrift research pin remains a1a613999eb0c744da96caa040941955eb210a21; no upstream model output is treated as experimental evidence.

The rigid-model correction invalidated the earlier golden ZTCF result by approximately 3.355e-4 rad. The old v1 fixture is preserved byte-for-byte as historical evidence; a new model-version-2.0 fixture uses the corrected rigid inertia and bias, unchanged registered tolerance, and a pinned source revision. The current adapter rejects the historical engine and unsupported source revisions, frames, internal-state inventories, or constraints. Five additional rejection cases protect these declarations. The standalone ZTCF article and paired Physics chapter link the new fixture and explicitly distinguish its interpretation. Those three excerpts are a dependency correction, not a complete audit of those sources. The normative record schema remains version 1 because its format has not changed.

The changed standalone article required regeneration of scientific-claim evidence digests. Only source/ledger digests changed: finding identities, scientific interpretations, review scope, and evidence status were not promoted. The first full run found the obsolete golden result; the second full run passed 4,212 tests, with 29 skipped and 129 deselected, and 92.36% coverage, but encountered the audit digest before its refresh. After refreshing that dependency, all 51 targeted audit/model/contract checks passed. This is full-suite execution plus a passing affected-dependency rerun, not a claim that the second full invocation exited green. Initial 63 model/derivation checks also passed. A convergence-test assumption was corrected: finite-difference force noise prevents indefinite fourth-order refinement, so coarse-step convergence and final-step absolute agreement are checked separately. At 400 steps the maximum relative rigid energy error is approximately 2.385e-11; this verifies the numerical implementation, not human validity.

Final validation: 34 static contracts, 128 content tests with four skips, a 348-source syntax scan, Ruff, Black, configured 74-file mypy, and individual model/contract mypy pass. All five worked-example fragments remain reproducible. Both affected books compile. All 12 revised applications pages (physical 140–151) were visually inspected, with final changed pages 142 and 148 reinspected after spacing/overflow fixes. The Physics dependency excerpt and adjacent pages 94–96 were inspected; inherited warnings elsewhere in that book are not a clearance of its remaining content.

The final applications render has 35 displays and 247 math containers with no math errors or page overflow. All 78 desktop/mobile/dark section views were inspected through nine contact sheets; 25 wide math containers scroll at 17.78 px at 390 px viewport width. All six affected ZTCF web excerpt views were inspected. The local published fixture URL returns HTTP 200 with a payload exactly matching the new source fixture. An inherited external-polyfill console error remains; no zero-console-error claim is made. Temporary QA and derivation scripts remain under the review directory and are not publication artifacts. Protected PR, merge, and deployment are still pending; update the turnover record after each event.


Applications publication checkpoint: critique #4219 deployment34088584702 succeeded. Applications commits replayed onto its protected merge4f9c10d0. Corrected model source pin is now9a809cbe947d4d80be39bf31b6feaa59b53192f1 in both adapter and v2 fixture; all12 contract tests pass after this metadata-only repin.


Applications PR #4221 is open from fix/applications-complete. All commit/pre-push hooks passed, including mypy, Bandit and unit tests. Protected CI/merge/deployment remain pending. Next issue #4220 records full substantive reads of Hybrid Tangent Spaces, its critique and accessible companion; claim expires08:39 UTC. Preliminary findings are in hybrid-partial-review.md, not a corrected-source clearance.


PR4221 CI follow-up: all eight textbook builds passed, but the custom code-quality job rejected one literal gravity value in the event-time regression test. It is now a named GRAVITY_M_S2 constant; numerical behavior is unchanged and all21 applications tests, Ruff and Black pass. The same quality checker passes over the tracked Python corpus using its normal exclusions. A naive recursive local run also scanned untracked historical QA scripts and reported111 issues there; those files are not in the PR or clean CI checkout, and no quality rule was weakened.


## Hybrid Tangent Spaces: Reconcile Mechanics, Timing, and the Critique

Issue #4220 follows sequential complete reads of the 11,543-word reference, 9,952-word critique and 4,607-word accessible companion. The correction preserves the learning question—how local sensitivity connects smooth motion and contact—while replacing unsupported results and erroneous proposed remedies. Original-word-count entries remain useful for documenting the reviewed scope; the revised texts are approximately 4,928/2,496/2,218 words before final layout edits. The shorter editions retain the substantive mathematical topics while removing duplicated arguments, fictional numerical studies, broken programs and administrative drafting instructions.

### Derivations and Model Decisions

The reference separates a trajectory's time derivative from a mode manifold's tangent space. It defines domains, admissible guards, direction, reset, competing events and continuation. First-order C 1 requirements are distinguished from second-order smoothness, transversality from spatial regularity, and finite differences from infinitesimal variations.

The saltation derivation uses one common-time comparison: expand the perturbed incoming event, apply the reset at its perturbed time, and subtract the nominal outgoing flow over that time offset. The result retains explicit guard/reset time dependence. Parameter effects are included without omitting parameter-dependent flows. The off-guard extension calculation proves that adding a multiple of the guard does not change the saltation. Curved guards and smooth velocity-dependent restitution are allowed; singular resets need not prevent a derivative. Grazing and competing event orders have separate limits. The critique's universal pseudoinverse repair and double-counted endpoint timing term are withdrawn.

Rigid impact is derived from impulse balance with an SPD mass matrix and a specified stationary frictionless contact. The normal projection is M-orthogonal, the velocity loss follows from that metric, and equality is admitted for elastic/tangent cases. The full configuration derivative acts on the incoming velocity. A translating-particle friction example includes the normal impulse's restitution factor, zero-tangent-speed convention and sticking cap, with explicit limits for rotating/multibody contact. Newton velocity restitution is not silently equated with Poisson impulse restitution. Multiple contacts require a physical rule and admissible impulses.

The two-mass golf example is fully reconciled: manufactured 0.200/0.046 kg,50 m/s,e 0.78 gives ball 72.3577235772 m/s, club 33.3577235772 m/s, loss 18.3065040650 J out of 250 J. A stipulated 0.5 ms duration implies average 6656.910569 N, not a measured peak. The loss uses reduced mass and preserves center-of-mass motion. Both incoming velocities affect both outgoing velocities. Effective contact mass, rotation, spin, shaft/grip bandwidth, face/launch-angle distinctions and aerodynamic flight bound the interpretation.

For the biped, five angles plus two floating-base translations are seven planar configuration coordinates in free flight; a pinned stance reduction is a different representation. A block impulse solve replaces an insufficient scalar angular-momentum equation. Sustained-contact forces retain Jdot-v curvature and require unilateral/friction feasibility. The critique's claim that Riccati recursion requires full row rank of B is corrected.

The ball saltation has eigenvalues magnitude 0.8 but largest singular value 3.704368967 under stated 1 m/1 m-per-second scaling. Dissipation along a trajectory is not contraction between trajectories. Full flow/jump propagation, input forcing after the last event, metric bounds, absolute determinant, transverse return-map stability and autonomous phase are distinguished. The critique's scaled-rotation counterexample was false: a common uniform norm bound contracts noncommuting products without any commutativity requirement. A strict-but-not-uniform scalar product tends to 1/2.

The Zeno example starts just after an upward bounce, derives finite accumulation time, and requires an explicit post-accumulation completion. Jump magnitudes do not alone equal total variation. Running integrability, impulsive cost sums, and state/sensitivity continuation are separate. Geometric upper bounds give upper bounds, not exact times. A per-impact unit cost diverges while passive energy losses are summable. Finite-step warnings are not proofs of infinitely many events, and more dissipation does not automatically eliminate Zeno.

Equal-coefficient Filippov friction includes mass and applied forcing. Pointwise changes at zero speed cannot create a larger static threshold under Filippov regularization. Attracting/repelling sliding are separated by directional signs; physical sliding contact is not automatically Filippov sliding. Complementarity products are smooth functions with a difficult feasible set; notation is not limited to LCPs and arbitrary LCPs are not promised polynomial-time solutions. Relaxation scaling, barrier sign, impulse/force distinction and contact feasibility are explicit.

The cost-state augmentation derives the boundary row including different running-cost values and time/state-dependent jump costs. Exact nonlinear second-order composition retains map curvature; an iLQR approximation is labeled accordingly. The continuous Riccati control term has the correct sign. Unsupported quadratic-convergence claims, iteration/runtime tables, company practices, and fictitious code/results are removed from both the reference and suggested critique remedies. Hessian regularization is not confused with modifying a physical saltation.

### Executable Evidence and Source Boundaries

The published NumPy example analytically locates one bounce and returns terminal state, initial-state Jacobian and restitution derivative. It rejects nonfinite inputs and unsupported pre-first/post-second event horizons. Every finite-difference rollout relocates its event. It is a manufactured verification example, not a hybrid DDP implementation or a validated golf simulator.

The initial regression run yielded seven independent mathematical passes, nine setup errors because the promised executable example was absent, and one failing source safeguard. After correction,25 combined numerical/derivation/link checks pass. These include three incoming-state cases, restitution sensitivity, unsupported branches, mass-metric impulse/energy, noncommuting contraction products, contact curvature, event-cost boundary terms, second-order chain rule, golf momentum/energy/average force, moving-floor sensitivity and reset-extension invariance. No numerical tolerance was weakened to fit a result.

Sources verified at bounded scope: Kong/Payne/Zhu/Johnson 2024 tutorial HTML (formal setup, saltation definition/derivation, flow composition, covariance/value discussion and limitations); Kong/Council/Johnson 2021 PDF (abstract, relevant hybrid backward-pass/mode-extension passages and experiment limitations); Posa/Cantu/Tedrake 2013 manuscript for the 2014 journal paper (abstract and contact/MPCC formulation passages); Cortes arXiv 0901.3583 PDF (solution distinctions, absolute continuity, Filippov definition/sign and friction examples); current official JAX custom_vjp API signature/example. The NSF copy and one arXiv HTML version timed out or failed; they are not represented as reviewed. These are not complete visual audits of the research PDFs. No unsupported theorem or numerical result is attributed to them. Most mechanical and sensitivity expressions are independently worked derivations checked with manufactured counterexamples.

Final full suite: 4,232 passed, 29 skipped, 129 deselected, 59 warnings; coverage rounds to 92.4%. The configured skips include absent optional Streamlit, obsolete helper tests and unavailable legacy entrypoints; the suite exited successfully. All 34 static CI contracts pass, along with 128 content-lint tests (four skips), configured mypy over 74 files, Ruff, Black and the custom quality check on the new test file. A mistaken content marker selected no tests and an incorrectly named link-test path selected no tests; neither invocation is counted as validation. The corrected content marker and actual tangent-series test path were used for the reported passes.

All three pages rendered with the root site configuration, restored afterward. The complete section review covered 36 reference, 19 critique and 16 accessible headings in desktop, 390-pixel mobile and dark mode: 213 views, inspected in all 25 contact sheets. The initial render contained 189/43/13 math containers and 33/4/0 displays respectively; exact counts are retained in hybrid-browser.log. Wide equations scrolled at readable font size with no document-width overflow or MathJax error. Visual inspection found two invalid equation-label prefixes rendered as literal text and an inherited white bibliography background in dark mode. The labels now use Quarto's eq- prefix, and article-local CSS follows the actual data-theme attribute. Both affected pages were rendered again. A stale cached stylesheet required revisiting the reference after the final CSS update; the final reference and critique bibliography backgrounds are transparent in dark mode. All 27 additional title, corrected-equation, bibliography, code-tail and comparison-table views were inspected in four final contact sheets. Inherited update-notice overlays and an external-polyfill console error remain visible in the local browser; this is not a zero-console-error assertion.

The last changes after the full numerical suite were equation-label syntax and local bibliography styling. The published code and numerical derivations were unchanged; the focused hybrid and tangent-series checks were rerun. No new PDF is part of this three-QMD batch. Formal route trust-ledger classifications remain deferred; the corpus inventory records the actual full-source review without promoting unrelated scientific evidence.

Applications PR #4221 protected-squash-merged as 21c7d89b9d5e30583b8ae6b48535aee100a0b457 after all checks, including E2E and every-route accessibility. Deployment 34095687458 is in progress at this checkpoint. Linear #4217 and contraction critique #4219 deployments succeeded; their inventory entries now reflect publication. Hybrid #4220 is ready for its protected PR but not yet published. Next source multibody-drift-control-v3.qmd was fully read and its findings registered as #4222, claimed through 09:41 UTC, with no content edits yet. The broader 405-source epic, corpus and route batches remain open.

Hybrid PR #4223 opened from fix/hybrid-tangent-rigor after replay onto the applications protected merge. Initial PR head f8ac56ac42000d2213e677519a1bae6e5b139c6b; all commit/pre-push hooks passed, including Bandit and unit checks. Protected auto-squash merge is enabled. CI, merge and deployment remain pending.


## Multibody V3: Motion, Loads, and Energy Under a Common Model (#4222)

The complete original 10,924-word article was read sequentially. Its central purpose is retained: investigate the same swing mechanism through complementary formulations, and make their connections useful to a reader. The replacement derives the quantities each formulation actually supplies, distinguishes implementation equivalence from physical validation, and connects joint loads, support, shaft motion, impact and flight without inferring causality from sequential segment-speed peaks.

### Derivation Decisions

- Declare independent local coordinates, the chosen zero-input channels and retained state/loads. Positive-definite inertia requires positive energy in every independent velocity; force covectors arise from virtual work. The potential gradient on the left has the opposite sign from the physical gravity force. A velocity-product term is not an energy source, and a rough surface does not increase inertia merely by increasing resistance.
- Preserve the two-point-mass relative-angle pendulum, deriving its mass matrix from Cartesian Jacobians. Check the determinant at straight and folded configurations. Derive Cartesian curvature accelerations separately from generalized acceleration; fast aligned motion can have zero generalized drift while requiring large inward Cartesian acceleration.
- Use the full controlled-trajectory linearization, including the nominal input multiplied by the derivative of its state-dependent input map. An explicitly calculated switching example has Hurwitz frozen matrices but an expanding two-step transition. Neither a drift-only Jacobian nor a frozen eigenvalue test supplies the missing trajectory conclusion.
- Derive the stationary bilateral constraint block solve and its Schur complement. The allowed acceleration set has a curvature offset and is a hyperplane only at rank one. The mass-metric projection acts on acceleration increments; the zero-input anchor retains normal acceleration. In the circle example, a radial force changes the reaction rather than the radial acceleration. Derive that reaction change, and distinguish stationary zero-work constraints from moving support and friction-limited unilateral contact.
- Separate inverse recursive Newton–Euler calculations from forward dynamics. Include COM moment arms and body-frame transport terms. Declare angular-first twists, actual downward home geometry, and the cross-position term that recovers endpoint velocity from a space twist. Write the body-COM adjoint matrix and its sign explicitly; test wrench and energy-rate identities.
- Derive task mobility and effective load from the complete joint-state equation. A given joint effort has a unique instantaneous task effect; the reverse redundant allocation is nonunique. The dynamically weighted null torque can affect later posture. At task rank loss, task inertia is undefined although joint inertia can remain positive definite, and Cartesian curvature acceleration can remain nonzero.
- Explain the kinetic metric, force raising and connection with their correct units. Under a nonlinear configuration chart, acceleration acquires a Hessian term; only same-state acceleration differences cancel it. Geodesic claims require removal of all nonconstraint forces. Dual-adjoint representation of one wrench and motion preserves their power pairing; a different moving observer generally measures different energy and power.
- Reconstruct both body force balances and signed force/moment powers. At an ideal joint, equal-and-opposite forces transfer energy between bodies while canceling in the total balance. An actuator moment contributes the relative-angular-velocity power. Summing the pendulum body equations recovers total actuator power, including nonzero torques. Segment angular slowing alone establishes neither total kinetic-energy loss nor its destination. Shaft strain energy, dissipation and boundary work must be included when the investigated mechanism depends on flexibility.
- Reject a universal bias-power quantity based on contracting the Coriolis/gravity bias with velocity. The kinetic-energy derivative also contains the mass-matrix derivative, whose velocity-product contribution cancels. Noether statements now require a variational symmetry and appropriate force conditions. The golf research section distinguishes net joint moments from unique muscle forces and instantaneous attribution from complete interventions through an impact event.

### Sources and Independent Checks

Primary reading was bounded to MIT Underactuated Robotics' manipulator/double-pendulum/constraint sections and Modern Robotics' complete short transcripts for inverse dynamics (8.3), forward dynamics (8.5), twists (3.3.2 part 2), rigid-body dynamics (8.2 part 2), and task dynamics (8.6). These support the stated formulations and conventions. This was not a full-book or video/figure audit. The redundant task-allocation and nonlinear-chart arguments were independently derived rather than attributed to the short invertible-Jacobian tutorial.

The published NumPy program constructs the declared pendulum and solves one stationary bilateral mode. The tests execute that exact program. Initial red evidence was four passing checks and eight missing-example setup errors. The first complete example passed all 12 cases; four additional checks brought the focused result to 16 passing cases. Independent comparisons use perturbed Cartesian geometry, projected inertial loads, per-body power, circle mechanics, a non-diagonal mass-metric projection, redundant torque allocation, a body-COM twist/wrench balance, controlled linearization and the expanding LTV transition. These are verification cases with manufactured parameters, not golfer measurements or integration-validation evidence.

### Rendering and Validation Checkpoint

The first browser inspection exposed unsupported inline math delimiters and a matrix row whose opening bracket was interpreted as optional spacing. All inline math was converted to the site's supported dollar delimiters, and the matrix row was disambiguated without changing its mathematics. A content run accidentally overlapped the temporary reduced Quarto render configuration and failed two configuration/digest assertions; after rendering restored the canonical configuration, all 128 content tests passed with four configured skips. No digest authority was promoted to excuse those failures. Configured mypy passed all 74 files. The initial 34 static CI contracts and focused Black, Ruff and custom code-quality checks passed.

The full regression suite passed: 4,248 passed, 29 skipped, 129 deselected, 59 warnings in 414.77 seconds, with coverage rounded to 92.4%. All 105 section views (35 headings in three modes) were inspected in 12 contact sheets. The corrected render has 210 math containers, 32 displays, no MathJax errors and no page-width overflow. All 28 wide equations tested at mobile width support horizontal scrolling at readable font size. Twelve additional title, adjoint, table and code-tail views were inspected in two contact sheets; the wide comparison table and code remain horizontally scrollable. The adjoint's right-hand expression was inspected after the earlier equation-scrolling pass. Inherited navigation overlays and an external-polyfill console error are not represented as a zero-console-error pass. No PDF belongs to this standalone QMD wave. Protected publication remains pending. The 405-source corpus, epic #4009 and route batch #4058 remain open.


### Hybrid Deployment Repair (#4225; Main-Health #4224)

Hybrid PR #4223 protected-squash-merged as d3873c9e94afcb7cf54fb88c648f0cf3539b48a9, but deployment 34100279949 failed the post-build health check before publication. The reference and accessible companion each linked to Hybrid_Tangent_CRITIC.html, although production excludes Advanced/*_CRITIC.qmd. The prior source-existence check and selected-page local render did not detect this production-route distinction. The updated editorial critique remains a corrected internal source, not a newly published public route.

Issue #4225 records the repair, and the automatically opened main-health #4224 is also claimed. The two links now accurately target the published critiques-and-responses index. The existing route regression now covers the hybrid reference and all excluded advanced critique filenames, preserving the production exclusions. The stronger check first failed on precisely the two offending links (five passes, one failure); after correction, all six link checks and 16 multibody checks passed together. Both changed public pages were rendered again. This repair changes navigation only; it does not promote any manuscript or scientific trust classification. Applications deployment 34095687458 succeeded and its inventory now records publication.

Final checkpoint: all 34 static CI contracts passed again after the production configuration was restored. Browser navigation from both repaired hybrid links reached the rendered Critiques & Responses index. The final focused 22 tests and both Python files' Black, Ruff and custom quality checks pass. No tests, builds or renders were running when this wave was staged.

Publication checkpoint: multibody and hybrid-route repair PR #4226 is open with protected auto-squash merge enabled at e67e159976bccd45d7ca81cec027f4ff3de8195e. Initial commit beee04c8 was replayed onto the protected hybrid merge before the ordinary first push; no force push was used. Both complete superposition chapter sources have now been read, and findings are registered as #4227. That chapter remains uncorrected and the full-corpus epic remains open.

## Complete Superposition Chapter: Mechanics, Trajectories, and Stability (#4227)

Both canonical sources were read completely before editing: Volume I chapter 3 (10,210 original words) and its web edition (9,717). The detailed findings were registered under #4227 and claimed by codex/session technical-review-20260906, with an initial expiry of 11:08 UTC on September 7. The work belongs to epic #4009, corpus #4021, and route #4055. The standalone superposition article corrected in an earlier wave is a separate source and was not replaced. The immutable upstream monograph remains untouched.

### Technical Decisions and Derivations

The replacement preserves a substantial mathematical treatment while removing duplicated sections and unsupported assertions. It distinguishes three operations: exact tangent-vector addition, exact fixed-state input increments, and first-order trajectory variations. The affine anchor is counted once; finite trajectory comparisons use a shared chart, initial state, and evolving zero-input baseline. Controlled linearization retains the sum of input-direction derivatives weighted by reference effort. Zero effort, voltage, and muscle excitation are separate interventions with different retained actuator states.

The kinetic metric is assembled from center-of-mass translation and rotation in compatible frames. Positive definiteness is conditional on regular independent coordinates and nondegenerate modeled inertia. The force-level Christoffel symbols are distinguished from their mass-raised acceleration-level counterparts. The drift retains external loads, potential, damping and velocity products. Geodesic language is restricted to the absence of nonconstraint forcing. Virtual work establishes the dual wrench map without asserting affine dependence on arbitrary commands.

All three mechanical formulations now use the same two uniform rods, fixed base, positive lengths/masses, COM inertias m l²/12, q1 counterclockwise from horizontal, and q2 relative. Derivation from Cartesian centers yields beta=m2 l1 l2/2, d=m2 l2²/3, a=m1 l1²/3+m2 l1²+d, M=[[a+2 beta cos(q2),d+beta cos(q2)],[d+beta cos(q2),d]], and c=beta sin(q2)[-2 v1 v2-v2²,v1²]. The potential gradient has cosine terms under this convention. The determinant is m1 m2 l1² l2²/9+m2² l1² l2²(1/3-cos²(q2)/4), strictly positive. The canonical C matrix has rows [-h v2,-h(v1+v2)] and [h v1,0], h=beta sin(q2).

Independent Newton–Euler checks differentiate the centers and recover elbow/base forces F12=m2(aC2-g), F01=m1(aC1-g)+F12. Moment balance gives tau2=Ic2(qdd1+qdd2)+rho2 cross F12 and tau1=Ic1 qdd1+tau2+rho1 cross F01+(l1 e1-rho1) cross F12. These recover three distinct applied torque vectors without using the mass-matrix formula to manufacture their expected values. The body-frame force equation includes omega cross v; the angular-first COM spatial equation uses I Vdot-ad(V)^T I V. Stacked free-body inversions are explicitly insufficient for a jointed chain. The kinematic and dual maps, bias accelerations and ideal-reaction cancellation are retained.

General input power is u^T B^T v. The positive identity v^T c=one-half v^T Mdot v cancels velocity-product power in the total mechanical energy derivative. Neither c nor gravity is incorrectly classified as dissipative loss. Shared-point joint force powers cancel globally while transferring energy between bodies; the actuator moment pair gives tau2 times relative elbow velocity. The golf interpretation requires segment boundary powers, external work and elastic storage before claiming proximal-to-distal transfer. Summing channel work along one trajectory does not superpose work changes across different interventions.

For stationary regular bilateral constraints, the chapter solves [M,-J^T;J,0][vdot,lambda]=[r+Bu,-kappa], with kappa=Jdot v. W=J M^-1 J^T and P=I-M^-1 J^T W^-1 J give the curvature-shifted affine acceleration and an input-dependent multiplier. The circle example makes the radial acceleration requirement explicit. Reduced coordinates retain M Ndot w. Moving boundaries can exchange power, nonholonomic distributions need not integrate to configuration charts, and unilateral/friction feasibility must hold before reusing a fixed contact mode.

The unit point-mass pendulum uses a separate, explicitly downward-vertical angle convention. At q=pi/6 its baseline is -4.905 rad/s²; torques 5,2.5,7.5 N m give 0.095,-2.405,2.595, verifying the baseline-corrected identity. The former tables contained accelerations inconsistent with -9.81 sin(q)+tau and an impossible gravity magnitude. They are replaced by computed RK4 tables. At t=2 s, q[5]=0.13355536, q[2.5]=0.00308791, q[7.5]=3.98101850 rad and their baseline-corrected defect is 3.84437524 rad. The +/-5 trajectories are exact odd-symmetry reflections and are explained as an exception, not a counterexample. Angles remain unwrapped; zero-reference relative errors are not assigned zero percent.

For the sampled inputs 5,-5,2.5,7.5 N m and times 0,.25,.5,1,1.5,2 s, steps .001 and .0005 s agree to below 2e-10, as does the state comparison with independent SciPy DOP853 (rtol=1e-12, atol=1e-13). The constant-torque invariant one-half v²+9.81(1-cos q)-tau q remains within 2e-10 J. These are numerical checks for the stated examples, not global solver guarantees. Both published table editions are parsed and compared against the runnable program to their six-decimal rounding tolerance.

Passivity uses a lower-bounded storage shifted to nonnegative and a declared power port. A bounded constant force on a free mass gives unbounded energy; damping velocity does not itself bound position. Passive feedback yields an energy inequality under well-posed compatible interconnection, not automatic asymptotic stability. Canonical port-Hamiltonian coordinates are (q,p), not silently (q,v). The scalar H=x^4/4, R=1 example has decay-to-energy ratio 4x² approaching zero, disproving the asserted uniform exponential bound from positive R alone. The damped pendulum's upright Jacobian has negative determinant and one unstable eigenvalue; its energy Hessian is indefinite. Contraction requires the full flow derivative of a uniformly positive metric and a quantified differential inequality on its domain.

State-dependent feasible inputs preserve frozen-state affinity. Independent channel bounds permit a Minkowski sum; coupled feasibility does not permit arbitrary independent maxima. The motor equations retain both torque and back-EMF constants, current dynamics, offset voltage-limited torque bounds, current-limit intersection and reflected rotor inertia. Sliding-friction nonsmoothness is distinguished from input nonlinearity; static friction is an interval reaction at rest. Lie brackets use Dg f-Df g and the correct mechanical block expression, including a configuration component. Driftless reversible Chow conditions are separated from accessibility and small-time controllability with drift. The x1dot=u,x2dot=x1² example has bracket rank but one-way physical motion. No bracket is presented as empirical proof of a golf sequencing mechanism.

Feedback linearization requires neighborhood relative-degree conditions, nonzero decoupling coefficient, a valid chart, feasible input and appropriate internal zero dynamics. The complete pendulum has no hidden internal subsystem for its angle output. A nonminimum-phase model does not imply every finite-horizon trajectory diverges. During final review, the phrase 'stable linear system' was tightened to an LTI closed-loop system with a Hurwitz matrix before asserting a quadratic contraction metric. Exercises now declare missing physical parameters and coordinate scales and ask for testable swing energy accounting.

### Sources and Reading Boundary

Primary sources used were the MIT pendulum model/energy/stability sections; complete short Modern Robotics transcripts for Lagrangian velocity products and actuation/gearing/friction; LaValle's complete short driftless and drift accessibility sections; and Khalil Lecture 12's storage/passivity definitions and pendulum example (PDF text read, rather than claiming a video or figure audit). The University of Michigan motor-model search extract provided its circuit, torque and back-EMF equations; a direct page open returned 403. Formulas and bounds were independently derived. Attempts to retrieve a complete Murray book timed out, and the separately inspected Khalil observer/state-feedback lecture openings did not provide the desired general relative-degree source; they are not cited as a complete reviewed reference. This chapter audit does not clear those external books, adjacent textbook chapters or human empirical validity.

### Validation and Publication Boundary

TDD began with one independent counterexample check passing and twelve setup errors because the reproducible chapter example was absent. Implementing the paired example made all thirteen pass. Added paired-table and first-kind Christoffel checks bring the focused suite to sixteen passes. Black at 100 columns and Ruff pass after removing a duplicated strip-character lint warning. The configured mypy lane passes all 74 files. All 34 read-only static CI contracts pass.

The root-selected Quarto render succeeds with 325 mathematical containers, 44 display equations and no MathJax errors or whole-document horizontal overflow. All 102 section views (34 headings, desktop/mobile/dark) were inspected in twelve contact sheets. Wide equations were tested for actual horizontal scrolling at a minimum 17.78px mobile math size. Fifteen further title, two-table, revised-feedback and code-tail views were inspected after the final wording changes. Both tables and the code block can scroll to their right edge. Inherited update notices/navigation overlays and the browser's recorded console messages are not represented as a zero-console-error result.

The full Volume I PDF rebuild succeeds at 169 pages. latexmk was unavailable because this Windows installation lacks Perl; repeated pdflatex passes completed instead, including the automatic index run. The changed chapter occupies PDF pages 53–66 (printed 37–50). All fourteen pages were visually inspected, including every equation, both tables, the complete code listing and all twelve exercises. The final changed table-description and feedback paragraphs were rebuilt and rechecked. No overfull box or undefined reference is attributed to this chapter in the final build. Existing warnings elsewhere in the book are left for their own full audits, not claimed resolved.

The first full run passed 4,263 tests and failed the existing literal-macro Coriolis notation guard. That guard required an obsolete expression rather than the explicitly defined vector/matrix relation. It now checks c, C, Cv=c and the positive energy identity in both editions; the independent Cartesian and Christoffel checks remain separate. All twenty focused checks pass. The final full suite passes 4,265 tests, with 29 skips, 129 deselections and 59 warnings in 342.21 seconds; coverage is 92.36%. Content checks pass 128 with four skips. Date-only generated registry changes were restored after verifying their semantic identity. No tests, builds or renders remained active when staging began. Protected publication remains pending.

Multibody PR #4226 merged through protected checks as 4230258ef0d08459ec73df1df4516adb71671d8d. Its associated deployment 34106420009 subsequently succeeded through build and publication, including the repaired hybrid links; main-health #4224 was closed against that evidence. Superposition PR #4231 is open after replaying only this wave onto that protected merge, with ordinary auto-squash merge enabled. All completed CI checks pass; browser CI remains in progress at this checkpoint. Both Physics of Golf glossary editions have now been read in full (10,020/9,603 original words). Findings are registered in #4232 and claimed until12:19UTC; canonical correction and publication are pending. No glossary source edits are part of the superposition wave.

Publication checkpoint: deployment 34106420009 completed successfully for merge4230258ef0d08459ec73df1df4516adb71671d8d, including build and deploy jobs. Multibody #4226 and the repaired public hybrid pages are now published. The superposition wave remains pending its own protected PR. The custom Python quality checker required the explicit GRAVITY_M_S2 name; after this test-only identifier change, both changed test files pass that checker, Ruff, and all twenty focused checks.

## Complete Physics of Golf Glossary (#4232)

The complete paired glossary was read before editing: 854 LaTeX lines/10,020 original words and 851 Quarto lines/9,603 original words. Issue #4232 registers six finding groups under epic #4009, corpus #4021 and route #4054. The codex/session technical-review-20260906 lease expires12:19UTC on September7. There were 422 original entries but only421 unique labels because Wrapping was duplicated. The corrected editions contain422 unique definitions:337 existing definitions revised, one Facet joint definition added, and84 definitions read and retained. This is a complete glossary audit, not a completed Physics textbook or corpus audit.

### Interpretation and Mechanical Derivations

The new introduction connects motion, energy and intervention questions through release and ground contact. A changing forearm-club angle is kinematics; shaft deformation may store energy; moment times its conjugate angular velocity gives net joint power. These do not uniquely identify muscle forces or neural commands. A stationary no-slip contact can change whole-body momentum while its contact-point power vanishes. Zero-input calculations retain the declared plant state, stored energy and specified loads, so they do not establish biological passivity.

For a declared rigid body and r from A to B, aB=aA+alpha cross r+omega cross(omega cross r). Angular acceleration and angular-speed-squared terms are distinct. The rotating-frame centrifugal term is minus omega cross(omega cross r); inertial circular acceleration is inward. Rotating-frame Coriolis force is -2m omega cross vrel. No frame term is an independent energy source. The mass matrix depends on configuration and modeled inertial properties; speed-dependent forces are not a universal growth of mass or loss of control authority.

Holonomic phi(q,t)=0 implies J qdot+phi_t=0 and a further differentiated acceleration condition. Constraint generalized force is J^T lambda; multiplier units depend on constraint scaling. Ideal stationary bilateral reactions have zero total power on admissible motion while transferring power between connected bodies. Moving supports, friction, deformation and mode changes need their own balance. Generalized momentum p=partial L/partial qdot obeys pdot=partial L/partial q+Qnc: a cyclic coordinate with no corresponding nonconservative force gives conservation, whereas zero applied force alone does not. Forward dynamics returns an instantaneous derivative; integration propagates it. Inverse dynamics does not uniquely identify muscle recruitment or make an arbitrary prescribed trajectory feasible.

The control definitions declare input port, state, chart, frame and constraints. At frozen state, feasible acceleration is a0+H U; direction, time horizon and coupled bounds matter. Previously qualified DCR, Natural motion, ZTCF family and ZVCF definitions are preserved. Normalized drift fraction uses comparable nonnegative magnitudes and a positive denominator; the both-zero case is undefined. It is not an invariant causal percentage of human effort.

The impact check assumes two translating bodies, one collision normal, negligible external impulse, a stationary ball and no rotational or tangential dynamics. With mc,mb>0 and incident club speed V>0, momentum gives mc V=mc vc+ +mb vb+, while restitution gives vb+-vc+=eV. Solving yields vb+=(1+e)mc V/(mc+mb) and vc+=(mc-e mb)V/(mc+mb). Substitution into one-half mc(vc+)²+one-half mb(vb+)²-one-half mc V² yields Delta T=-one-half mc mb(1-e²)V²/(mc+mb). Thus e<1 can coexist with ball speed greater than incident club speed. The total translational energy change is neither a ball-energy fraction nor a governing equipment test. The entry separately defines e=(vb+-vc+)/(vc--vb-) with signed normal velocities and a positive approach denominator. The former universal0.83 ball limit and83percent energy-transfer inference are removed.

Directional effective mass is the reciprocal of n^T J M^-1 J^T n when that mobility is positive; active constraints require the constrained mobility. A zero mobility does not yield a finite effective mass. Spatial power uses compatible angular-first wrench/twist ordering and matching origins/frames. Work, force power, moment power, segment-interface transfer and elastic storage are distinguished. Proximal speed peaks, lag angles and a straight shaft at impact do not prove energy transfer or optimal launch.

### Anatomy, Tissue, Equipment and Evidence

Wrist flexion/extension, radial/ulnar deviation and radioulnar forearm rotation are distinguished using local segment axes, grip and lead/trail conventions. Wrist cock often concerns lead radial deviation, but its measured forearm-club angle is not an elastic-energy meter. Contraction means force generation and includes shortening, isometric and lengthening modes; fascicle and whole musculotendon lengths need not change together. Zero joint work does not mean zero metabolic demand. Recruitment and rate coding are separate, and fiber categories do not dictate universal golf timing.

Pain threshold is not a tissue-damage threshold. Symptoms, signs and nociception are separated; swing appearance is not a diagnosis. Unsupported injury causation, universal muscle activation sequences and prevention guarantees are removed. The feedback example concerns approximately60ms goal-related responses in an object-manipulation experiment, explicitly not a golf measurement or universal correction deadline. Neural processing, EMG and mechanical actuation have different onset definitions.

The tissue definitions retain anisotropy, heterogeneity, loading history and viscoelasticity. A universal fascia-versus-tendon ranking, water/elastin percentage or dominant whole-body energy meridian is not inferred from specimen testing. Material modulus, structural stiffness and dynamic impedance differ. Beam EI curvature and bending energy require a declared beam model; tip stiffness3EI/L³ requires a uniform cantilever and small transverse tip loading. Modal frequency depends on stiffness, inertia, mode normalization and support conditions. A240–280CPM fixture result converts to4–4.67Hz without becoming a universal3–5Hz moving-swing mode.

Geometry definitions correct crossing tangents, signed tangential acceleration, the nonunique plane through a line and the difference between two-plane and two-dimensional motion. COM, COP and two-foot vertical loading are distinct measurements. Air-relative velocity matters to flight; ball compression is not COR; rough does not invariably increase spin; top-line appearance is not a launch law. Stroke is intentional forward club movement under the governing definition and need not be a full swing or strike the ball. The yard is exactly0.9144m.

Seven references were verified and added to the common bibliography. Carson, Richards and Mazuquin(2019), DOI10.1080/17461391.2018.1508504, was checked against the original accepted paper's title page and abstract for grip-dependent local wrist kinematics. Crevecoeur, Thonnard, Lefevre and Scott(2016), DOI10.1523/ENEURO.0129-15.2016, was checked through the original PubMed abstract, PMID27022624, for the object-manipulation result. Bonaldi and colleagues(2023), DOI10.3390/bioengineering10020226, was checked through the original publisher PDF: four fresh-frozen donors aged54–89 and uniaxial fascia-lata testing support specimen/protocol distinctions, not a full-golf causal mechanism. IASP Terminology supplies the revised2020 pain distinction. OpenStax Anatomy and Physiology2e section10.4 supplies the basic muscle-tension terminology; its pedagogical simplifications are not copied as universal biological laws. Official USGA Equipment Standards and What Is a Stroke resources support governing-protocol and stroke definitions. Some direct USGA requests returned403; the stroke definition was also available in the indexed official excerpt. The access year2026 does not assert a newly changed rule. External whole books and unrelated empirical claims are not cleared by these bounded readings.

### Integrity, Layout and Verification

Three new integrity cases initially failed on the duplicate Wrapping entries and edition math differences. The final checks require unique coverage of at least422 terms, resolving noncircular aliases, identical term sets, equations, citation keys and full prose after citation-syntax normalization. Kinetic Chain's self-reference and the missing Facet joint target are repaired; Normalized drift fraction is placed under N. Ruff, Black at100 columns and the custom Python quality checker pass. The configured mypy lane passes74 files. All34 read-only static contracts pass, and the content lane passes128 tests with four skips. The final full suite passes4268 tests,29 skips,129 deselections and59 warnings in393.08seconds; coverage92.36percent. A later focused command named a nonexistent legacy test path and collected nothing; that invocation is recorded as a command error, not a scientific regression. The corrected file selection was rerun. Generated date-only registry changes were restored after semantic comparison.

The Physics PDF was rebuilt through pdflatex, BibTeX and convergence passes; latexmk cannot run here because Perl is absent. The final book has582pages. All33 glossary pages, PDF531–563/printed503–535, were inspected in17 spreads. The seven new references were inspected on PDF564,565,566,568,570 and573. Scoped emergencystretch removed the glossary's overfull lines, including two initial protrusions exceeding24points. Final changed-chapter math and citations have no unresolved references or overfull lines. Existing other-chapter warnings and legacy bibliography annotations remain outside this review.

The root-selected web render succeeds. All90 section views covering30 headings in desktop1440px, mobile390px and dark1440px were inspected. There are85 MathJax containers and two displays, with no MathJax error or whole-document horizontal overflow. The original inherited math styling shrank a mobile display to12.8461px, so the glossary's scoped stylesheet retains17.78px math and horizontal scrolling. The two collision displays reach their true right edges,53px and158px respectively, in a269px viewport. Twelve further title, COR, wrist and reference views plus both equation right edges were captured. The first wrist locator selected an introductory mention and was corrected to the actual definition.

Final dark-mode inspection exposed an inherited white appendix behind light bibliography text. The page stylesheet now makes that appendix transparent. A first rerender appeared unchanged because the local service worker retained the prior CSS; clearing that local origin's service-worker cache and reloading confirmed the canonical stylesheet and readable references in all three modes. The final targeted views were visually inspected. Browser console messages and inherited site utility overlays are not represented as a zero-console-error result. The temporary QA helpers and image files are deliberately excluded from the commit.

Superposition PR4231 protected-squash-merged as e223e5e87e83b2f747af1e2f8475ac6e24235b4e at10:47:32UTC. Its deployment34113284485 was still in progress at this glossary checkpoint. The glossary is prepared on a child branch after378f7d1d; only its own commit must be replayed onto protected main before first push. No commit or push is permitted while tests, renders or builds are active. The full variational print chapter has now been read and reveals additional remainder, numerical, stability, QR and geometry defects; its paired web reading, issue, claim and correction remain pending.


## 2026-09-07 — Complete Variational Chapter (#4234)

### Scope, Purpose and Publication Boundary

Both entire canonical editions were read before correction: Volume I `chapters/ch02_variational.tex` (9,915 original words) and `quarto/ch02_variational.qmd` (9,414). This includes the proofs, examples, algorithms, summary and exercises. Issue #4234 records six finding groups under epic #4009, corpus #4021 and route #4055; its claim was checked and posted for codex/session technical-review-20260906 through13:11UTC. The chapter is now a coherent paired treatment with a shared 74-line executable example and eleven substantive exercises. Duplicate summaries and unsupported guarantees are replaced by derivations, counterexamples and stated domains. The immutable upstream monograph is untouched. This chapter review does not clear the remaining405-source corpus.

The organizing question is how a declared change in state or input changes a specified later golf outcome. A first variation is a model derivative, not a finite intervention or an observation of neural control. Earlier effort can establish velocity, activation and shaft states that shape later motion; later inputs can still change some impact variables. Constraint reactions can redirect acceleration with zero total mechanical work. These mechanisms enter the reference dynamics and output derivative. They cannot be inferred from one segment speed peak, nor does a locally insensitive output prove earlier muscular work was irrelevant. Feasible inputs, measured versus inferred states, nonlinear rollouts and competing mechanisms remain necessary.

### First Variation, Remainder and Propagation

For xdot=F(x,u)=f(x)+G(x)u, a feasible reference and bounded piecewise-continuous perturbation family xeps(t0)=xbar(t0)+eps xi0, ueps=ubar+eps v yield xidot=A xi+Bv. Here A=Df+sum_j ubar_j Dg_j and B=G along the reference. The controlled Jacobian term cannot be omitted or replaced by an undefined tensor contraction. Feedback instead differentiates the closed-loop field, adding G Dpi. Bounded derivatives on a common valid tube give fixed-finite-time O(eps²) error; input bounds may permit only one-sided perturbations. Contact-mode changes require separate treatment.

The exact integral Taylor remainder is integral_0^1(1-s)D²h(xbar+se)[e,e]ds, with no additional factor one-half. For F0=f+Gubar and finite state/input errors e,w, the exact forcing remainder is rho=integral_0^1(1-s)D²F0(xbar+se)[e,e]ds+[G(xbar+e)-G(xbar)]w. Thus norm(rho)<=H norm(e)²/2+LG norm(e)norm(w). For F=x²+(1+x)u the exact remainder is e²+ew. Input affinity removes a pure w² term at fixed state, not the mixed term. If z solves the linear equation with the same initial displacement and forcing, d=e-z satisfies ddot=A d+rho and d(t)=integral Phi(t,s)rho(s)ds. The old residual omitted A d. A uniform STM bound K gives K(t-t0)(H E²/2+LG EU); stability alone does not make K<=1. C² suffices for the stated quadratic bound; cubic Taylor error needs additional Hessian regularity. Neither that error nor first-order sensitivity proves quadratic DDP/SQP convergence or intrinsic manifold curvature.

STM composition and inversion follow from uniqueness. Jacobi's identity uses trace(Phi^-1 Phidot), not an inverse transpose, and is different from the rank-one determinant lemma. Liouville follows by integrating detdot=trace(A)det. The Peano-Baker series follows from repeated Volterra substitution, with later matrices on the left and kth term bounded by (L Delta)^k/k!. For nilpotent upper and lower generators A1,A2 on successive unit intervals, exp(A2)exp(A1)=[[1,1],[1,2]], while the reverse is [[2,1],[1,1]]. Ordinary exp(integral A) requires additional commutation conditions.

Variation of constants is exact for the variational equation. The output kernel C_T Phi(T,s)B(s), with units, state scales and chosen output declared, determines local influence. A stable scalar has larger terminal response to a later equal short input; double-integrator position has kernel T-s and velocity kernel1. Thus there is no universal earlier-is-more-influential rule. For a transverse event g(x,t)=0, deltaT=-gx xi/(gx F+gt), and delta h=hx xi+(hx F+ht)deltaT. Direct parameter dependence and resets add terms; grazing can invalidate differentiability. Fixed nominal-time sensitivity cannot silently stand in for impact-event sensitivity.

### Flow, Numerical Examples and Adjoint Boundaries

The held-input nonlinear flow retains the integral of F along its nonlinear trajectory. Continuous input affinity does not make this flow input-affine: xdot=xu gives varphi_h=x exp(uh). Its derivatives obey Xdot=AX, X0=I and Udot=AU+B, U0=0. Differentiating a numerical solver produces the derivative of that implemented map; fixed-step Runge-Kutta stage differentiation agrees with the correspondingly augmented stages. Exact flow and its discretization remain distinct. The block exponential exp(h[[A,B],[0,0]]) gives Ad and Bd even for singular A. For the double integrator, Ad=[[1,h],[0,1]], Bd=[h²/2,h]^T. An A^-1 formula needs invertibility and may be numerically unattractive.

The downward point-mass pendulum has the negative gravity sign, A=[[0,1],[-g/l,0]], B=[0,1/(ml²)]^T and analytic sine/cosine STM. The illustrative rounded g=10, m=l=1 is explicitly distinguished from9.81 elsewhere. Its period is1.986918s. At0.25s the STM is [[0.703441,0.224760],[-2.247601,0.703441]]; at0.50s it is [[-0.010342,0.316211],[-3.162109,-0.010342]]. Initial0.1rad gives [-0.001034,-0.316211] at0.5s. A unit N m s angular impulse at0.25s gives [0.224760,0.703441] at0.5s. The upper-right entry has seconds and lower-left reciprocal seconds; the state norm needs scales. Quarter period is approximately0.49673s. An ideal torque impulse demonstrates a linear response, not a feasible muscle command. Both displayed tables are generated from the chapter program and independently checked against a matrix exponential to rounding tolerance5.1e-7.

With parameter-dependent initial state and prescribed control, Sdot=Fx S+Fp+Fu up and S0=dx0/dp. For the remaining-cost column adjoint, -lambdadot=Fx^T lambda+Lx and lambdaT=psix. Differentiating lambda^T S and integrating gives dJ/dp=psip+lambda0^T S0+integral[Lp+lambda^T Fp+(Lu^T+lambda^T Fu)up]dt. This derivation fixes the web terminal-sign discrepancy and retains both missing boundary/input terms. A moving final time adds (psix^T F+L)Tp and direct psit Tp when present. Variable initial times and resets need their own terms. A maximum or minimum can be nonsmooth when active extrema change or tie.

The nondimensional xdot=px, x0=p example has x=p exp(pt), S=exp(pt)(1+pt), and gradient lambda0+integral lambda x. At p0.4,T0.7 the program returns1.329106595267083. Independent solve_ivp integration of the objective and a central1e-5 parameter difference agree to rel1e-8/abs1e-9. The lambda0 term is necessary. The discrete adjoint is derived separately from xnext=fk(x,p); reverse-mode automatic differentiation differentiates the actual graph and is not automatically a matching continuous adjoint or physical-model certificate.

### Stability, Authority, Frames and Numerical Geometry

The controlled divergence is div f+sum ubar_j div gj; feedback changes it. Liouville's coordinate-volume factor does not by itself describe a whole finite region with varying Jacobian or an arbitrary density. Negative trace shrinks volume but does not imply distance contraction: diag(1,-2) has determinant exp(-t) and a growing direction exp(t). Strictly negative trace without a uniform bound need not give a uniform exponential rate. A damped pendulum retains volume factor exp(-ct) even at its unstable upright equilibrium.

The metric derivative is xi^T(Pdot+A^T P+PA)xi. Exponential decay in a fixed state norm requires a uniformly positive and uniformly bounded metric with the stated matrix inequality; this lower-bound qualifier was made explicit in final proofreading. For a smooth LTV counterexample, D=[[-1,4],[0,-1]], Omega=[[0,1],[-1,0]], R=exp(Omega t), A=RDR^T has both frozen eigenvalues-1. The rotating frame has D-Omega=[[-1,3],[1,-1]], eigenvalues-1 plus/minus sqrt3, and a growing direction. Direct ODE propagation independently checks R exp((D-Omega)t). Nonnormal transient growth is a further distinct issue.

Bv is an increment in the derivative, not an instantaneous state displacement. A mechanically fully actuated d-DOF system commonly has2d states and rank-d B, so rank B<n is not a mechanical underactuation test. The finite-horizon Gramian is positive semidefinite by its integral-square representation and positive definite precisely for unrestricted LTV controllability. It does not certify bounded-input feasibility. For a double integrator, W=[[T³/3,T²/2],[T²/2,T]], det W=T⁴/12, yet position and velocity changes from rest are bounded by umax T²/2 and umax T. Input weighting and unit scales must be explicit. For full column rank, kappa2(B^T B)=kappa2(B)².

The moving frame xi=E eta yields etadot=(E^-1 A E-E^-1 Edot)eta+E^-1 Bv. Setting E=Phi eliminates the homogeneous coefficient only in that representation; E retains the physical growth and can be ill-conditioned. STM is the derivative of a specified flow, not generally Levi-Civita parallel transport. On a Euclidean line xdot=ax has flow derivative exp(a(t-s)), whereas Euclidean parallel transport preserves lengths. Coordinate transformations act at both endpoints.

The QR factorization Pk Qk=Qnext Sk gives Rnext=Sk Rk, with the new triangular factor on the left. The between-reset Ydot=AY solution is not continuously orthonormal. Accumulated R can still overflow, underflow or become ill-conditioned. Scaled factors and checkpointed local transitions manage computation without recovering information already lost. Stiffness is a stability restriction relative to the desired solution accuracy; it is different from conditioning. diag(-1,-1000) gives condition number exp(999t), while exp(-100t)I has condition number1 despite tiny entries. Absolute machine epsilon is not a dimensionally meaningful standalone singularity test.

Matrix-exponential recommendations now distinguish degree/scaling selection, backward error, conditioning and forward error; no fixed Padé7/8 rule guarantees spectral or unit-roundoff accuracy. Full dense STM propagation costs n² states and dense matrix multiplication, while selected directions, sparse products or an adjoint can change the cost. A numerical derivative can faithfully differentiate the wrong force law.

Symplecticity preserves the canonical two-form and volume, but volume alone is weaker above two dimensions. Implicit midpoint and Gauss Runge-Kutta methods are counterexamples to the blanket claim that Runge-Kutta is nonsymplectic. Kick-drift symplectic Euler for the unit oscillator has matrix [[1-h²,h],[-h,1]], determinant1 and trace2-h². For0<h<2 it preserves the positive quadratic half(q²+p²-hqp); at h>2 it has an eigenvalue beyond the unit circle. At h2 the Jordan structure also matters. Thus symplecticity is not an all-step/all-time energy guarantee, and a short downswing alone does not certify a coarse step. Independent swing restarts do not accumulate one continuous trajectory's integration error.

The body-to-space rotation equation is Rdot=R hat(omega_body), using right multiplication; space components use left multiplication. Frozen exponential updates are first order for general time-varying rates and preserve the group in exact arithmetic, with roundoff monitoring still relevant. The discrete Euler-Lagrange equation is derived from fixed-endpoint action. Regularity, discrete Legendre transforms, retained symmetry and forcing qualifications govern local solvability, symplecticity and momentum claims. A generic position/velocity Jacobian is not thereby canonically symplectic.

### Primary Sources and Verification Limits

Baake and Schlaegel, The Peano-Baker Series, https://arxiv.org/abs/1011.1775 and its PDF, supplied the ordered-series, convergence, composition and determinant cross-check. The accessed PDF is version3 dated20July2025; all four technical sections were read, not represented as a complete bibliography audit. The2011 journal metadata was separately verified. Al-Mohy and Higham2009, DOI10.1137/09074721X, and the Manchester author PDF https://eprints.maths.manchester.ac.uk/1217/01/covered/MIMS_ep2009_9.pdf supplied the abstract/introduction and overscaling example; the entire22-page paper was not read. These support the bounded algorithm statements, not a blanket optimal-cost claim.

Hairer's geometric-integration lecture2, https://www.unige.ch/~hairer/poly_geoint/week2.pdf, was read for the basic methods, symplectic Runge-Kutta proofs and variational-integrator sections. The whole18-page lecture and figures were not audited. West's https://lagrangeweb.web.engr.illinois.edu/var_int/ textual account was read for variational/symmetry qualifications; its figures were not inspected. MIT's https://underactuated.mit.edu/lqr.html trajectory-linearization sections were checked for feasible-reference feedback interpretation; the whole book was not audited. The counterexamples, boundary terms and golf interpretation above were independently derived, not imported as extended quotations. SciPy's official expm documentation was opened as an implementation cross-check but is not claimed as a full documentation review. Canonical links use these primary sources; no new BibTeX entries were required.

### Regression, Rendering and Publication Checkpoint

The initial TDD run had one pass and ten setup errors because the required shared example was absent. After implementation the first eleven focused cases passed; adding a smooth LTV check and independent tests of both published tables brought the set to fourteen, all passing. Ruff's E731 finding in an initial test draft was repaired with a typed local function. Ruff, Black at100 columns and the custom Python checker pass. All34 static contracts,128 content tests with four skips, and configured mypy over74 source files pass. The616-source title audit passes. The full suite passes4,282 tests,29 skips,129 deselections and59 warnings in592.39seconds, with92.36percent coverage. Generated date-only artifacts were restored after semantic comparison. The final two prose-only input/metric qualifiers were regenerated in both editions, rebuilt, inspected and checked with the focused fourteen tests; they did not justify another full-suite run.

The root-configured Quarto render succeeds. The rebuilt Volume I PDF has161 pages; the complete changed chapter is PDF31-44, printed17-30. All fourteen pages were inspected in seven spreads, including both tables, the complete program and all exercises. Final qualifier pages31 and38 were reinspected after rebuilding. No unresolved math references or overfull lines occur in this chapter. Other-chapter float and legacy warnings and the MiKTeX update notice remain outside this content correction.

The browser review inspected117 section views: all39 headings in desktop1440px, mobile390px and dark1440px. There are293 MathJax containers and51 displays, with no MathJax error or whole-document overflow in those checks. The scoped canonical stylesheet keeps mobile math at17.78px;34 overwide equations were verified to scroll inside269px containers. Additional title, both tables, code-end and final qualifier views were captured in all three modes. An initial right-edge test selected an equation that already fit and correctly reported no horizontal scroller; the locator was changed to the visibly overwide first equation. This was a QA-target error, not a page defect. Inherited console messages and utility overlays are not described as zero-console-error results. Temporary authoring and QA files remain untracked.

All eighteen targeted views and three mobile right-edge views were visually inspected. The table, program and first display reached their actual right edges at445px,590px and132px respectively. The target helper's scroll-into-view operation can also shift a wide table horizontally; full-section captures retain the initial left columns, and the right-edge checks establish access to the last columns. No page-width overflow or MathJax error occurred in the final three-mode check.

Superposition PR #4231 deployment34113284485 has now succeeded. Glossary PR #4233 protected-squash-merged as954d3975fcb777d09582fc0140c44c54b1e87a06 with all checks green; deployment34117093101 was still running at this checkpoint. Variational work is on its child branch after ab5ac555e019efdb9d103dfbe604198ee167e72d. Replay only this wave onto protected main before the first push, after every local test/render/build is finished. No variational PR or publication is claimed yet. The next unreviewed long paired chapter is duality.


Publication update: glossary PR #4233 deployment34117093101 succeeded through publication. Variational commit d1428436 was replayed alone onto that protected merge as0682ae24, then pushed after all local checks finished; all push hooks passed, including pytest. A first rebase attempt stopped on an unstaged AGENT_HANDOFF.md status with no textual diff; refreshing that file in the index removed the normalization-only status and the rebase succeeded. PR #4235 is open with protected auto-squash enabled. Both complete duality editions and the existing generator/LQR implementation are now read; issue #4236 records the next correction scope. No duality canonical changes are part of PR #4235.


## 2026-09-07 — Complete Stability–Optimality Duality Review (#4236)

### Scope and Mathematical Purpose

Both entire ch06_duality sources were read: Volume_I/chapters/ch06_duality.tex, 9,763 original words, and quarto/ch06_duality.qmd, 9,458. Every proof, numerical example, geometric/biomechanical interpretation, summary and exercise is included. The complete src/affine_control/lqr.py and the ch06/build sections of scripts/generate_worked_examples.py were read. Issue #4236 records five finding groups under epic #4009, corpus #4021 and batch #4055. The codex/session technical-review-20260906 claim was checked free and posted through14:06:33UTC. The immutable proximal-distal monograph is unchanged. In the current compiled Volume I this source is numbered Chapter7 because ch03b is an additional numbered chapter; ch06 is the source identifier.

The revised argument is that a particular quadratic optimal-control value can also certify stability under declared assumptions. It is not a proof that a golfer minimizes that criterion, that the criterion is physical energy, or that the optimal state curve is a geodesic. The golf sections connect retained mechanical/actuator states, feasible references, impact output derivatives, remaining control authority, measurements, noise, delays, contact modes and finite interventions. Multiple kinematic trajectories can realize an acceptable task outcome; inverse identification of an objective requires discriminating observations. Effort penalties, work, metabolic expenditure and tissue loading remain distinct quantities.

### Value Identities, Rates and Finite Horizons

For continuous LQR, Q>=0, R>0, stabilizability of (A,B) and detectability of (Q^(1/2),A) give the stabilizing semidefinite Riccati value. Positive definiteness requires more; Q>0 is a convenient sufficient condition here. Stable unpenalized directions can leave S singular. Differentiating V=x^T Sx and substituting the CARE gives stage+Vdot=(u+Kx)^T R(u+Kx). Integration retains -V(x(T)); the infinite-horizon optimality argument needs admissibility and a legitimate terminal limit. On feedback, Ac^T S+SAc=-(Q+K^T RK)=-D. D semidefinite only gives nonincrease without a further invariance/detectability argument.

The sharp quadratic value rate from this inequality is beta=lambda_min(S^(-1/2) D S^(-1/2)). The S-norm rate is beta/2. Euclidean conversion gives sqrt(kappa(S)) exp(-beta t/2). The simpler lower bound lambda_min(D)/lambda_max(S) uses the maximum, not minimum, eigenvalue of S. The scalar A=B=Q=R=1 example gives S=1+sqrt2, state rate sqrt2, value rate2sqrt2 and state multiplier0.37159545847 at t0.7. It directly falsifies the prior factor-of-two state-rate claim.

For discrete LQR, H=R+B^T SB and K=H^-1 B^T SA. Expanding stage+Vnext-V gives (u+Kx)^T H(u+Kx), so Ac^T SAc-S=-Q-K^T RK. This derives the missing effort term rather than restoring it by assertion. The DARE is distinct from the continuous ARE. With delta=lambda_min(S^-1/2 D S^-1/2), S-D=Ac^T SAc>=0 implies0<delta<=1; rho=1-delta gives V_k<=rho^k V0 and norm<=sqrt(kappa(S)) rho^(k/2) norm0. At rho0, Ac0 produces one-step termination. Scalar A1.2,B=Q=R1 yields state multiplier0.40647187995 and value multiplier0.16521938919, independently checked.

Finite-horizon DRE includes Sdot and the terminal boundary; discrete feedback uses S_(k+1). Zero terminal weight makes S(T) singular and a terminal value cannot bound error there. Infinite-time or receding-horizon stability does not follow from finite value decrease. Uniform metric bounds and nonlinear remainder/constraint control are explicit. A homogeneous quadratic deviation objective is distinguished from expanding a general cost about a nonoptimal feasible trajectory, which can need linear value terms and feedforward correction. For impact H(x), C_T^T W C_T is the squared-linearized-output penalty. At nonzero nominal target residual it is only the Gauss–Newton part of the nonlinear terminal half-Hessian; the weighted second-derivative residual terms are stated. Event-time and reset sensitivities must be included for moving contact events.

### Shared Numerical Authority and Independent Checks

The original print already used the #3518 numerical generator; web still contained its superseded incorrect S/K and invented table, including a feedback actually having spectral radius1.043 while described as stable. The web now includes a generated Quarto partial registered in the same build() dictionary as the existing print macros. Existing print remains the first dictionary item to preserve its test contract. The new renderer reuses the existing residual validation, then separates matrix presentation from trajectory-table formatting. No solver algorithm or historic numerical model was changed.

The historical A=[[1,.1],[.981,1]], B=[[.005],[.1]] mixes first-order drift and second-order input position response. Both editions now declare an illustrative discrete pendulum-like model, not exact ZOH dynamics or a validated finite pendulum intervention. A future physical model must derive A/B together under one hold/discretization assumption. Normalized angle/rate/input directions require scales; gain components with different units cannot be ranked by raw magnitude. Scaling x=Tz,u=Uv gives Az=T^-1 AT,Bz=T^-1 BU,Qz=T^T QT,Rz=U^T RU,Sz=T^T ST,Kz=U^-1 KT.

Checked matrices are S=[[94.9386,20.7112],[20.7112,7.3663]], K=[17.1287,5.5643], Ac=[[.9144,.0722],[-.7319,.4436]], and D=[[39.3393,9.5310],[9.5310,4.0962]]. The unrounded residual bound is1e-10, closed-loop eigenvalues.6281,.7298, kappa36.6808, conservative rho.9831, norm prefactor6.0565. At step10 actual V10/V0=.0043 versus bound.8432. The actual one-step ratio and cumulative bound columns are distinguished. Initial Euclidean norm growth to1.2654 is compatible with monotone value decrease and nonnormal stable dynamics. The separate scipy.linalg.solve_discrete_are solution checks every displayed S/K entry and independently propagates all eleven value/norm/ratio rows with rounding tolerance5.1e-5.

### Geometry and Robustness

The Hessian of x^T Sx is2S. S/2S differ only by a declared constant metric normalization, but a nonlinear Hessian is neither automatically positive nor a tensor: coordinate transformation adds gradient(V) times the second derivative of the chart map. At a stationary point that extra term vanishes. A covariant Hessian requires a connection and positivity. A metric along one reference is not automatically a regional metric. TM is the disjoint union of tangent spaces, locally a product; a global product identification needs additional structure.

The fully actuated counterexample A=diag(-1,-2),B=I,Q=diag(3,12),R=I has S=diag(1,2), closed loop diag(-2,-4). Starting from(1,1), its exact optimal curve (exp(-2t),exp(-4t)) satisfies x2=x1² and is not a geodesic of constant S. Initial velocity(-2,-4) and acceleration(4,16) have determinant-16, excluding even straight-line reparameterization. Constant-metric geodesics are straight; geodesics generally need not globally minimize distance or attract dynamics. Ellipse axes follow S, not generally Ac. Contraction requires the full metric material derivative and symmetric Jacobian inequality throughout a region with uniform bounds and path assumptions.

For A=-I2,B=0,Q=I2,R1, the plant is entirely uncontrollable but S=.5I and kappa1. State scaling can arbitrarily worsen kappa without altering physical authority. Weak input to an unstable penalized state can instead make value diverge. Increasing Q need not improve conditioning, and larger S does not imply a larger region of attraction. Finite-horizon controllability/output-reachability should use declared units, inputs and constraints.

For common scalar gain eta, the correct identity is (A-eta BK)^T S+S(A-eta BK)=-Q-(2eta-1)K^T RK. Q>0 makes eta>=.5 sufficient in this stronger setting. Boundary and semidefinite-Q cases need their own assumptions. The scalar plant is actually stable for eta>sqrt2-1≈.414214, so the guarantee is not necessary. Lower eta is weaker feedback. An arbitrary norm-ball DeltaK bound is not equivalent; additive E requires E^T S+SE<D, with sufficient2||S||||E||<lambda_min(D), E=-B DeltaK.

The SISO derivation sets z=(jwI-A)^-1 b,L=kz,R=r and uses Az=jwz-b to obtain r(|1+L|²-1)=z*Qz>=0. This is the Nyquist exclusion of the open unit disk centered-1, not a closed-loop pole disk. At a unity-gain crossing it gives the familiar60-degree separation from the critical negative-real direction under the stated negative-feedback single-loop assumptions. Multiple uncertainty channels and general structured margins need different analysis. Saturating u=-(1+sqrt2)x at |u|1 in xdot=x+u yields xdot1 at x2, while the unconstrained derivative is-2sqrt2. The implemented controller/observer/actuator combination must be analyzed; full-state LQR margins are not inherited by general LQG.

### Dissipation, H-Infinity and Estimation

Dissipativity is defined relative to a storage and supply rate. It is not an unconditional iff stability statement and cost is not mechanical stored energy. For normalized xdot=Ax+Bu+Ew,z=(Q^.5x,R^.5u), the game ARE is A^T S+SA+Q-SBR^-1 B^T S+gamma^-2 SEE^T S=0. Existence, the stabilizing game branch and admissibility are needed, not just any positive root. Under u=-R^-1 B^T Sx, completion yields Vdot+z^Tz-gamma²w^Tw=-gamma²||w-gamma^-2 E^T Sx||². Integration retains the initial/terminal storage. A zero-initial induced L2-gain certificate does not force Vdot<0 under every disturbance. For xdot=-x+u+w,z=(x,u),gamma2, S=(-2+sqrt7)/1.5, x1,w3, storage rate1.35134149 is positive while the supply balance is-33.46332751. The smallest attenuation is generally an infimum, not necessarily an attained minimum; unrelated energy criteria are not simultaneously optimized. General feedthrough/output-feedback H-infinity formulas are outside this normalized example.

Kalman–Bucy dynamics are written as SDEs with independent Brownian noises, Gaussian initial state and continuous noise intensities. Covariance Pdot=AP+PA^T+Qw-PC^T Rv^-1 CP runs forward; control Riccati duality for time-varying coefficients needs time reversal and transformed boundary/coefficient histories. With M=P^-1 and Ao=A-PC^T Rv^-1 C, differentiating the inverse gives Mdot+Ao^T M+MAo=-M Qw M-C^T Rv^-1 C. Covariance and precision play different roles; homogeneous estimate forgetting is distinct from nonzero uncertainty under continuing noise. Uniform convergence still needs positivity/bounds and strictness or observability assumptions.

For observer F(xhat,u)+L(xhat,t)(y-h(xhat)), the full held-y/u Jacobian is DxF-LDh+DL[.]innovation; DxF=Df+DG[.]u for the affine-input plant. The scalar F=x²+(1+x)u,h=x,L=x² at(.3,.7,1.2) has Jacobian1.75, checked by independent central differences with step1e-5. Neither transposing a generic nonlinear control law nor an EKF covariance automatically gives regional contraction. Conditional EKF convergence is distinguished from an unconditional guarantee; DDP contains second-order dynamics beyond a linearized Riccati step. The observation Gramian includes sensor/noise weighting and state scaling; muscle attachments are not independent sensors, and no universal shoulder/wrist observability ranking is asserted.

For e=x-xhat the linear augmented matrix is [[A-BK,BK],[0,A-LC]], so its nominal poles are the union. The equal-rate cascade [[-1,1],[0,-1]] has transition exp(-t)[[1,t],[0,1]], which precludes C exp(-t) with finite constantC for all times, despite stability. Any strictly slower rate can be bounded. General nonlinear or two-way coupling requires an appropriate cascade/small-gain argument; separate stable blocks alone are insufficient. The golf experiment exercise asks for actuator port, retained states, sensors, noise scales, impact outputs and contact assumptions, and for evidence distinguishing an objective change from mechanics.

### Primary Sources and Limits of Review

Tedrake's https://underactuated.mit.edu/lqr.html was read for basic HJB/LQR, local trajectory, finite-horizon and discrete derivations; it is not a whole-book audit. Its simplified positivity wording is not imported as a theorem for semidefinite Q. Willems1972 DOI10.1007/BF00276493, https://link.springer.com/article/10.1007/BF00276493, was read at abstract/metadata scope for the storage-and-supply framework, not all31 pages. The old Kalman1960 citation actually identifies A New Approach to Linear Filtering and Prediction Problems, DOI10.1115/1.3662552, and cannot support the chapter's unqualified historical LQR attribution. That unsupported attribution was removed rather than replaced by invented history.

All four text pages of Megretski's MIT6.245 lecture4 Using H2 Optimization were read: https://ocw.mit.edu/courses/6-245-multivariable-control-systems-spring-2004/b49e52078cd6ec5648ce2d66dff869f2_lec4_6245_2004.pdf. The available text has an inconsistent perturbation/sign expression around4.1.2, so the canonical negative-feedback return-difference and eta identities above were independently derived rather than copied. Doyle1978 Guaranteed Margins for LQG Regulators, https://authors.library.caltech.edu/records/e39yd-evs06, was checked at repository metadata/abstract scope (IEEE TAC23(4),756–757, DOI10.1109/TAC.1978.1101812). Both attempts to open its PDF returned internal errors; no full-paper reading is claimed. Its abstract supports the bounded no-universal-margin claim.

Tedrake's https://underactuated.mit.edu/robust.html was read for L2-gain and normalized robust-LQR sections, not the whole book. Megretski's lecture7 Algorithms for H-Infinity Optimization, https://ocw.mit.edu/courses/6-245-multivariable-control-systems-spring-2004/a9100bcb2c4fe5f05ef2d86007d1fd65_lec7_6245_2004.pdf, was read through PDFpages0–5 and stabilizing-Riccati theorem7.2: general plant channels, rank/stabilizability/detectability assumptions, strict bounded-real lemma, gamma feasibility and game branch. Remaining pages were not fully audited. Bishop and Del Moral, https://arxiv.org/pdf/1610.04686, version7 dated1December2018, was read for abstract/introduction through pages1–5, SDE/filter equations and time-reversal statement, not all34 pages of proofs. The journal is SIAMJCO55(6),4015–4047(2017). No source figures were visually inspected, and no complete-paper claim is made. Canonical derivations/counterexamples are independently worked, with short bounded source links rather than extended quotations.

### Validation and Publication Checkpoint

The initial TDD run produced one failure and eight fixture errors because the shared web artifact and program were absent. After implementation all nine focused tests passed, including independent SciPy DARE/trajectory checks and the counterexamples above. The print/Web program is byte-identical. A first PDF build failed on superscript2 in a code comment; replacing it with ASCII Python exponent notation fixed the encoding issue. Subsequent bibliography and repeated LaTeX passes resolved cross-references. Final additions explicitly qualify nonoptimal-reference linear terms and terminal Gauss–Newton curvature; both editions were regenerated together.

The rebuilt Volume I PDF has154 pages. All thirteen changed chapter pages, PDF88–100 / printed74–86, were rasterized and visually inspected in seven spreads. This includes the complete generated table, all code lines, the final qualifications and eleven exercises. No overfull line, unresolved reference or rendering defect occurs in this chapter. Remaining warnings in other chapters and the MiKTeX update notice are outside this correction. Full-suite and browser results will be appended after completion. No commit/push occurred during active tests or rendering.

Variational PR #4235 protected-squash-merged as fa87e53e5057cbf535e1879b9046f829a523f7ce at12:33:35UTC. Deployment still needs confirmation. The duality working branch starts after variational metadata4a4d3dd5693e5f71d85f8b154522e40a0d11d44b; replay only this new wave onto protected main before first push. No duality PR or deployment is claimed at this checkpoint. The405-source full audit remains open.


Final local validation: the first full run with60s per-test timeout terminated in the pre-existing benchmarks/test_benchmark_simulation.py::test_benchmark_ddp_mock_initialization while evaluating Hessian norms; it is not reported as a passing suite. Rendering was completed before retrying with CI's120s allowance. The full retry passes4,291 tests,29 skips,129 deselections and59 warnings in287.52seconds, coverage92.36percent. All nine new focused tests,34 static contracts,128 content-lint cases with four skips, Ruff, Black100 and configured mypy74 pass. The broad custom quality scan included temporary untracked authoring/QA scripts and reported118 issues there; the explicit changed production/test file scan passes. Those temporary files are not part of the PR. Date-only generated artifacts were restored after semantic comparison. No implementation was changed to accommodate the benchmark timeout.

Browser QA inspected all114 section views across38 headings in1440px light,390px light and1440px dark. There are270 MathJax containers and40 displays, no math errors or document overflow in the checks. All13 contact sheets were inspected. The scoped CSS retains17.78px math in269px mobile display containers;31 overwide equations were verified scrollable. All31 overwide display left edges were explicitly reset and checked against their containers, and an additional left-edge screenshot was inspected. Twelve title/table/code-end/terminal-curvature targeted views were inspected, plus mobile right edges at table445px,code479px,equation142px and the final exercise. The helper's scrollIntoView can shift a wide table to intermediate columns; initial full-section and explicit final-edge views establish access to every column. Inherited console messages and floating utility controls remain visible; this is not a zero-console-error claim. The entire current chapter is complete in print and web; its protected merge/publication is still pending.


Publication/synchronization update: variational PR #4235 deployment34122526556 completed successfully. Duality commit78a6c19cabdc7f3d221d7d1d94fb0b7f5d35a7e2 passed all commit hooks. The planned rebase stopped because AGENT_HANDOFF.md again had an unstaged normalization-only status with an empty textual diff. The shell's following push command nevertheless ran and completed, including every applicable pre-push hook and unit tests. An attempted process lookup found no remaining matching push to interrupt; the remote branch was already created. No history rewrite was used to repair this sequencing error.

After refreshing the handoff index, protected main was merged normally. Its complete tree was independently verified byte-identical to the prior variational metadata4a4d3dd5693e5f71d85f8b154522e40a0d11d44b. The four merge conflicts were therefore safely resolved to the reviewed duality versions: HANDOFF, VolumeI PDF, REVIEW and corpus index. Mergec13c715e3da2f119b63274c85481e9f4b9fa5381 has an empty tree diff from reviewed78a6c19c; no content or tests changed. Merge hooks passed and the normal push completed. PR #4237 is open with protected auto-squash enabled; its final diff is the intended twelve-file duality wave. Publication remains pending. Keep future dependent commands sequential so a failed synchronization cannot fall through to a push.

Both complete Physics kinetic-chain editions have now been read (print9,580/web8,767 original words), including all examples, callouts and exercises. Issue #4238 records sequencing/power attribution, moving-grip release, ground-wrench axes, coupled modes/X-factor evidence, energy/efficiency and drift/instruction errors. An initially entered route batch#4056 was checked against the actual issue titles and corrected to Physics#4054. The claim was free and is held through15:10:37UTC for the current codex session. Canonical correction has not started and is outside PR #4237. No nested AGENTS/CLAUDE files exist under the Physics book. A separate partial read of VolumeI/main.tex also observed pending appendix claims (commutation described as necessary, SciPy as a generic LMI solver, truncated import example); the entire appendix is not yet reviewed and remains part of the full-corpus work.


## Kinetic Chain — Full Paired Review, Issue #4238

Both complete original sources were read in sequence, including every callout, table, figure source, interpretation and exercise: print9,580/web8,767 original words. Canonical chapter30 is compiled Chapter26 in the Physics PDF. Claim was checked free and posted for codex/session technical-review-20260906 through15:10:37UTC. Branch fix/4238-kinetic-chain-rigor starts after duality metadata35dd08e890cfb25331691f0c8a28f2da34a83741; replay only this wave onto protected main before first push, after all tests/builds/renders finish. No kinetic PR/publication is claimed at this checkpoint. Original checkout and immutable upstream monograph remain untouched.

### Derivations and Decisions

The prior universal whip narrative, unsupported phase percentages, post-contact club-speed timing, angular/linear speed comparisons and peak-velocity/peak-acceleration equivalence are replaced by declared segment definitions, disjoint energy partitions, frames, event times, rate definitions and uncertainty. Euler angle rates require the angular-velocity map E; the corresponding generalized moment is E^T times the physical moment, preserving power. A wrist is a joint region, and a shoulder-line measurement may overlap thorax mass. No cohort-independent sequence is assumed.

Segment mechanics start from COM Newton–Euler and the inertial derivative of angular momentum. The body-coordinate equation retains omega cross Iomega. Power uses force dotted with the material point velocity plus couple dotted with angular velocity, not a torque-only work formula. Equal/opposite joint forces cancel on summing bodies when their application-point velocities agree, though individual boundary powers can transfer energy. A joint actuator's combined couple power uses distal minus proximal angular velocity. Ideal constraint power, actuator work, spring storage and damper loss are explicitly distinct.

For the spring/damper example, separation0.1rad, speeds5/3rad/s, k100Nm/rad,c2Nms/rad give transmitted torque14Nm. Proximal/distal powers -70/+42W sum to -28W, with20W storage and8W dissipation. Thus proximal power loss is not automatically distal power receipt. M^-1 times a force residual has acceleration units, and multipliers are solved with constraint consistency; neither an undefined constraint Lagrangian nor an inverse-created force is used. Net inverse-dynamics torque is not an individual muscle solution.

The two fixed-axis rotor counterexample has I1=2,I2=.5,k100,tau50,zero initial state,zero damping/gravity. Mean rotation obeys theta_bar_dd=20 and relative rotation obeys delta_dd+250delta=25. Therefore theta_bar=10t^2,delta=.1(1-cos(sqrt250*t)),theta1=theta_bar+.2delta,theta2=theta_bar-.8delta. The first acceleration is20+5cos, always>=15; the second is20(1-cos), with its first peak atpi/sqrt250=.1986917653s. Proximal velocity is strictly increasing on[0,1], so no interior proximal speed peak precedes that distal acceleration maximum. This directly disproves universality, without denying model-dependent useful sequencing. Energy T+U exactly equals50theta1 from rest. Independent solve_ivp integration checks the full four-state solution and energy, not a duplicate analytic implementation.

The three-rotor mode problem uses Mdiag(2,1.2,.4),K[[120,-120,0],[-120,220,-100],[0,-100,100]]. It has one free rigid mode and two elastic modes; generalized eigenvectors are mass-normalized. Tests check Kpsi=Mpsi*diag(omega^2) and psi^TMpsi=I. A mode belongs to the assembled chain. Relative damping differs from damping each absolute angle to ground. Quarter-period timing does not follow without defining forcing and the compared observable.

X-factor is a declared difference in turn angles, not the angle between rotation axes. Backswing-positive alpha=thetaT-thetaP increases when pelvis turns targetward ahead of thorax. A maximum inside the window has equal turn rates; no hip-angle threshold follows. A maximum over a window including the reference event is nonnegative by definition, and a signed two-event difference is a different statistic. Zero velocities do not imply zero separation/preload; independent segment extrema need not coincide. Actual tissue energy needs constitutive/activation/routing assumptions, so a spring example is explicitly a model. All six exercises are rewritten with defined variables and solvable checks, including radian conversion for a rest-angle spring and event-aware release comparisons.

Club and hand speed cannot identify muscle shortening velocity. The muscle-tendon length Jacobian, tendon compliance and pennation intervene; eccentric/isometric/concentric conditions differ. A net grip wrench is not one anatomical wrist actuator. Two separated hand forces can provide a couple. The club energy equation retains F_H dot v_H, torque_H dot omega, gravity/elastic potentials, other external power and losses under the declared rigid/flexible model. The claim that wrists cannot drive the club is withdrawn; simulation evidence supports a bounded counterexample, not universal coaching.

The moving-grip equation is derived by eliminating F_H between m*aG=F_H+mg and COM moment balance. With rG=rH+ell*e, aG=aH+ell*alpha*e_perp-ell*omega^2*e, it gives (I_G+mell^2)alpha=tau_H+mell(g-aH)dot e_perp. The radial term has zero moment about the grip; prescribed pivot acceleration does not disappear at release. For m.2,ell1,I_G.02,theta0,omega2,vH(0,-3),aH(0,-20),tau0, alpha9.263636...,force(-.8,-.1852727...),grip power.555818...W. Independent force, COM moment and energy-rate tests reproduce it. Replacing aH by gravity givesalpha0, byzero givesalpha-8.9181818. These are different boundary conditions. The holding torque follows the same equation with theta=theta_a+phi0. A feasible bound does not impose a human binary latch. Release continuity, impulses when relevant, endpoint contact geometry/time and changed grip work are explicit. No universal late-release percentage survives.

Ground axes are right-handed x target,y transverse,z up. r cross F has yaw rxFy-ryFx. Pure vertical force cannot create yaw about z. r(.4,.2,-1),Fz1177.2 givesmoment(235.44,-470.88,0). AddFx100,Fy200,mz10 gives(435.44,-570.88,70), hence3.5Nms yaw impulse over.05s if netmomentconstant. Whole-body angular momentum about a fixed origin or total COM is distinct from pelvic angular velocity; other external moments and transport terms must be respected. Internal counter-rotation is possible with no external yaw moment. CoP does not encode all shear/free moment. Stationary ideal contact can do zero work while transmitting momentum. Force peak is neither zero force at impact nor a COM-speed maximum.

The mechanical budget retains initial energy, signed actuator work, external work, elastic/gravitational storage, body and club kinetic energies and dissipation. Raising80kgCOM.1m costs78.48J; geometry alone cannot justify deleting gravity. Positive and absorbed actuator work are separated before interpreting ratios. Example100initial+500positive-80absorbed-20loss=500final,partitioned250club/250other, gives.50club/positivework versus.595238club/network. Neither determines metabolism or remainder-as-heat. Initial energy can permit a ratio aboveone. Collision is a separately specified interval/model. Segment angular-speed reduction and negative net joint power do not identify individual eccentric muscle action.

The full state is(q,v),extended by activation/tendon states when the input is excitation. Zero torque, zero excitation and zero net grip couple are different ports/interventions. An affine instantaneous difference G(x)u is not a finite-trajectory difference once states diverge. Endpoint sensitivity uses state transition Phi and the integral of PhiG delta_u, with terminal-output derivative and event-time/impact-map terms where needed. The central golf argument is strengthened around retained state, geometry, feasible remaining control and uncertainty; drift magnitude alone does not infer latency, optimality or absent distal control. Measurement, inverse-dynamics residuals, work closure, EMG limits, trial comparisons and discriminating interventions connect the theory to testable claims. Slow-motion practice is not asserted dynamically similar, and smooth input need not mean small club jerk near constraints.

### Primary Evidence and Limits

Cheetham author-hosted original PDF https://www.philcheetham.com/wp-content/uploads/2011/11/Stretching-the-X-Factor-Paper.pdf was read as text through all11 pages, including methods, results, discussion and references. Its stated bibliography is Thomas(ed.),Optimising Performance in Golf,AustralianAcademicPress,Brisbane,2001,pp192–199,ISBN1875378375. The old wrist-paper title/journal entry was false and is replaced by the correct incollection while retaining the key. The actual study has10higher-skilled/9less-skilled right-handed golfers,5-irons,electromagnetic sensors,30Hz,pelvis/T3 turns; the old15-video-player and distance-correlation claims are removed. Group association is distinguished from unmeasured tissue/neural mechanisms in the authors' discussion. No source figure was visually inspected; none is reproduced. Author explanatory pages were read as additional context, not independent cohorts.

Vena etal. publisher abstract/metadata https://link.springer.com/article/10.1007/s12283-010-0059-7 were read; full text was not accessible through that page, and the attempted CiteSeerX PDF returned InternalError. The abstract reports sequencing support for2of5 subjects under its angular-velocity definition. Canonical prose makes only this limited statement. The bibliography note now records the sample scope. Sprigings/Neal publisher abstract/metadata https://doi.org/10.1123/jab.16.4.356 were read, not the restricted full paper: planar3segment simulation with torque generators,about9% speed improvement under optimized active wrist torque. Author is EricJ.Sprigings,notEvert; year2000,vol16(4),pp356–366,DOI added. Legacy key Sprigings2005 is retained for compatibility but publication year remains2000. Neither source is an experimental instruction to force a late wrist action.

Nesbit/Serrano2005 publisher PDF https://www.jssm.org/volume04/iss4/cap/jssm-04-520.pdf was read selectively: abstract/introduction, model/methods and work definitions, results/Tables2–3 and relevant discussion on efficiency and interface power. The14-page paper is not claimed exhaustively audited; plotted figures were not visually inspected. Four amateurs,one selected swing each,and reconstructed joint work limit generalization. Their ratio is club work/total body joint work,20.2–26.8%,not metabolic efficiency. Canonical energy equations are independently derived, rather than accepting every anatomical explanation or notation in the paper. PMC initially returned a browser check; publisherPDF retrieval succeeded. No broad inference from inaccessible sources is made.

### Initial Validation and Newly Queued Findings

TDD first produced seven fixture errors because no shared published program existed. After implementation, all seven focused cases pass, including three different grip acceleration boundary conditions. The program is byte-identical in both editions; the tests independently integrate the rotor and verify force, moment, energy and modal identities. Ruff and Black100 on the new test pass. Title-case audit passes617 sources. Initial PDF build and bibliography pass succeeded; layout QA identified long headings/moment tuple and a prose line, which were shortened/reflowed in both canonical sources. Final PDF and web builds, complete visual checks and full suite are pending at this checkpoint.

Primary-source verification also confirmed targeted misattributions in both ch14_complete_swing editions (Nesbit ratio presented as muscle metabolic efficiency/heat) and both ch22_anatomy_joint_modeling editions (Cheetham X-factor cited for lead-hip internal-rotation range). Follow-up issue #4239 records exact findings and acceptance under the same epic. Those complete chapters are not claimed audited and are not silently changed here. Kinetic findings do not close the405-source corpus or the wider Physics batch. Duality PR #4237 remains under its protected E2E check at this checkpoint; no bypass or self-approval is used.

### Final Kinetic Validation and Publication Checkpoint — 14:12 UTC

The initial complete suite had 4,296 passes and two failures: both encoded the old figure inventory. Removing the unsupported universal-sequence TikZ diagram intentionally changes the inventory to 27 chapters with figures, 28 figures, 25 TikZ pictures, three includegraphics, 28 labels, three web figures and 25 missing web counterparts. Updated only these expected counts; retained the real parity deficit. Focused mechanics plus figure-audit tests then passed all 30 cases. Final full suite: 4,298 passed, 29 skipped, 129 deselected, 59 warnings in 271.13 seconds, with 92.36% coverage. Content checks: 128 passed, four skipped. Static checks: 34 contracts passed. Configured mypy passed 74 files; repository Ruff and Black100 passed (635 files). Targeted custom quality passed after consolidating the repeated gravity constant in the new test. Title audit passed 617 sources; final SPEC changelog check passed.

The Physics PDF builds to 574 pages. Every revised chapter page (PDF 392–405, printed 364–377) was visually inspected after layout corrections. This caught a draft string-escaping error that rendered the radian-per-second unit incorrectly; both sources now use an explicit math roman unit. Long headings, a tuple and prose were reflowed. Corrected bibliography pages 557, 564 and 565 were also visually inspected. Three original index entries were retained; the final additional build exited successfully and makeindex accepted 699 entries, rejected none and reported zero warnings. No unresolved chapter references or overfull chapter boxes remain; minor underfull vertical boxes are not hidden.

Browser QA inspected 108 section views (36 headings in desktop light, 390-pixel mobile and desktop dark modes), then 18 targeted views after the unit and document-title fixes. All 190 MathJax nodes render without math errors or document overflow. The 12 wide displays retain readable 17.78-pixel type and scroll inside the 269-pixel mobile content area; both equation and code right edges were checked visually. Code, exercises and references remain accessible. One targeted run initially timed out on an off-screen theme toggle; scrolling to the top before toggling fixed the QA harness, and the final run passed. Existing utility overlays and one console error/one warning were observed; this is not a zero-console-error claim. The browser document title now uses the full chapter title without duplicating the visible heading.

All tests, builds and QA processes finished before staging or publication. Date-only generated artifacts were restored only after semantic comparison. Duality PR #4237 has protected-squash-merged as 9780f2a85bc921b73979f1a16a3de2abad7dac6b; deployment 34128830965 was still in progress at this checkpoint. Kinetic publication remains pending an ordinary protected PR. Next issue #4239 is claimed through 15:54 UTC for the identified paired citation passages; full audits of those chapters remain open. The 405-source corpus is unfinished.

Publication checkpoint: PR #4240 is open for #4238 after a successful single-commit rebase onto protected main9780f2a8. The first push passed all hooks, including unit tests; no force push or approval bypass was used. Duality deployment34128830965 has now completed successfully. Kinetic merge/deployment remain pending protected checks.

## Targeted Physics Citation Follow-Up — Issue #4239

Claim checked free and posted through15:54UTC before editing. Branch fix/4239-physics-evidence-attribution starts after kinetic metadata e6f96350; PR4240 remains under protected auto-merge. This wave reads and corrects the whole energy-budget section in both ch14 editions and the cited hip-motion interpretation in both ch22 editions. Full chapter audits remain open. This bounded follow-up removes confirmed cross-chapter errors discovered while verifying kinetic evidence; it does not replace the longest-first review queue.

Revisited the primary publisher Nesbit/Serrano PDF Methods work definition, Results Tables2–3 and discussion; the ratio is club work/body joint work in four amateur cases. It is not metabolism, ball energy or evidence of elite thermal superiority. Revisited Cheetham's author PDF sensor placement and X-factor definition: pelvis/T3 turn, not femur-relative-to-pelvis rotation. Source figures are not newly claimed inspected. Primary URLs and limitations are recorded in attribution-review.md.

The replacement energy section uses E=T_body+T_club+U_g+U_e and E_f-E_i=W_act+W_ext-D for a declared preimpact golfer/club system, ideal internal constraints, signed mechanical actuators, other external work and separately modeled passive losses. Gravity is not counted twice. A prescribed boundary or residual actuator must contribute its actual work. Positive/absorbed work integrate max(P,0) and max(-P,0) over the same interval and sum over declared actuators, giving W_act=W_+-W_-. Net joint quantities cannot identify individual muscle activity or heat. The arithmetic check independently returns 100+500-80-20=500J,250/500=.5,250/(500-80)=.5952380952. The example uses club kinetic energy and explicitly differs from the paper's club-work numerator. Every energy-store and work term has joule units.

Phase discussion now retains asynchronous segment extrema and continuing active work; a turn angle needs a constitutive model to identify tissue storage. Grip power uses force dot application-point velocity plus couple dot angular velocity, internal to the combined system. Stationary ideal ground contact can transfer momentum without work; deformation/slip needs its own balance. Impact enlarges the system to the ball; retained club/body motion is not heat, and ball energy alone does not specify flight. Final rest need not restore initial gravitational or elastic storage. Drift depends on the zero-input convention and does not supply energy or determine metabolism. These distinctions replace the original unsupported all-remainder-as-heat and reduced-muscle-work-means-more-ball-energy claims.

Hip prose now declares anatomical segment frames, relative femur/pelvis motion, rotation sequence, sign and reference posture, rather than equating global pelvis turn with a joint coordinate. Generic ranges are not individual limits; task motion and assessment conditions must be compatible. No replacement clinical range or professional requirement is invented. Neighboring categorical ROM/FAI and joint-idealization claims remain pending the complete anatomy audit. Adjacent complete-swing DCR phase numbers, constraint-work/eccentric-action and neural generalizations are also explicitly pending.

Initial web/PDF builds passed. The rebuilt Physics book has576 pages; revised energy text occupies PDF513–515 and hip interpretation PDF349. All these pages and adjacent hip page350 were visually inspected. No altered equation clips; small title/prose overfull warnings and unrelated legacy chapter warnings remain rather than a book-wide zero-warning claim. The final web QA first failed because the existing hip label was on a separate line and rendered as literal text. The source now attaches that ID to its heading and retains the previous generated anchor as an alias. The renamed energy heading retains its old anchor. Final browser checks are pending at this checkpoint. Static34 and content checks passed before this anchor-only repair; no new executable production behavior is introduced.

Final #4239 validation: the repository-prescribed tests/ suite passed4,253 cases,29 skipped,129 deselected,50 warnings in151.26s,coverage92.36%. This invocation is scoped to tests/, unlike prior root collection; it does not claim an identical collected count. Final content-lint passed128,skipped4,deselected4324 in7.32s. Static34, title617 and SPEC checks passed. All21 targeted browser views (seven targets across desktop/mobile/dark) were visually inspected. The final title/list repair was rebuilt and checked in six additional hip views; all six ROM items now render as a list, both old and corrected anchors resolve, and both browser titles use their full chapter names. One wide work-integral display scrolls188pixels to its right edge at17.78pixel font. The first edge screenshot was captured after delayed math shifted the viewport; a final rerun initialized the section math, repositioned the display and verified its actual visible right edge. Existing page utility overlays, an update notice and one console error were observed; no zero-console claim. No print source changed after the inspected final PDF.

All test/build/browser processes finished before staging. Generated dates were restored only after semantic comparison. Kinetic PR4240 remains mergeable under protected auto-merge with its E2E check pending; no bypass. The entire next9,578-word sources-of-nonlinearity article has now been read, including the appendix and lay explanations. Issue #4242 records the confirmed inconsistent inertia/Coriolis/gravity, affinity/superposition, friction/contact, muscle, fluid and control claims; checked free and leased through16:31:38UTC. No canonical edits for that next issue are included in this wave. Full ch14/ch22 audits and the405-source corpus remain open.

Publication checkpoint: #4239 is pushed as67fc6c2d and draft PR #4243 targets fix/4238-kinetic-chain-rigor while prerequisite PR4240 finishes protected E2E. This preserves the focused ten-file diff. It must not merge into the prerequisite branch. Once4240 merges, retarget to main, synchronize protected main by an ordinary merge if needed, mark ready and use protected squash auto-merge. No force push or bypass. The next issue4242 is claimed; its source is still unchanged.

Protected checkpoint14:39UTC: kinetic PR4240 merged94d8b2b427e218986008cceb12cd177853f4c067 at14:37:14UTC after every check passed. Deploy34133950325 is in progress. The incoming main tree was proved identical to prerequisite e6f96350 (empty tree diff). Ordinary merge into4239 conflicted only in three logs/spec files and generated PDF; retained the newer tested HEAD versions because they already contained that exact prerequisite tree. The staged diff against82b7c9d1 was empty, with no unresolved files. Merge7607acbc records protected ancestry without changing the validated content, and all hooks passed. No force push, bypass, discarded user edit or silent rebuild. PR4243 will now target main and proceed through ordinary protected validation.

## Complete Sources of Nonlinearity Review — Issue #4242

### Scope, Claim and Publication Turnover

Read all1,007 original source lines/9,578 words, including the abstract, every catalog entry, synthesis, lay explanations and notation appendix. No separate print edition or figures were present. Issue4242 records seven grouped findings under epic4009/corpus4021/mechanics4058; claim checked free and posted for codex/technical-review-20260906 through16:31:38UTC before editing. Branch fix/4242-nonlinearity-rigor starts after a4cdbdbf, the attribution metadata checkpoint. Canonical source is now rewritten, not merely queued. Temporary drafts, builders, primary scans and QA artifacts are explicitly excluded from staging.

Attribution PR4243 retargeted main, became ready and enabled protected squash auto-merge after its ancestry-only merge. It merged419f94964fecafe8035c4a9f7543002cee31c751 at15:06:55UTC with all current protected checks green. The earlier cancelled run and its failed aggregate were superseded by the complete passing rerun. Kinetic deployment34133950325 was cancelled when this new main revision started deployment34136596818; the newer deployment contains both corrections and is still pending. This is not claimed as a successful kinetic-specific deployment. No force push, bypass or self-approval.

### Mechanical Derivation and Corrective Decisions

Use a planar two-link model throughout: q1 from downward vertical, q2 relative, COM distances r1/r2 and COM inertias I1/I2. Differentiating the two COM positions gives translational Jacobians; add I1*v1²/2 and I2*(v1+v2)²/2. Hence A=I1+m1*r1²+I2+m2*r2²+m2*l1², B=m2*l1*r2, D=I2+m2*r2², M11=A+2B*cos(q2), M12=D+B*cos(q2), M22=D. Positive-up potential yields the declared positive gradient g and applied force -g. A rigid physical pendulum at a fixed pivot has constant pivot inertia, contrary to the original example. Test geometry independently reconstructs M for three poses and differentiates it centrally to obtain Christoffel terms.

The velocity-quadratic vector is B*sin(q2)*[-2*v1*v2-v2²,v1²]. Use C=B*sin(q2)*[[-v2,-v1-v2],[v1,0]], whose Mdot-2C is skew. Combining kinetic and potential derivatives gives E_dot=v·u. Important correction to an initial concern: C is nonunique; the original alternative C could yield the same correct Cv for a standard M even without the selected matrix skew identity. The fault is not that every alternative C is invalid. The original mass matrix and force-vector/matrix combinations were inconsistent. The article now explicitly separates c from C and the gravitational Jacobian from gravity torque. The full drift has upper block v, not identity; full G has upper zero and lower M^-1 B. Acceleration increments add at one complete state with the baseline counted once, while evolving finite trajectories do not generally add.

For rigid constraints, substitute Ma=r+Bu+J^T*lambda into Ja=gamma. W=J*M^-1*J^T and lambda=W^-1*(gamma-J*M^-1*(r+Bu)) give a=P*(r+Bu)+M^-1*J^T*W^-1*gamma, with P=M^-1-M^-1*J^T*W^-1*J*M^-1. Independent constraints and feasible reactions are required; mode changes need a new solve. Impact equations now declare normal relative velocity, both colliding bodies or a stationary boundary, normal restitution and feasible impulse. Prescribed moving surfaces require a velocity bias and can supply energy. Rigid does not imply plastic, and an active-constraint change does not automatically change ambient state dimension.

### Friction, Actuators, Storage and Fluids

Rest-friction example balances the total drive plus other loads. With mass1, static5 and kinetic4, (drive,load)=(3,0),(6,0),(-6,0),(-4,4),(3,4) produces accelerations0,2,-2,0,3. Static reaction has finite force and zero instantaneous work at rest; a sliding Stribeck law or tanh regularization is not exact static holding. LuGre retains bristle deflection with a length-valued g_f; the resisting-force sign and stiffness conversion are declared. A play operator clips prior output to the current positional gap: commands[0,.4,1,.4,-.4,0] produce[0,0,.5,.5,.1,.1] for half-gap.5. It is not a full motor/load torque law. Deadzone is a continuous memoryless command map; two .75 commands versus1.5 at threshold1 distinguish command nonadditivity from force addition.

Clipped command and realized torque are different ports. DC motor current is a retained state with L*i_dot=V-Ri-ke*omega and torque=kt*i; back-EMF is not loss of the torque/current proportionality in that model. Speed, position, current and power restrictions are distinct. A mechanical power bound gives no torque limit at zero speed. Transmission torque and velocity ratios must be dual for power conservation. Prestress depends on retained state, commanded load or boundary work; radial centrifugal tension is not axial spinning.

Cubic spring potential differentiates to its restoring force; tangent stiffness differs from secant stiffness and a negative quartic coefficient is only local without stabilizing terms. Two-rotor elastic storage gives E_dot=tau_m*qdot_m-tau_l*qdot_l; a relative damper dissipates c*(qdot_m-qdot_l)². Open-path hysteretic work includes storage, whereas a closed stable cycle can measure loss. Kelvin–Voigt needs no extra internal state beyond deformation; Maxwell force-state z gives U=z²/(2k), U_dot=z*v-z²/c. Constant complex stiffness is bounded to harmonic positive-frequency e^(i*omega*t), with cycle loss pi*k*eta*X² at fixed amplitude, rather than invented instantaneous acceleration damping.

Aerodynamic force uses relative air velocity and dimensionless lift/drag coefficients. The old rho*A*CL*(omega cross w) lacks a length. A local spin-parameter coefficient CL=cS*R*|omega_perp|/|w| yields .5*rho*A*cS*R*(omega cross w), with a declared regime and zero-cross-product limit. The independent coefficient arithmetic1.2*.0015*.2*50²/2=.45N is illustrative. Moving air can supply ground-frame body work. Wake state and added-mass velocity terms cannot automatically be replaced by an independent sinusoid or constant inertial correction. Current-state heating Ri² and commanded-current heating Ru² demonstrate why augmentation does not guarantee affinity.

### Muscle, Control and Golf Interpretation

At fixed fiber length and speed, Fmax*(a*f_l*f_v+f_pe) is affine in activation. The Hill shortening equation (F+aH)*(vs+bH)=(F0+aH)*bH gives vmax=F0*bH/aH and normalized denominator vmax+(F0/aH)*vs, correcting the inverted curvature factor. F0=100,aH=25,bH=1 gives100,37.5,0 at speeds0,1,4; the hyperbola identity and endpoints are independently tested. The values are algebra examples, not physiological parameters or an eccentric law.

With a_dot=(e-a)/.04 and a(0)=.6, zero excitation leaves a=.6*exp(-t/.04), so at20ms activation is.3639 and fixed illustrative active force40*a is14.56N. Excitation acts first on the retained activation state; a torque-off intervention differs. Pennated massless series balance uses Ft=Fm*cos(alpha), and lmt=lt+lm*cos(alpha). Implicitly solving fiber velocity can destroy activation affinity even when the algebraic fixed-speed force expression is activation-linear. Local antagonists F1=a1*(F0-k*r*q),F2=a2*(F0+k*r*q) give torque r*F0*(a1-a2)-k*r²*(a1+a2)*q; equal activations cancel torque at q0 while r=.03,k10000,a1=a2=.5 give9Nm/rad restoring stiffness. Force sums remain valid. These bounded models distinguish net moments, co-contraction, force, work, metabolic cost and retained history.

For a nominal smooth mode, A=Df+sum(u_j*Dg_j), and matched-state terminal variation integrates Phi(T,s)*G(xbar(s))*delta_u(s). Outputs need Dh, event timing and reset derivatives; different mode order defeats one smooth-mode formula. Linearization includes operating input and any nominal defect. Full-state G is2n by n, so computed torque cancels the acceleration equation rather than inverting G. Output feedback linearization needs relative degree, rank, internal dynamics and feasible commands. Impedance control has no unconditional guarantee. The bicycle example now distinguishes speed-independent curvature tan(delta)/L from lateral acceleration v²*curvature.

The golf argument connects preparation, retained activation/storage, geometry, feasible action, remaining time and output sensitivity. Grip-force work depends on application-point velocity; equal pose or club speed does not establish equal internal state. Continuing muscle work and energy redistribution coexist. A drift norm does not prove a neural latency, universal proximal sequence or negligible wrist authority. Ball contact is an impact, not backlash; the club remains hand-constrained. These mechanisms must be measured or modeled jointly before a proposed swing explanation is treated as validated.

### Primary Reading and Validation Boundary

Checked Tedrake's two-link convention and selected generalized-speed, constraint-elimination and impulse equations. The independently reconstructed COM model translates his pivot-inertia convention. Read LuGre primary model-definition pages419–420 (PDF3–4) visually, including the bristle figure and Eqs1–4; no whole-paper review claim. Read Millard2013 abstract/model introduction and visually inspected PDF2–3, including model force/pennation and activation equations and Figs1–3. DOI10.1115/1.4023390 is verified; PubMed and the scan footer disagree on article-number formatting, so the reference omits that number. OpenSim activation implementation and branches are distinguished from the simplified constant-time-constant example. NASA is used only for the lift coefficient convention; MathWorks only for positional backlash behavior.

Hill1938 DOI endpoint returned403. The original university-hosted scan was accessible; selected printed pp160–162 establish isotonic shortening, dimensions of a/b, experimental frog-muscle scope and the limitations of extrapolating toward lengthening. No60-page reading claim. No empirical results or source figures were copied into the article. Seven reference entries identify the purpose and limit of each source.

Eight initial tests failed at the missing-program fixture (TDD red), then all eight passed after publication of the program and after the full rewrite. Black100/Ruff and617-title checks passed. Final source is8,247words with53 headings;72 original rendered heading targets are retained through59 explicit aliases plus unchanged generated IDs. Final rebuild and all-section desktop/mobile/dark QA are in progress, followed by complete repository/static/content validation. No print artifact is applicable to this standalone article. Full corpus and next longest machine-learning chapter remain open.

### Final Nonlinearity Validation and Next Review

Root full suite passed4,306 tests,29 skipped,129 deselected,59 warnings in200.21s; coverage92.36%. Content-lint passed128 with4 skips and4332 deselections in6.63s. All34 CI contract checks passed. Ruff, Black100 (636 files), configured mypy (74 files), title617 and SPEC checks passed. The quality-check CLI has no --help option and treats positional arguments as Python sources: the exploratory help invocation and accidental QMD/CSS inputs failed as such, not as article syntax validation. Checking the actual extracted Python and test then identified the repeated gravity literal; it is now the declared GRAVITY_M_S2 constant. Final Python quality passes and all eight focused mechanical tests pass after that behavior-preserving extraction. No full-suite rerun is claimed after this final constant-only change.

Final article render succeeded. All53 headings were captured and visually inspected in desktop1440, mobile390 and desktop dark modes (159 views). All72 original heading targets resolve;256 MathJax containers/45 display equations show zero math errors and no document-wide overflow in these views. Twenty-two wide displays reach their actual right edge at17.78px font, with max360px horizontal movement, and every resulting screenshot was visually inspected. The eight-row/three-column table scrolls445px within its269px mobile container; both its right side and last rows were inspected. Nine code start/middle/end views were inspected across the three modes. Final constant-only rebuild was verified by a browser assertion on the published code, left/right scroller checks (213px desktop and539px mobile, code font15.75/15.56px), and actual screenshots. The first final desktop-left capture showed the table because delayed MathJax layout moved the viewport; it is excluded and a fully initialized/repositioned capture replaces it. Existing utility overlays and an update notice were visible; one console error/two warnings remain, so this is not a zero-console or site-wide UI certification.

Generated registry changes were semantically compared after all tests/builds completed. The reader-study diff was JSON reformatting plus generated date, not changed observations; the date/format-only files were restored after comparison. No immutable monograph changes. All source text, model distinctions, equations, numerical examples, references, synthesis and lay explanations were reread for consistency. No print edition applies. Publication is pending an ordinary protected PR; the405-source corpus is not complete.

While validation ran, read the entire next Volume0 machine-learning chapter9,456 inventory words and its complete1,100-word web companion, including the figure, Python program, references and all16 exercises. Confirmed findings are recorded in issue4244 under epic4009/corpus4021/Geometry4055. It is checked free and leased through17:23:33UTC before any future edits. Its reward favors hanging while claiming upright; PPO clipping is falsely a hard ratio limit; policy evaluation is confused with optimality; n-step bias/variance is reversed; adjoint and learned-mechanics guarantees lack assumptions; code mishandles partial/oversized batches; g/l identification is confused with separate g and l. The editions' scope differs substantially. No canonical machine-learning changes are included in this wave.

Publication checkpoint: ready PR #4246 contains the complete nonlinearity wave. Original commit b371f9c7 was rebased before its first push onto protected main419f94964fecafe8035c4a9f7543002cee31c751, producing3b03d5d3998019c463ec559b8c6404d80fd06e48. The old base a4cdbdbf had exactly the same tree as protected main; the before/after commit trees are identical. Hooks passed on commit and push; no force push or concurrent test/render. SPEC is now keyed to PR4246. Protected merge and deployment remain pending. Attribution deployment34136596818 completed successfully at the checked419f9496 revision; it includes both attribution4243 and kinetic4240 corrections, superseding the cancelled kinetic-only run. The405-source corpus remains open.


## Complete Machine Learning Chapter Review — Issue4244

Both Volume0 editions are now reconciled after their complete readings. The full technical derivation, primary-reading boundaries, TDD evidence, numerical checks, failures and corrections are recorded in [machine-learning-review.md](machine-learning-review.md). This wave links the statistical/control methods to the golf investigation through state retention, actuator input choice, task objectives, physical power and identifiability. It does not infer human physiology from a learned simulated trajectory.

Validation: root4331 passes and one bibliography parser failure, resolved by field formatting and29 focused passing tests; coverage92.41%. Final static34/content128 withfour skips; mypy75 and Python quality pass. Final web and292-page print builds succeed. All111 section views,28 mobile equation edges,57 final targets,six code-left captures,19 chapter pages,three bibliography pages and corrected TOC inspected. Detailed failure history includes duplicate amsmath labels, two line overflows, missing helper docstring, bibliography parsing, dark appendix background and stale localhost service-worker CSS. Generated semantic data were compared before restoring date/format-only changes.

Nonlinearity PR4246 is now protected-squash-merged as e56a4b3bb7a97230bcafb99d0d559f1f614349a4 after all checks passed; deployment34142203580 remains in progress at this checkpoint. Machine-learning publication is pending; keep the405-source corpus open.

Machine-learning publication hook checkpoint: the first push failed in isolated pre-push mypy because the implicit NumPy Array alias was not recognized without NumPy installed. An explicit typing.TypeAlias declaration fixes that environment; the exact hook and normal mypy pass. Runtime and chapter excerpt are unchanged. Initial PR creation failed because the rejected push had created no branch. Full details and pre-push rebase tree-equality evidence are recorded in machine-learning-review.md. No hook bypass or force push.


Publication checkpoint: the corrected alias and all commit/pre-push hooks passed; commit6f3dfb95 was successfully pushed without force. Ready PR #4247 now contains the complete machine-learning wave and references issue4244, epic4009, corpus4021 and Geometry4055. SPEC is keyed to PR4247. Protected merge and deployment remain pending.


### CI Image Accessibility Follow-Up

The first PR4247 head passed all Python checks and all eight textbook builds, but CI34145727144 failed its every-route gate: the network figure had a caption but no HTML alt attribute. Added explicit fig-alt to the canonical Quarto figure. The same verifier now passes four local evidence items (1366px and390px, light/dark), with no serious/critical axe violations on the scanned route. This is a four-view chapter check, not a second local239-route run. The first local attempts found a missing manifest, an unrelated unpruned draft lacking an H1, then an expired local server; the first running-page check also exposed the legacy polyfill normally removed by deployment. Built a one-page manifest from the actual rendered page, restarted the localhost server and applied the existing deployment polyfill filter before the successful check. No gate was weakened. Nonlinearity deployment34142203580 has separately completed successfully.


## Complete Degrees of Freedom and Robot Models Review — Issue4248

Both complete Physics editions are corrected after the9,436/8,605-word original readings. The chapter now connects anatomical reduction, constraint rank, club task geometry, force/torque transmission and experimentally distinguishable control hypotheses. [dof-urdf-review.md](dof-urdf-review.md) records the full derivations, independent counterexamples, primary-source reading boundaries, TDD and failure history. The paired chapters, corrected model diagram, downloadable2R XML and shared example code agree; all seven exercises are well-posed. This is not a complete physiological human simulation or proof of a neural optimization strategy.

Validation: full root4,361 passes plus one stale figure-count expectation, repaired with53 focused passes; coverage92.47%. Static34,content128/four skips,mypy76 and Python quality pass. All168 section views, eight mobile equation right edges,54 final targets,20 print chapter pages and five bibliography pages inspected. Retained all67 actual live anchors,21 labels and22 index entries. Final566-page Physics PDF and web build succeed; print contents spacing repaired after visual inspection. Four deployment-verifier views pass with zero serious/critical accessibility violations. Initial manifest path error caused404s and was corrected; no false page-failure attribution.

Machine-learning PR4247 is protected-squash-merged as0f71111543bc907cfd749a60732a8db6abc90f3c after all new checks passed; deployment34149681983 completed successfully through build, verification and publication. Nonlinearity deployment34142203580 succeeded. This chapter's protected publication is pending. The405-source corpus and final whole-book consistency work remain open; the immutable upstream monograph was not changed.

PR4249 publication update: the initially missed separate Python quality lane failed on gravity literals. Reviewed remote repair7b9fe5f7 using the existing9.81 constant; all30 focused tests and targeted quality pass. All new protected CI checks passed and PR4249 merged as1c45083fe4a5ef4de98a8a6fde569912e3fefadf at19:08:44UTC. Deployment34154444770 is running. The exact local static-coverage gap and untracked-QA scan limitations are recorded in dof-urdf-review.md.

## Complete Screw-Axis Review — Issue4250

Both introductory Geometry editions are rewritten after complete9,433/897-word original readings. The paired treatment now distinguishes finite relative displacement, instantaneous velocity and time-varying paths; reconciles angular-first conventions, body/space derivatives, adjoint direction and inverse-transpose wrench transformation; derives point-load power and grip subsystem interpretation; separates reciprocal motion/wrench pairing from geometric screw products; and solves a fully specified four-bar through separate free-body balances and virtual power. All15 exercises are corrected, including the former false hinge-reciprocity claim. The web now contains the complete substantive treatment. [screw-axes-review.md](screw-axes-review.md) records all findings, derivations, primary-reading boundaries and validation failures.

TDD22 physical/contract cases fail before implementation and pass afterward; independently executed published output agrees. Ruff/Black, targeted quality, mypy and34 static contracts pass. Full root tests and final print/web QA remain in progress. All55 original print labels and24 index terms are retained; old web anchors are explicitly preserved for rendered verification. The vector diagram follows a specified projected helix. First source-build guards caught missing labels/index terms before writing canonical sources; the first corrected print build exposed a LaTeX row-break ambiguity, which was fixed. No full-corpus completion or whole-book layout certification is claimed.

Publication update: predecessor PR4249 deployment34154444770 completed successfully through publication.

Screw-axis final local evidence: full root4,384 passes,29 skips,129 deselections; configured src+scripts coverage78.59%, same collected src-only coverage92.5%. New module100%. Static34,content128/four skips,mypy77,Ruff/Black and targeted separate quality pass. All111 section views,22 equation edges,42 final targets,three final heading views and all15 corrected print pages plus contents/bibliography inspected. Full284-page Volume0 PDF converged. Final mobile diagram refinement retains readable labels in a keyboard-focusable region; final verification follows. Detailed counts, source-reading limits and inherited whole-book warnings are in screw-axes-review.md. Protected publication remains pending.

Final presentation checks: all 34 static contracts pass again. The exact CI content command (`pytest --override-ini addopts= tests/ -m content_lint --timeout=120`) passes 128 tests with four skips and 4,365 deselections in 13.31s. An earlier invocation mistakenly selected the nonexistent content marker and ran no tests; it is not evidence. The final browser verifier passes all 14 viewport/theme cells with zero serious/critical axe violations. All jobs finished before staging, commit or push.
