--- .github/workflows/ci-standard.yml
+++ .github/workflows/ci-standard.yml
@@ -250,3 +250,3 @@
       - name: Audit Python Dependencies
-        run: pip install pip-audit && pip-audit -r requirements.txt --progress-spinner=off
+        run: python3 -m pip install pip-audit && python3 -m pip_audit -r requirements.txt --progress-spinner=off
       - name: Critical Module Coverage Gate
